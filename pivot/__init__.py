"""Pivot — the recoverability instrument (epic.pivot, done-line 0031).

The deterministic half of the benchmark: it builds the cube frame,
the calibration populations, and the grader, and never calls a model.
Inference is played by cold agents; results land as records.
"""
