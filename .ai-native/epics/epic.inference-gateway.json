{
  "epic": {
    "id": "epic.inference-gateway",
    "owner": "bdo",
    "arc": "Every inference the loop runs becomes a routed, receipted, fallback-able act against an admitted mind. Today the loop has exactly one way to think — a bare `claude -p` inside the gate pen — and the day it was first exercised in anger it jammed twice (600s timeouts, issues #95/#96) and a session had to fill the seat by hand. The gateway is the local-first, AI-native answer: not LiteLLM (a dependency-heavy proxy with YAML for a brain) but what LiteLLM would be if it were a loop citizen — the mind registry (loop/minds.py, built and waiting) as the control plane of admitted, bdo-signed records, and one small gateway pen as the data plane: resolve mind -> backing reference -> one normalized completion call, every call a receipt (mind, prompt_hash, latency, tokens, outcome). Local is the privileged default (zero disclosure); an external family is the deliberate, receipted exception; a failed mind falls back by admitted rule, never by a stuck gate.",
    "context": "Born from bdo's direction of 2026-06-12 ('we should probably build a local-first AI-native gateway... inspired by LiteLLM, not LiteLLM') the same day the single-provider seam double-timed-out. The substrate reserved every seam this needs before it was asked: loop/minds.py (atom.model-registry.v0, experience-layer wave 2) already admits minds with local/external families, secret-refusing backing references, and the odysseus:// scheme — bdo's own Odysseus workspace (Desktop/odysseus-sacrificial) serves local models via vLLM/llama.cpp/Ollama and its Cookbook does hardware-fit model selection, so Odysseus is a registered backing, never a fork; the trust ladder carries the local-model and external-mind classes with no rungs yet (the privileged default and the receipted exception, in code); the runs ledger and prompt_hash lineage make every past gate verdict replayable ground truth. One OpenAI-compatible client over stdlib covers Ollama, vLLM, llama.cpp, LM Studio, OpenRouter and OpenAI in one stroke; Anthropic/claude stays one named mind among many. Infrastructure-as-code was considered and declined: there is no fleet to declare — the AI-native IaC is the log itself (minds, rungs, routing rules as admitted records). D-2 grows the which-mind axis minds.py already names: a mind may not judge output its own backing wrote.",
    "value": "The loop stops having a single point of thought. bdo chooses which minds may judge, from measurements instead of vibes; a jammed provider becomes a receipt and a fallback instead of a stuck seam and a hand-filled seat; local models carry the routine load at zero disclosure and zero marginal cost; and the same replay harness that picks the local model pays the inference-evals debt and is the convergence instrument epic.pivot needs.",
    "horizon": "The loop runs its gates against minds it chose from its own measurements. Every inference in the repo goes through the gateway pen against an admitted mind; the local default carries the routine verdicts; claude and other external families judge only where the measurements say they must, as receipted exceptions; a mind that hangs or dies is a fallback receipt, not an incident; and bdo reads one surface — which mind judges which seam, at what agreement, latency and cost — and changes the routing with one stamp.",
    "pieces": [
      {"atom": "atom.gateway-pen.v0", "glue": "the data plane: a small pen (outward reach lives in pens, never loop/) that resolves an admitted mind's backing reference (env: / http(s):// / odysseus:// / file:), makes one normalized OpenAI-compatible completion call over stdlib, and writes one receipt per call — mind id, prompt_hash, latency, tokens, outcome. Local-first in code: an external-family mind is refused unless the call names the receipted exception. No streaming until a surface needs it; a gate needs one completion."},
      {"atom": "atom.minds-first-light.v0", "glue": "the idle organ exercised: the first minds registered for real through loop.minds (bdo-signed — registering a backing is a disclosure event): the Odysseus/Ollama local backing the Cookbook recommends for this hardware, and the claude backing as a named external mind. The registry stops being dormant; the census reads it alive."},
      {"atom": "atom.gate-through-gateway.v0", "glue": "the gate pen's launch seam rerouted: gate.py composes the judging context exactly as today but inferences through the gateway against the mind the routing names, instead of a bare claude -p. The 600s double-timeout class of failure becomes a receipted, fallback-able event; D-2's which-mind axis enforced at the seam (a mind never judges its own backing's output)."},
      {"atom": "atom.replay-evals.v0", "glue": "the local-provider question answered by measurement: replay already-judged gate prompts (prompt_hash + verdicts on the log are ground truth) through each candidate local mind; report agreement, latency, and timeout-rate per mind per seam. Pays the inference-evals debt named in the five-gates arc; shares the convergence-across-independent-inferences shape epic.pivot's instrument needs. The output is a report bdo reads, and the routing he stamps cites it."},
      {"atom": "atom.routing-rules.v0", "glue": "the LiteLLM-inspired part made AI-native: routing and fallback as admitted records — which mind for which seam, fallback order, budget caps — folded at call time by the gateway pen, superseded never erased. The control plane bdo steers with one stamp; no YAML, no config file outside the log."}
    ]
  }
}
