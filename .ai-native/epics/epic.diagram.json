{
  "epic": {
    "id": "epic.diagram",
    "owner": "bdo",
    "priority": "bdo's arc, named 2026-06-19 ('Just epic.diagram') in the working session that located the existing diagram-author skill (Desktop/SubProtocol/vault/skills/diagram-author + _theme/svg/compose.py) and resolved to port it INTO ontum 'turned up to 11'. It is a COMPOSING arc, not a from-scratch build: it ports a real, tested renderer and reuses ontum organs that already exist (authoring.js, term_economy.py, the value-gate/§10 discipline, the causality Pattern Commons) and refuses to double-build any of them (§10). Awaits bdo's confirm-arc before the pieces are carried.",
    "arc": "ontum gains a first-class diagramming ORGAN: a governed surface that captures subject-matter intent and renders honest architecture diagrams. The port is shaped by the membrane architecture this session designed — the diagrammer is built AS that architecture: a generative pole (describe/gesture + bounded inference) feeds an ingress GATE with teeth (the diagramming canon, refusing), which gates a deterministic body (compose.py: spec -> SVG, the fold), standing on truth (the repo, in projection mode). The horizon is not a prettier drawing tool: it is a shape-transfer instrument whose diagrams carry an SME's intent faithfully and can REFUSE a dishonest picture with a cited reason — the thing draw.io structurally cannot be.",
    "context": "Three things this arc must hold, each a correction the repo's own second-set-of-eyes (D-2) made when this design was reviewed on 2026-06-19 by three independent reviewers citing real files: (1) THE SME IS A NAMED CANON, NOT THE data-visualizer. The data-visualizer advisor is the *charts* canon (it ranks encodings of MAGNITUDE) and is an advisory persona pointed at a substrate (ontogrammatic/html_design_tokens.py) that resolves nowhere on disk; an architecture diagram encodes CONNECTION and TYPE, no magnitude. The governing SME must be the architecture-diagramming canon, named: Moody's *The Physics of Notations* (semiotic clarity, perceptual discriminability, semantic transparency, complexity management, cognitive integration, visual expressiveness, dual coding, graphic economy, cognitive fit), graph-drawing aesthetics (Purchase's empirical crossing studies; Battista/Eades/Tamassia), and C4 (Simon Brown) for the containment/zoom genres. The data-visualizer is demoted to a SUB-advisor, summoned only for genuinely quantitative genres (e.g. sankey). (2) AUTO-LAYOUT IS NOT AN EXTENSION — IT IS REFUSED. compose.py is explicit-position BY DESIGN (taste.md refuses auto-layout: 'produces unpredictable diffs when one node is added'), which protects diff-stability — ontum's byte-deterministic grain. The arc adds at most a deterministic layout AID (tier-declaration -> grid-snap: you declare rank, the tool does the arithmetic), never a constraint solver / orthogonal router. (3) DO NOT DOUBLE-BUILD. New module diagrams/ sibling to causality/ — never an extension of causality/canvas.js (would break the live causality preset). Reuse the schema-driven DISCIPLINE, not the artifact. Any diagram-from-truth projection folds INTO causality/term_economy.py, never a parallel diagram_economy.py. Consult the live causality/patterns/pattern-commons.v1.json (53 patterns), do not invent. THE LOAD-BEARING GOOD IDEA, all three reviewers agreed: the 'taste gate with teeth that REFUSES' — and it half-exists already (qa.py exits 1, detects edge-through-node via Liang-Barsky). The whole arc is the smallest honest path from that to a governed organ.",
    "value": "A diagram in ontum becomes an act on the record, not a drawing: it carries whose intent it serves, which canon governed it, and what it refused — and a picture that lies (orphan node, untraceable hub, invented notation, edge through an unrelated node) is refused with a cited 'why' the same way the off-log gate and mock-shame refuse. The architecture this conversation kept iterating stops going stale, because in projection mode the diagram is a fold over the repo, never a second source of truth. And the SME-intent loop (describe -> render -> the SME confirms the picture carries what they meant) makes the diagrammer a shape-transfer instrument, the visual form of the design-conversation discipline.",
    "horizon": "Done at epic scale: ontum has diagrams/ with a deterministic compose.py spec->SVG floor; a refusing gate (qa.py promoted to the ontum hook grain) whose every refusal cites the named canon; one layout aid (tier-declaration grid-snap) that keeps diffs stable; generative authoring (describe -> schema-valid diagram spec, validated before render, reusing the authoring.js pattern); projection-from-truth folded into term_economy; recursive zoom for complexity; the causality look served live; and a governed, growing diagram Pattern-Commons family. A genre is added only when two real diagrams refuse to fit the existing ones (the §10 test) — never to fill a zoo. The first increment proves the gate-with-teeth + named canon + one genre on a real ontum content shape, requiring none of the rest.",
    "pieces": [
      {
        "atom": "atom.diagram-floor-gate.v0",
        "status": "LANDED (PR #322, rcp.merge.322) — the floor, the canon, the refusing gate, as ONE cell",
        "glue": "The deterministic spec->SVG floor (compose.py, explicit-position, byte-deterministic), the named architecture-diagramming canon (canon.md: Moody + Purchase graph-drawing aesthetics + C4), and the refusing gate (qa.py: deny = exit 2 + cited principle on stderr, fail-open). Shipped as one work-particle, not three (bdo's grain note, 2026-06-20): the gate imports the floor's closed NODE_TYPES (I-4) and every refusal cites the canon by name, so none can be judged apart. Done-line 0139. The §10 pair holds: the honest pipeline state-machine passes; a one-orphan variant is refused citing [graph-drawing: no orphans]."
      },
      {
        "atom": "atom.diagram-structural-plane.v0",
        "status": "STRUCTURE-FIRST cell 1 (in flight, done-line 0151) — the plane and its boundaries before the fixtures",
        "glue": "bdo's structure-first redirect (2026-06-20): the planes and boundaries themselves come before the fixtures, features, and attributes — snap and genres were trying to stand on structure that did not exist. Make REGIONS first-class declared structure: a node belongs to a boundary by declaration (node.region == region.id), not by where its rectangle sits. compose.py renders regions as structural containers (deterministic); qa.py gains check_region_membership citing the canon's C4 containment rule (a node claiming a region not declared -> deny, the structural analog of an orphan; a node geometrically outside its declared region -> deny). Backward-compatible: a spec with no regions behaves exactly as before."
      },
      {
        "atom": "atom.diagram-composition.v0",
        "status": "STRUCTURE-FIRST cell 2 — larger systems: nesting + the holonic move (recursive zoom, pulled forward)",
        "glue": "Composition and containment as first-class structure: a region nests inside a region, and a node IS itself a sub-diagram (the holonic move). Recursive zoom — a node expands into its own gated sub-diagram, each level carrying its 'what is deliberately not shown' caption (Moody complexity-management + cognitive-integration) — was the old wave 4; structure-first pulls it forward because composition is structure, not experience. Each level is gated by qa.py the same way; depth follows C4's levels (context -> container -> component). The bond: this is separable value from cell 1 (you can have flat declared regions without nesting), so it is its own cell."
      },
      {
        "atom": "atom.diagram-attribute-model.v0",
        "status": "STRUCTURE-FIRST cell 3 — authorize the system to hold its attributes",
        "glue": "bdo: 'authorize the system to be capable of holding its attributes.' Extend the diagram SCHEMA so nodes and edges can HOLD their structural attributes before any feature manipulates them — inherit causality/canvas.js's already-declared holonic node fields (sites / space / strata{fundamental,derived,learned} / anima{strength,tempo}), do NOT rebuild them (§10, no double-build). The model carries the slots; population by later cells. The bond: orthogonal to region structure (cells 1-2), so its own cell."
      },
      {
        "atom": "atom.diagram-gridsnap.v0",
        "status": "a CONSEQUENCE of the plane, not a first requirement — snap to a region grid that now exists",
        "glue": "Now downstream of structure (bdo's reorder): snap answers 'snap to WHAT?', and the what (the plane + its regions) exists only after cells 1-3. Tier/slot-declaration -> deterministic grid-snap within a declared region: a node declares a tier and order, the tool computes x/y from the region's column step + margin; diffs stay stable (add a node -> only its column shifts). Explicitly NOT a constraint solver, NOT orthogonal routing (taste.md refuses auto-layout) — authorize the system to HOLD structure, never to INVENT it. Gutter-suggestion may ride here; full Sugiyama/ELK is deferred to a named offline dependency if ever needed."
      },
      {
        "atom": "atom.diagram-first-genre.v0",
        "status": "one genre, now standing on real structure",
        "glue": "Ship exactly ONE new genre whose ontum content shape genuinely refuses the flat node-graph (the pipeline STATE MACHINE or the summon->judge->receipt SEQUENCE), now built on the structural plane + regions + composition rather than jammed in front of them. Carries the mandatory 'what is deliberately not shown' caption. Earned genres beyond this are added only when two real diagrams refuse to fit (§10) — the membrane cross-section is explicitly NOT a genre (invented notation, refused by graphic economy / semantic transparency); it stands as a one-off authored diagram only."
      },
      {
        "atom": "atom.diagram-generative.v0",
        "status": "wave 2 — the generative pole (verified-clean reuse)",
        "glue": "Generative authoring: describe/gesture -> bounded inference -> a schema-valid diagram spec -> validated BEFORE render. Reuse causality/authoring.js's pattern directly (validateSpec is schema-agnostic and tested — refuses 6 malformed specs; this was the one reuse the repo verified clean), feeding it the diagram SCHEMA and a diagram-vocabulary prompt. Natural language is never truth: it produces a typed draft the gate (atom.diagram-gate.v0) still judges. Closes the shape-transfer loop with a confirm step (the SME verifies the rendered picture carries their intent — the R7 comprehension discipline, visual)."
      },
      {
        "atom": "atom.diagram-projection.v0",
        "status": "wave 3 — projection-from-truth (folded in, never parallel)",
        "glue": "Point the organ at the repo: a diagram becomes a FOLD over real evidence (module/membrane structure, pipeline, log spans), so the architecture diagram regenerates from truth and cannot drift. DO NOT build a parallel diagram_economy.py (§10): fold the diagram projection INTO causality/term_economy.py as a classified projection of a term/relationship graph, resolving every node/edge against committed bytes and refusing what does not resolve (term_economy's existing grip teeth). Acceptance: given a fixed repo state, a deterministic diagram spec with stable ids; a citation that resolves nowhere is refused, not drawn."
      },
      {
        "atom": "atom.diagram-experience.v0",
        "status": "wave 4 — complexity + the served surface",
        "glue": "Recursive zoom (a node expands into its own gated sub-diagram, each level with its 'what is not shown' caption — Moody's complexity-management + cognitive-integration; the mechanized form of taste's 'split into one diagram per shape, linked in prose'). The causality look-and-feel (cream paper, hand-drawn marker, anima) as a reskin of the renderer, served live. A governed, growing diagram family in causality/patterns/pattern-commons.v1.json (consult first, deposit exemplars; the experience skill's ritual, not its external foundry commons). The experience layer is skin + surface, never counted as diagramming rigor — the rigor stays in the gate."
      },
      {
        "atom": "atom.diagram-canvas-first-cut.v0",
        "status": "in-flight — the editable-canvas first cut, PR #709 (done-line 0192)",
        "glue": "CTA-A = A1 (bdo's stamp on .ai-native/proposals/diagram-canvas.proposal.md, 2026-06-23): the smallest first cut that BRIDGES the diagrammer's two halves — the deterministic renderer + refusing gate (the rigor) and an editable, schema-driven, persisted graph engine. diagrams/ gains canvas.html + canvas.js (a diagram SCHEMA mirroring compose.py's part-types; select / move-writes-x-y / schema-driven inspector / toJSON-fromJSON to localStorage + file; declared show/hide/lock/reorder layers, membership declared not geometric), and an export path that runs the qa.py preflight so the canvas cannot ship a picture the gate refuses. The canvas EDITS the compose.py spec (the spec stays truth; the canvas is an editor over it) and reuses causality/canvas.js's schema/persistence DISCIPLINE by pattern, never the artifact (§10, no double-build). compose.py honors declared layers behind `if spec.get('layers')` (backward-compatible, byte-identical for existing examples); qa.py gains check_layer_membership (a part on an undeclared band refused, cited under C4 containment). The §10 pair holds: an honest layered spec renders, a layer-orphan is refused; the canvas round-trips through toJSON/fromJSON with teeth. Done-line 0192."
      }
    ]
  }
}
