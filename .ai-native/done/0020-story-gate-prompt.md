# Done-line 0020 — the story gate's versioned prompt — the Reader of cold-reader stories

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `.ai-native/nodes/story-gate.claude.v1.md` exists as a §7 node prompt that grades a candidate body against the pattern commons — the five movements, cold-readable, refusing pointer/rip/synopsis — uses `route_back` as the loop edge that carries the author's next move, requires being handed the source alongside the prose, and ships with a contract test in `tests/test_prompts.py` (verdict set, source-handed requirement, cold-reader bar, five movements) plus its semantic-eval debt named: the two live Reader runs (edition 1 `route_back`, edition 2 `conform`) on the record. Wiring it into the pipeline as a real first stage is the named next increment, not this one.
