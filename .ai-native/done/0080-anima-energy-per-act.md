# Done-line 0080 — Anima's energy/tempo: an act's cost, folded from compute already on the log

Written before code, per §9.4. When this line is met, stop.

This is agenda [[anima]]'s **energy/tempo** dimension — *what an act costs* — its first taste. It folds compute the log already carries; it captures nothing new. Anima's *rhythm/breath* dimension (the orchestrator's heat/cool) is already sensed by `loop/temporal`; this is a sibling, not a re-derivation.

> **Done when:** A pure read-only fold (`loop/energy.py`, stdlib only, the `loop.gaps`/`loop.temporal` grain) reads the energy *already on the log* — `latency_ms` and `tokens` on receipts — and renders **energy-per-act**: the beat (latency) and yield (tokens) of each act, aggregate pulse (count, total/mean/median latency) and tempo (tokens/sec over token-bearing acts), grouped by mind and by outcome, each measure citing the receipt ids it folds (the census / term-economy citation discipline). It captures **nothing new** — an act is any receipt already carrying `latency_ms`; an absent field is absence, not zero (a jammed act burns the beat and yields nothing). The teeth (§10): a **strain** fold names energy spent without yield — *burned* acts (latency spent, outcome≠ok or no tokens → the beat happened, nothing came out) and *fallback* acts (`fallback_from` set → a second act's energy spent for one answer) — and reports the wasted latency; tempo is computed over yielding acts only, so a burned act's huge latency can never masquerade as throughput. Proven by a test: synthetic receipts where a clean act, a burned act, and a fallback act are each locally valid yet refuse to fit one productive picture — strain separates them, tempo excludes the burned one, division-by-zero is impossible, and an empty log folds to "no acts carry energy yet" (absence, not a fake zero); plus a controlled literal on the *real* committed log — the qwen3 timeout `rcp.eacd7effeb7b` (182s, zero tokens, issues #95/#96) reads as burned. The full suite green; a merge receipt.

> **Non-example:** capturing new timing/tokens (this folds compute already on the log, zero new capture); counting a burned act's latency as productive tempo (a divide that lies); treating an absent `tokens` as a zero yield-rate (absence is not zero); a constant or fabricated metric (a fold that does not read the receipts); re-deriving the orchestrator's heat/cool (that is anima's *rhythm* dimension — `loop/temporal` senses it; this is *energy/tempo*); or a crash / `needs-you` on an empty log (the fold reads what is there, honestly).

> **Arc:** can't point at a named arc yet because none exist under agenda [[anima]] — bdo names the converging arcs (energy/metabolism is the one this serves). When he names the energy arc, this tie firms to it; until then the association is the agenda itself, drawn here honestly rather than invented (absence is information).
