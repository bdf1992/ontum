# Done-line 0122 — the reconcile verb — the land-chain cascade fix, branded

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `pr.py reconcile <n>` exists as a branded PR-pen verb that, in an isolated worktree (never the viewport a parallel session may hold), merges the advanced trunk into a confirmed PR's branch — letting the `.gitattributes` `union` driver resolve the append-only logs that GitHub's server-side merge cannot — and hands the branch off through the same suite + branded-push gate as any session, while REFUSING any real (non-log) conflict back to a session on the branch (the merge-node lands work, it never authors a resolution). A test pins both refusals (wrong target; a real conflict naming its path) and the full suite is green.
