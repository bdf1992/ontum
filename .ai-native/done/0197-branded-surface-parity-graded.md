# Done-line 0197 — Branded Claude surface — Phase 1 parity, self- and peer-graded, landed as a PR

Written before code, per §9.4. When this line is met, stop.

> **Done when:** the parity checklist (`vscode/ontum-surface/PARITY-CHECKLIST.md`) reads **18/18 green with checkable evidence** — OR the bridge spike has recorded an honest `STOPPED-infeasible` finding — AND `vscode/ontum-surface/GRADES.md` carries both a self-grade and an independent `/code-review ultra` peer grade plus an empty `## bdo's grade` section — AND the work is opened as a **PR** to main (never merged; the merge-node lands confirmed arcs, and this arc is unconfirmed).

This is the marker the overnight administer loop builds toward (`ADMINISTER.md`).
The target is `epic.digital-experience` / `.ai-native/proposals/branded-claude-surface.proposal.md`. The first increment is the engine-bridge spike; you cannot mark a driving row green over a bridge the spike has not proven. Faking green fails the grade (§10).
