# Done-line 0006 — the epic layer: bdo steers arcs, not tickets

Written before code, per §9.4. When this line is met, stop.

> **Done when:** epics are first-class records (`.ai-native/epics/`)
> carrying the arc told fully, its context, the value in the owner's
> terms, the horizon (what done looks like at epic scale), and the named
> pieces with the **glue** — how each local piece composes into the arc;
> an atom files under an epic via `incidence.serves` *or* the epic's own
> pieces list (so settled atoms retro-file without re-judging: the epic
> points at them, their hashes never move, D-5); the CLI inbox and the
> web page brief **epic-first** — the arc above the items, the glue on
> each card — and unfiled atoms still render; and the directive ships as
> its own exemplar: two epics retro-covering everything built so far,
> plus `atom.epic-layer.v0` briefed, judged by the real L0, waiting at
> bdo's stamp grouped under its epic.

**bdo's sizing rule, on the record (chat, 2026-06-10):** "stories should
carry much more — epic level ideas and context, not individual tasks or
tickets… you can employ larger swaths of value at once as long as they
have local pieces that glue into value." So: one version may carry an
epic-sized swath when each local piece lands its own receipt. §9.3's
node rule is untouched — no second *real node* before the first has a
passing receipt; this sizes *value*, not gates.

Phase-2 echo, noted not coded: many atoms settling into one epic is the
vault's scale-recursion (onta → ontum, ontum-evolution §4) showing up at
the work layer.

Next, not now: epic-level verdicts (stamping an arc as a unit), epic
progress sensing in the orchestrator's pressure, real quality gates
(bdo's reservation: L1/L2/eval/confirm are still mocks).
