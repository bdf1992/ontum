# Done-line 0132 — Parity teeth that prove the claim, not just the file

Wave-1 follow-on for `epic.repoprompt-parity`. The matrix's §10 teeth (`loop/parity.py` `validate()`) only proved that a `have` row's cited *file exists* — not that the file does the claimed thing. That blind spot let one row stand on a false claim: `multi-root workspaces -> have -> field.py --all`, noted as folding "ontum + odysseus + holonsearch", when `field.py --all` folds every arc *within* ontum and reaches none of the three sibling repos. A `have` citing a real file with a false claim is a ghost-in-spirit the existing teeth cannot see — exactly the locally-fine row §10 says a gate must learn to refuse.

> **Done when:** every `have` row in `loop/parity.py` carries a required `evidence` substring and `validate()` refuses a `have` whose evidence is absent from the cited file's bytes (not merely a missing file) — the term-economy grip rule pushed from *existence* to *claim*; the multi-root row, whose three-repo claim does not resolve under those teeth, is re-verdicted `build` citing a new piece `atom.multi-root-fold.v0` added to `epic.repoprompt-parity`; `tests/test_parity.py` gains a non-vacuous tooth (a `have` citing a real file but with an absent evidence substring is caught) and the committed matrix passes the strengthened check; and `python -m loop.parity` is green with the new work-particle announced, independently judged with a receipt, and PR'd for the merge-node.
