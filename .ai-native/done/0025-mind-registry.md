# Done-line 0025 — the mind registry: pluggable judging minds as admitted records

Written before code, per §9.4. When this line is met, stop.

> **Done when:** a judging mind is an admitted record, not a code literal —
> `loop/minds.py` folds `mind` admissions (local the privileged default,
> external the receipted exception) and lists which minds are registered;
> minds are admitted through the registry's own pen (`loop.minds register`,
> the surface-registry sibling), bdo-only, with the backing recorded as a
> *reference* and a raw inline secret refused; and the no-self-signing line
> grows a which-mind axis — a pure rule that a mind may not judge output its
> own backing wrote, even across an API. §10 proof: the registry *refuses* —
> a raw-secret backing is rejected, a non-bdo admission is rejected, and
> same-mind judging is refused.

## Out of scope, named (later increments)

- **The reach** — actually invoking a registered model lives in a pen under
  `.claude/` (the reflect split); this increment registers minds and rules
  them, it does not call them.
- **Receipt wiring** — recording which mind judged on each receipt, so the
  no-self-sign rule fires inside the loop, is the branded-spawn-rail's job
  (the next wave-2 piece).
- **Interaction surfaces** (odysseus) — registered through the existing
  surface-registry sibling; this increment is the mind axis.
