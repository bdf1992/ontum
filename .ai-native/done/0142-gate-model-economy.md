# Done-line 0142 — the gate randomizes its model and the run ledger folds cost by model

Written before code, per §9.4. When this line is met, stop. bdo's direction this session (after the rail fix, done-line 0138 / PR #288): instead of pinning one gate model, have the rail **randomly choose a model per run**, record the model and its cost, and let an audit **compare cost vs impact**. His chosen shape: **cost-only first** (randomize + record model+cost+verdict, audit on cost and the verdict breakdown; the impact score is deferred, named, not faked), the pool is **all three** (`claude-opus-4-8`, `claude-sonnet-4-6`, `claude-haiku-4-5`), and the reports surface through the **sourcing/run stats** (`loop.runs`) rather than a bespoke surface — compose, don't double-build (§10).

This builds on the now-fixed rail (stacked on PR #288) and on the existing run ledger `loop/runs.py` (the surface that already asks whether a run "did anything worth a damn"): the gate-economy data rides the `run` event it already writes, not a second ledger. Scope guard (§10): cost is truth captured from the `claude -p` envelope; the audit reports it honestly per model and must NOT fabricate an impact number while impact is deferred — a run with no recorded cost confesses, it is never counted as $0.

> **Done when:** `.claude/skills/gate/gate.py` selects the judging model **uniformly at random** from a configured pool (env `ONTUM_GATE_MODELS`, default the three ids above; a set `ONTUM_GATE_MODEL` still PINS one, overriding the draw) on each launch, and `launch_claude` parses the run's **cost** (`total_cost_usd`) and token usage from the `--output-format json` envelope and carries the chosen model + cost into the run record; AND `loop/runs.py` `record()` accepts an optional `model` and `cost` (usd + input/output tokens) written onto the `run` event, with matching `record` CLI flags the gate pen passes; AND `loop.runs` grows a **per-model cost breakdown** in both its render and `--json` (per model: run count, total and average `total_cost_usd`, token totals, and the movement/verdict mix already folded) — cost-only, the impact column explicitly marked deferred, never fabricated; AND `tests/test_gate_economy.py` joins the suite with §10 teeth: the model picker draws only from the pool and honors the `ONTUM_GATE_MODEL` pin override (a pinned env yields exactly that model, an unpinned draw stays within the pool over many draws), and the cost fold groups cost+runs by model and reports a model whose run carried NO cost as unpriced (not $0), while a fabricated/constant cost fold fails the test. Read-only fold except the one `record` write; impact stays deferred and is named, not invented (D-4).
