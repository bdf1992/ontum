# Done-line 0129 — Mark-label on the gap fold — epic.three-marks wave 1

Written before code, per §9.4. When this line is met, stop. This is wave 1
of epic.three-marks (sketch-ink-paint.proposal.md): the cheapest proof the
framework reads true against the live backlog — a pure relabel of an
existing read-only fold, no write path touched (decision 1: prove the
label before any doctrine sentence is minted).

> **Done when:** `loop.gaps` attaches a `mark` — one of `sketch` / `ink` /
> `paint` — to every gap it yields, derived from the gap's `kind` by one
> fixed mapping (`KIND_MARK`), and `render` shows it; and a fixture test
> proves the mapping reads true across the gap kinds, including the planted
> §10 case a naive "name contains `.mock` → sketch" rule would mislabel —
> a `.mock` *writing* actor is an ink-authority leak (`ink`), not `sketch`
> — which the test catches. The full suite is green.
