# Done-line 0125 — recommend — /ask expanded into a generative branching tree, with the code bound

**Goal:** bdo, 2026-06-18 — asked for "a version of the ASK skill called recommend" that "composes a complex and informed recommendation with options, mostly multi-select and themes with routing decisions… compositions, multiple patterns over it… 20 options and several routes… until we have a generative compositum." Through two corrections (rejecting a "document + decision-pens" render and an "inverse ask" mis-read) he named the true shape: **multiple tabs of 4 options with routing, where one route prompts a second set of checkboxes** — a **generative branching tree** of `AskUserQuestion` panels. He chose "build the generative step now." **Arc:** serves `epic.owner-harness` — the synchronous owner-decision surface, scaled from `/ask`'s single fork (done-line 0119) to a routed decision-space; it composes `/ask`'s law, it does not double-build it (§10).

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `recommend` exists as a skill sibling of `/ask` with three coherent, cross-referencing parts. (1) A **skill** `.claude/skills/recommend/SKILL.md` (prompt-as-code) names the shape: a compositum is a generative branching tree of panels (each ≤4 tabs × ≤4 options); a route selected in a tab **generates** the next set of checkboxes, composed live by the session (the inference) from the route + the gesture; every panel is recommendation-first and narrated. It states the architectural fact that the composer **must be the session** (prompt-as-code), because `AskUserQuestion` is a session tool no subprocess can reach. (2) A **policy** `.claude/skills/recommend/policy.md` inherits `/ask`'s whole law (R1–R7 + refusal teeth) as its floor and adds the tree rules RC1–RC5 (routed tree, routing-generates-next, bounded generation, session-composer, real-decision-space-not-dump) with its own refusal teeth. (3) A **code bound** `.claude/skills/recommend/compose.py` that runs the deterministic refusal check on every generated panel **before it renders** — REUSING `ask_guard.shape_problems` for the R1/R2 floor (I-4, one definition) and adding the tree bound (the 4×4 cap + header cap). Its `--selftest` proves the §10 teeth: the bound passes a well-formed panel and refuses a malformed one. The generative step is **demonstrated this session** by walking at least one branch live (a route prompting a generated next panel). The discipline shares the registered memory `ontum-ask-surface-discipline` with `/ask` (one home, two directions).

> **Non-example:** a flat menu of 20 options crammed toward one modal (violates the 4×4 cap and RC1); a "document + decision-pens" render where a static artifact holds the composition and the modal only polls it (bdo's rejected v0.1 shape); an "inverse ask / confirm-not-select" framing (my rejected mis-read — recommend SELECTS through routes, it does not present-and-confirm); a `compose.py` that re-implements `shape_problems` instead of reusing it (a twin that drifts, violates I-4); wiring `compose.py` as a second `PreToolUse` hook beside `ask_guard` (two guards on one seam — enforcement-as-a-hook is `ask_guard`'s separate seam, owned by the gateway-spine).

> **Out of scope — named (next increment, its own done-line):** the **generative composer's learning loop** — folding the audit trace (shared with `/ask`, done-line 0119's named next increment) for *generation misses* (a route whose generated branch bdo abandoned or unchecked wholesale), the signal the composer must learn from; and any decision to wire `compose.py`'s bound into live `PreToolUse` enforcement (that rides the gateway-policy-spine, not this piece).

> **Evidence expected:** the three files above + the `MEMORY.md` index line for the shared memory updated to name recommend; `compose.py --selftest` green; at least one live branch walked this session; a backed atom with an independent value-gate receipt (D-2); a numbered session report; a merge receipt.
