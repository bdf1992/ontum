# Done-line 0043 — Handoff: the third gate refuses a hollow atom (the second deterministic gate)

@
Written before code, per §9.4. When this line is met, stop.

> **Done when:** the L2 handoff seam has a real, deterministic backing that can
> refuse. A pure-stdlib fold in `loop/handoff_gate.py` reads a single atom's
> `story.text`, `incidence.touches`, and `incidence.hands_off_to` and returns
> **`send_back`** when any of the three an implementer cannot start without is
> missing (naming which), **`ready_for_spec`** when all three are present — the
> *third* real gate and the second of the law kind, where the value gate (first
> light) is judgment and placement is the field. It is exercised §10 by a test
> where an atom locally fine at every earlier gate (it had value, it placed
> cleanly) is **hollow** at handoff — a surface with no declared downstream —
> and the gate `send_back`s it while passing a complete atom, and that test
> **fails on a fixed-verdict mock** (the §10 teeth — a check that cannot refuse
> is not a check). The verdict reaches the log only through `loop.node judge`
> (no second write path, D-4), carrying the `prompt_hash` of the versioned law
> at `.ai-native/nodes/handoff-gate.det.v1.md` (§7). This is a piece of
> `atom.gates-enumerated.v0` under `epic.experience-layer` (de-mock the
> remaining gates; mock temperature toward 0/5; every gate carries a non-accept
> verdict in its history).

## Out of scope, named (bdo's stamp, not this session's)

- **The `node_real` admission that makes it the live gate.** Flipping
  `handoff-gate.mock.v0 -> handoff-gate.det.v1` live is bdo's realness stamp
  (done-line 0028 scopes arc-confirmation to the owner-stamp stage, not
  `node_real`). The gesture path is now the `realness-intake` skill: bdo closes
  the stage's realness-confirm issue with a comment and a session admits it
  real on his authorization. This session builds and proves the backing; the
  gate goes live when he stamps, and the first real `send_back` on the live log
  follows that stamp.
- **Judging the story's substance.** v1.0.0 decides only the *presence* axis
  (the cheapest real refusal). Whether a present plan is *good* is judgment, a
  later inference extension — the same shape value-confirm will take.
@
