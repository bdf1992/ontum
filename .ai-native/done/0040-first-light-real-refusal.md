# Done-line 0040 — First light: a real gate's refusal receipt on main

Written before code, per §9.4. When this line is met, stop.

> **Done when:** the real value gate (`value-gate.claude.v1`, admitted real
> by bdo, adm.4f5b1dc678ee — not a mock returning a constant) has launched a
> mortal `claude -p` mind against `atom.rename-vars.v0` and that mind, reading
> the composed log context, returned `reject_no_value` — the first refusal
> receipt on the log written by a node that *could have accepted* (§10). The
> enforcement proof needs both poles, so the same real gate also accepted a
> genuine owner-value atom: one real `accept` and one real `reject` on the
> log, from the same prompt-hash, proving the verdict tracks the content and
> not the turnstile. Both receipts ride the branch through the merge-node onto
> `main`; each headless run left its trust-rail GitHub issue (born before the
> run, closed by the verdict). The hollow `_note` on rename-vars that *claimed*
> issue #47 refused it stops being aspirational and becomes a real receipt.
