# Done-line 0038 — bdo confirms an arc by closing its GitHub issue with a comment; the system reads his intent and lands

Written before code, per §9.4. When this line is met, stop.

> **Done when:** an arc with PRs waiting surfaces as one GitHub issue bdo
> can close with a plain-language comment on his phone; a session reads
> that comment, judges his intent (confirm / decline / unclear), and on a
> clear confirm runs the confirm pen and lets the merge-node land the
> arc's PRs — always replying with how it read him and what it did, and
> never landing on an unclear read (it reopens and asks). The deterministic
> gh I/O (open, find-closed, read-comment, reply) is a tested pen; the
> intent judgment is the session's, not a keyword match.
