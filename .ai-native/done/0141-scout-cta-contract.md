# Done-line 0141 — epic.strategy-metabolism wave 1 — the ScoutCTA contract, before code

Written before code, per §9.4. When this line is met, stop. The first real slice of epic.strategy-metabolism (proposed; the rename of epic.explore-lobe rides decision A/B): the ScoutCTA *contract*, defined and proven by one hand-authored fixture and §10 failure cases BEFORE any generating loop. The read-only loop/scout.py that emits such CTAs over research-over-priors, and the Claim and Prune organs, are named in strategy-metabolism.md but built by later lines — not this one. Adopts bdo's carry-over decisions as working defaults (A Scout, B Scout→Claim→Build→Prune, C persist-as-expiring-conjecture, D handed-not-self-aiming), read from the loop's re-invocation as his go; if a default is wrong, superseding this bar is his alone.

> **Done when:** the first ScoutCTA contract is fixed and proven before any generating loop — (1) an **input contract** is written: a *handed* purpose+goal (an epic id or outcome slug) plus the allowed prior sources; Scout never self-aims (decision D). (2) The **output schema** is the strategy-metabolism.md §6 ScoutCTA shape, with `expiry` non-empty and `minted: false` / `truth_claim: false` / `review_required: true` mandatory, and every `priors_consulted` entry a `file:line` or log-record substring. (3) One committed **hand-authored fixture** ScoutCTA exists that is schema-valid, reads as a useful move, and does NOT read as truth. (4) **Failure cases** are demonstrated by a §10 check in `tests/`: a ScoutCTA whose `priors_consulted` do not all resolve on disk is REFUSED as ungrounded (the ghost refusal, reusing `causality.term_economy.resolve_evidence` — no second truth); a ScoutCTA with empty/absent `expiry` is REFUSED (a conjecture without decay is a ghost obligation); and a fabricated/constant CTA cannot pass the schema+grounding validator. (5) The **review boundary** is explicit and enforced by the marks: a ScoutCTA is conjecture — surfaced beside maintenance gaps, `review_required`, and unable to land, mint, or alter truth state (D-4); Scout develops and requisitions review, never self-lands. The generating `loop/scout.py`, and the Claim and Prune organs, are NAMED but not built by this line.
