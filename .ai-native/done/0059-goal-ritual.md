# Done-line 0059 — The /goal skill — a multi-step ritual that defines one hour+ goal over field and dialogue

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `.claude/skills/goal/SKILL.md` exists as a multi-step,
> no-new-code `/goal` ritual that derives ONE goal worth an hour+ of
> attention over two sources — the live field (only existing read-only
> folds: `python -m loop.gaps`, `.ai-native/epics/`, `python -m
> loop.digest`; it adds no Python of its own) and dialogue with bdo —
> and holds every candidate to a goal-best-practices checklist mapped
> onto ontum-native gates: **specific / outcome-stated** (names a changed
> state of the system tied to one existing epic piece, not a task list);
> **measurable / falsifiable** (a receipt could prove it — *no receipt,
> no version bump* — and it carries a required non-example plus the §10
> refusal test); **right-sized** (an hour+ unit, thicker than a ticket —
> the scale owner-harness rejects — and thinner than a whole epic;
> out-of-room means shrink and ship the smaller thing); **relevant** (it
> ladders to an existing epic's `horizon`, and when no epic fits it
> surfaces `needs-you` rather than authoring one — absence is
> information); and **time-bound** ("when met, stop" is the envelope).
> The ritual *thinks then writes*: it culminates by minting the goal as
> the next numbered done-line through the existing records pen (`python
> -m loop.pen new done`) — never a second write path, never editing a
> frozen line, never inventing a missing epic, never standing in for
> bdo's stamp. Proven this session by the §10 pair run against the live
> repo and recorded in the report: the field-derivation step actually
> executes with no new code (the named folds run and yield the
> orientation the ritual consumes), AND a plausible-but-bad candidate is
> *refused* by the checklist (a ticket-thin item, or one under no
> existing epic) rather than passing — the gate notices the misfit.

## Out of scope, named

- **Defining bdo's first real goal.** Building the front door is not
  walking through it; the first real `/goal` pass — deriving bdo's own
  hour+ goal over live dialogue and landing its done-line — is the next
  invocation, not this increment's bar.
- **A `/goal` survey / dashboard mode.** The full-ladder progression map
  across all nine epics (the earlier "survey the whole map" framing) is a
  distinct read-only view; this ritual defines ONE goal and stops. A
  survey fold is a later, separable increment.
- **Auto-starting the workhorse session.** `/goal` ends at a written,
  frozen goal; executing it (the hour+ of actual work against that
  done-line) is a separate session.
