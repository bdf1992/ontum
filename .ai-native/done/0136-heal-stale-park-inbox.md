# Done-line 0136 — Heal the stale-park phantom — the owner inbox suppresses a superseded version's park

﻿**Goal:** bdo asked "what else needs healing?" ? `loop.heal` named three stale-park over-bites (`field-topology.v0`, `gates-enumerated.v1`, `gates-enumerated.v2`): a gate correctly negated an OLD version, the LIVE version then PASSED the same gate, but the old refusal still surfaced as a live park on the owner's inbox (confirmed at session start: `loop.node inbox` showed `atom.field-topology.v0 ? parked`). The cause is precise: `gaps`, `digest`, `pull`, and `field` all already filter superseded versions via the canonical `reconcile.superseded_atom_ids` (done-line 0056), but `loop/node.py inbox` was the one owner surface that never did. **Arc:** `epic.the-field` (confirmed) ? it owns whether a thing is "a parked piece" vs history.

> **Done when:** (1) `loop/node.py` `inbox` filters atoms in `superseded_atom_ids` ? the SAME canonical definition the sibling surfaces use (no second truth, I-4) ? so a version a higher sibling replaces never surfaces as a live owner park; its receipt stands on the log as history. (2) The teeth (?10): a NON-superseded park still surfaces ? the filter is not a blanket hide. (3) The `field-topology.v0` phantom is gone from `loop.node inbox` (0 parked where it showed 1). (4) Full suite green; dogfooded as an atom on the log (atom + an independent value-gate receipt naming it, D-2); a PR.

> **Non-example:** hiding ALL parks (over-suppression ? the ?10 teeth catch this); a second/duplicate superseded definition inside node.py instead of reusing `reconcile.superseded_atom_ids` (a second source of truth ? ?10); pushing the fix into `orchestrate.next_action` (broader blast radius across the whole fold ? the heal move points at the surfaces, and the surface filter is where every sibling already lives); back-editing the log to erase the old park receipts (history is append-only; the park stands as history, only its surfacing changes).

> **Evidence expected:** the `node.py` import + inbox-filter diff; the ?10 test in `tests/test_node.py` (a superseded park hidden, a live park kept); the inbox showing 0 parked for the phantom; the full suite green; the atom + its independent value-gate receipt; a PR under `epic.the-field`.
