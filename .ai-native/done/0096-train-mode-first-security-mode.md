# Done-line 0096 — train mode: the first security mode, observe-everything block-nothing

Written before code, per §9.4. When this line is met, stop.

> **Done when:** the first security MODE — train — turns on as an admitted record, never a constant. A signed security_mode admission (type:security_mode, mode:train, scoped to a session or "*", enabled, --by bdo, superseded to close) is opened through the one admission pen (python -m loop.node mode-train start --session <id> --by bdo); a session signer is refused (no self-granted un-guard, D-4). The guard (command_guard.py) reads the posture at hook time from a pure log fold (active_mode(fold, session) in loop/reconcile.py, default normal; an unreadable fold stays normal, so train never silently un-guards). While the calling session's train mode is active the guard OBSERVES and TAGS but does not deny: every operation normal mode WOULD deny is allowed (exit 0) and recorded to the sensor trace (tool-use.jsonl) as status:would-deny carrying the fence rule it would have hit, the tags.classify intent, the surface, and the opening admission id (train_session) — instead of status:denied + exit 2. §10 proof, one op / two modes / negative control: git add . returns exit 2 denied with NO train admission (the control), and exit 0 would-deny git-add under an active train session for that session_id — divergence on the record; if the control ever allows, the check is dead and the bar is not met. bdo is never gated. The learning fold (loop/train.py) and any stricter mode are out of scope — the next node.
