# Done-line 0161 — epic.environments — the local-preview deployment face

Written before code, per §9.4. When this line is met, stop. The LOCAL-tier DEPLOYMENT face of `epic.environments` (bdo's confirmed arc, adm.b30f27b9d5ad), the sibling of the local-tier WORKING face already landed (the snapshot spine, `loop/snapshot.py`, rcp.merge.369). The local deployment env is where a session serves a named snapshot on localhost so bdo can EYEBALL it before anything leaves the machine ([[bdo-eyeballs-on-live-url]], the local face of his live-URL judging surface) — and the act is recorded as a promotion to the local environment (the local deploy ledger). A snapshot is promoted across environments; this is its first rung, local.

This piece COMPOSES, never double-builds (§10): it reads `loop/snapshot.py`'s `resolve` for the snapshot's acceptance verdict (it never re-judges acceptance — that stays the pipeline's, D-4) and reuses `loop/web.py`'s localhost-serve pattern for the actual serving. It adds only the local-deploy GATE (the teeth) and the local-deploy LEDGER (the record). The teeth: you do not preview a lie — only an ACCEPTED snapshot may be served to the local deployment env; a snapshot that resolves stale, ghost, or unaccepted is refused at the gate and nothing is recorded. Verifying the served commit equals the working tree (needs git, which loop/'s law forbids in a fold) and the git/github tiers are later pieces, NOT this line.

> **Done when:** a new module `loop/preview.py` exists (stdlib, local-first — a localhost server is local coordination, not the no-network ban's egress, the `loop/web.py` precedent; no git) that (1) `gate(name)` resolves the snapshot via `loop.snapshot.resolve` and returns deployable iff the verdict is `accepted`, else held with the resolve verdict cited; (2) `deploy_local(name, by)` on an accepted snapshot appends ONE `local_deployment` admission (environment `local`, the snapshot name + its commit + the localhost url, `by`, `ts`) recording the promotion to the local env and returns it, and on a non-accepted snapshot refuses writing NOTHING; (3) a read-only ledger fold `local_deployments()` and a CLI — `python -m loop.preview` (the local deploy ledger + which snapshots are serveable), `python -m loop.preview gate --name <n>` (exit 0 deployable / non-zero held, the CI seam), `python -m loop.preview serve --name <n> --by <who>` (gate + record the local_deployment + serve the tree on localhost via the web.py serve pattern) — each ending in a clear `done | report | needs-you`; AND the §10 test passes in `tests/test_preview.py`: an ACCEPTED snapshot deploys and records exactly one `local_deployment`; a `stale`, `ghost`, AND `unaccepted` snapshot is each REFUSED at the gate with NOTHING recorded (the two locally-fine records — a snapshot and the local-deploy gate — refuse to fit when the snapshot lies); the SAME snapshot is refused before its atom is accepted and deploys after (the gate is not vacuous); and a fabricated/constant gate fails the test. Read-only and propose-only but for the one `deploy_local` pen; acceptance is read from `snapshot.resolve` (which derives it from the pipeline's receipts), never a second authority (D-4, §10).
