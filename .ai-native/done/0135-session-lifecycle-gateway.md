# Done-line 0135 — The production session-lifecycle organ — every new session registered, watched, and softly continued within a tool-scoped gateway

> **Done when:** the continue-beat's forcing `Stop`-block is retired (the hook unwired and deleted, `loop/patrol.py` slimmed to the escalate-marker organ it shares), and in its place the production session-lifecycle organ stands: every new session auto-registers itself (a `SessionStart` hook records `session_id -> project_dir` into `~/.claude/ontum-sessions.json` — "enable for all new sessions"); `loop/watcher.py` is a pure, §10-tested fold naming which registered sessions are idle-and-gateway-eligible and emitting the exact `claude --resume -p` fire command; the soft idle reminder (`loop/retry.py` + `.claude/hooks/idle_reminder.py`) injects a gateway-bounded, decaying, session-first suggestion the agent reads and decides on (never a `decision: block`); the gateway carries a real **tool-scope** (`loop/inference.py` policy `scope`, default `propose-only`) so a permitted continue is still tool-bounded — widened only by bdo's stamp; and the network-reaching firing lives only in a pen (`.claude/skills/continue-probe/`, dry-run by default, external-cron the standing tick), never in `loop/`. The whole carries a fresh atom and an independent value-gate receipt, and the suite is green.

This is tier-1 production of the continue-probe lift (memory `ontum-continue-probe-session-lifecycle`); it supersedes the continue-beat (done-line 0127, the wrong primitive — a `Stop` block that forces continuation blind to whether bdo is present). The **observation/tier-3 lift** (the session-lifecycle as an observed fold + an inferring management agent that closes/compacts/escalates) is the named next increment, not this bar.
