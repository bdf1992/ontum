# Done-line 0158 — epic.strategy — the Claim organ (bound a conjecture into work)

Written before code, per §9.4. When this line is met, stop. The Claim organ of epic.strategy (Scout -> Claim -> Build -> Prune): the bridge from a conjectural ScoutCTA to bounded work. Claim does NOT prove the CTA — it only says "this frontier is bounded enough to work or watch." Prune (§8, its build requisitions a panel review for intent) is a later line.

> **Done when:** a new read-only fold `loop/claim.py` exists (stdlib-only, no network, no git — loop/'s law; in the loop/scout.py + loop/gaps.py grain) that, given a ScoutCTA, FIRST validates it through `loop/scout_cta.validate` (it only claims a real, grounded conjecture — an invalid/ungrounded ScoutCTA is REFUSED, never bounded) and then produces exactly ONE marked **claimed_frontier** per strategy-metabolism.md §7 — `status: claimed_frontier`, `source_cta`, `claim_type` in {arc, epic, spike, branch, watch, defer}, `owner`, `guard_policy`, `budget`, `review_date`, `exit_condition`, `expiry`. The claim is always **BOUNDED**: it carries a non-empty `exit_condition` and `expiry` (an unbounded claim is the overgrowth Prune would have to clean — refused at the door). It does **NOT prove** the CTA: it adds no truth, carries provenance that is not `minted`, and never sets `truth_claim`/`minted` true. It writes nothing (no event/receipt/admission, no mint). AND the §10 test passes in `tests/`: a valid ScoutCTA is claimed (bounded, claim_type in the set, marked proposed), an invalid ScoutCTA (e.g. missing expiry, or one that fails the contract) is REFUSED with the reason, and the produced claim never carries `minted`/`truth_claim` true. Backed by `atom.claim.v0` through the pipeline (announced + an independent value-gate verdict — no off-log bypass). Read-only and propose-only (D-4): Claim bounds, it never proves, judges, mints, or lands.
