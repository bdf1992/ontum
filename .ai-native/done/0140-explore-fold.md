# Done-line 0140 — epic.explore-lobe wave 1 — the explore lobe's read-only fold

Written before code, per §9.4. When this line is met, stop. The first real slice of epic.explore-lobe (proposed): the explore lobe's read-only fold — the forward twin of loop/gaps.py — that turns research over priors into one pointed, marked call to action. Axis-pointing across the value-manifold, the seam into summon/gaps, and any persisted conjecture trace are later lines (atom.axis-manifold.v0, atom.explore-seam.v0), not this one. This line does NOT resolve the three forks: it builds only the handed-purpose path (the minimal slice common to both branches of fork 1 — even a self-aiming organ must act on a given purpose); whether the organ self-aims (fork 1), what the manifold's true axes are (fork 2), and whether a call to action leaves a marked trace (fork 3) stay bdo's to settle.

> **Done when:** a new read-only fold `loop/explore.py` exists (stdlib-only, no network, no git — loop/'s law; in the loop/gaps.py + loop/heal.py grain: a pure fold over records already on disk) that, given a handed purpose+goal (an epic id or an outcome slug), emits exactly one marked, proposed-tier **call to action** carrying kind, subject, the move worth making, and its cited real antecedents (each a `file:line` or a log-record substring) drawn from priors — the log, the deterministic folds, and the causality relation graph; it writes nothing (no event/receipt/admission, no mint) and every emitted field carries provenance that is not `minted`; AND the §10 test passes in `tests/`: a call to action whose antecedents resolve on disk is emitted and marked proposed-tier, while a candidate move whose antecedent does NOT resolve is REFUSED as ungrounded (no antecedent, no call to action — the ghost refusal causality already makes), and a fabricated/constant emitter fails the test. Read-only and propose-only throughout: the fold never judges, stamps, or mints (D-4).
