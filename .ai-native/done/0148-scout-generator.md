# Done-line 0148 — epic.strategy wave 2 — the Scout generator (grounded teeth)

Written before code, per §9.4. When this line is met, stop. Wave 2 of epic.strategy: the Scout generator that joins the explore-fold's derivation to the ScoutCTA contract, and CLOSES review finding #1 on PR #321 — explore.py's grounding was vacuous because its arc-doc antecedent (the epic file contains its own id) always resolved, so the ghost refusal could never bite a real move. The generator's teeth must be able to refuse a real, fabricated move — not only directly-constructed test fakes.

> **Done when:** a new read-only fold `loop/scout.py` exists (stdlib-only, no network, no git — loop/'s law; in the loop/gaps.py + loop/explore.py grain) that, given a handed purpose+goal (an epic id or outcome slug), derives the forward move (reusing `loop.explore.derive_move` — no second derivation) and emits exactly ONE **ScoutCTA** conforming to `loop/scout_cta.py`'s §6 schema, **validated through `scout_cta.validate` before emission** (a CTA that fails the contract is never emitted). Its `priors_consulted` are **SUBSTANTIVE and non-self-referential**: real log records / done-lines that name the subject or the epic id, resolved via `causality.term_economy.resolve_evidence` — explicitly **excluding** the arc-doc self-reference (the epic file containing its own id/piece) that made explore.py's teeth vacuous. A purpose whose move has **no** substantive resolving prior is **REFUSED as ungrounded** (the teeth bite a real move). It writes nothing (no event/receipt/admission, no mint) and every emitted field carries the conjecture marks (status:conjecture, review_required, truth_claim:false, minted:false). AND the §10 test passes in `tests/`: (a) Scout grounds a **live** purpose — its `priors_consulted` all resolve and **none is the epic file**; (b) a **ghost** epic/subject with no log activity yields no substantive priors and is REFUSED; (c) a fabricated ScoutCTA cannot validate. Read-only and propose-only (D-4): Scout develops and requisitions review; it never self-lands.
