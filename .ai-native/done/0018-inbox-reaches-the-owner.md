# Done-line 0018 — the inbox reaches the owner: registered surfaces, reflected

Written before code, per §9.4. When this line is met, stop.

bdo's directive, on the record (chat, 2026-06-10, stamping the
epic-layer atom): "ALL apporoved, make it happen, these need surfaced
within a UI so make sure your priritzing the intent" (spelling his) —
given minutes after he looked at GitHub Issues, saw nothing, and
concluded the hook was lying. The hook was right; the queue was real;
the surface he visits showed none of it. This done-line executes the
already-stamped surface-registry story (rcp.f68f73f3fee8) at the point
of his pain: the owner's queue, visible in the UI he actually opens.

> **Done when:** external surfaces are admitted records — a `surface`
> admission through the one pen (id, kind, address, `--by`), never a
> code literal — and the owner's queue *reaches* the registered GitHub
> surface: a pure fold (`loop/reflect.py`, stdlib, no network) computes
> each registered surface's desired view (one issue per atom awaiting
> the admitted-real owner stamp, briefed arc-first, with the verdict
> set and the clear line) and the drift against what was last
> reflected (reflections are log records, so drift is itself a fold);
> the reflector pen (`.claude/skills/reflect/`, the only writer,
> `gh`-backed like the PR pen) applies exactly that drift — opens the
> issue when an atom arrives at the stamp, closes it with the verdict
> and receipt id when the stamp lands — and every applied act is
> receipted on the log with provenance; re-running with no drift is a
> no-op; the summon hook's backlog line also names drift, so a stale
> surface is surfaced ambiently rather than silently wrong; and tests
> pin the fold's purity, the no-op, the refusal to reflect to an
> unregistered surface, and the close-on-clear (§10: a cleared atom
> whose issue stays open must not fit).

Scope, honest: GitHub Issues only — it is the surface bdo demonstrably
visits and the one his amend named. The served, authenticated web
inbox (auth is still named-not-built) and ambient *write* hooks
(today's hooks never write; an auto-reflecting hook changes that
contract) are next-not-now, each its own stamped increment. Verdicts
keep flowing one way — from the log outward; the issue is a mirror,
not a second write path (D-4: clearing stays the pen).
