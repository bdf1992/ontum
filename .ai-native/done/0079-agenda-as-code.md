# Done-line 0079 — The current agenda and its named arcs are declared as data and surfaced at session start

**Goal:** `goal.agenda-board` (under `epic.owner-harness`) — the owner's agenda surface, **priority one** (bdo, 2026-06-14). This is its first, ambient cut: the SessionStart reminder of *which agenda we are on and which arcs serve it*, so we stay on-agenda and a second agenda forces an explicit declaration rather than drift. The rich board (priority-ordered roll-up with association edges, the active agenda pinned) is the same fold rendered fuller — **one source of truth**, not a second. **Arc:** serves the meta — it is the surface that holds [[anima]] and every agenda after it; Anima is its first declared agenda.

Written before code, per §9.4. When this line is met, stop.

> **Done when:** The current agenda is declared as **data, never a constant** (§7): a penned declaration under `.ai-native/agendas/` (its own `.pen.json` form, records-pen paved) names an agenda — id, statement, its **named arcs** (each: name, one-line, the epics it threads), and which agenda is **active**. A pure read-only fold (stdlib, the `loop.gaps`/`loop.summon` grain) resolves the **active** agenda and its arcs, drawing each arc's association from the **existing greppable `> **Arc:**` / `> **Goal:**` ties** rather than re-declaring it (location is not identity — don't double-declare what the ties already carry). The **existing SessionStart summon hook** surfaces it ambiently — `[agenda] current: <id> — <statement>; arcs: <names>`, and "no arcs named yet" when none — **fail-open, exit 0 always** like the rest of that hook. The reminder is data-driven by construction: it reads the declaration, never a literal, so changing the active agenda in the declaration changes the reminder, and a reminder naming an agenda absent from the declaration is impossible. Proven by a test: a fixture declaration yields the exact reminder line naming the active agenda and its arcs; an agenda with no arcs surfaces honestly; a second declaration marked active flips the reminder (proving data-driven, not constant). Anima is the first declared agenda (active); its arcs are bdo's to name and are listed as he names them — empty until then (absence is information).

> **Non-example:** a hardcoded "remind about Anima" string in the hook (a constant in code, the §7 violation this exists to prevent); re-declaring arcs the ties already carry (a second source of truth); a hook that crashes or blocks when no agenda is declared (the contract is exit-0-always); or surfacing the agenda by re-folding gaps/census instead of composing them.

> **Out of scope — named (later cuts):** bdo *naming/activating* an agenda by a GitHub gesture (the intake path, like arc-confirm) — this first cut has a session author the declaration, lifting bdo's spoken naming to intent; the GitHub-native agenda intake is its own increment. And the **rich board** itself — priority-ordered roll-up across agendas with drawn association and the active agenda pinned — of which this is the one-line ambient rendering.

> **Evidence expected:** `.ai-native/agendas/.pen.json` + `.ai-native/agendas/anima.md` (the first declaration, active); the read-only fold; the summon hook printing the `[agenda]` line in this repo; a test driving a fixture declaration to the exact line, the no-arcs case, and the active-flip; the full suite green; a merge receipt.
