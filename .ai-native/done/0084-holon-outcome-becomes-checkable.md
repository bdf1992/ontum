# Done-line 0084 — The holon outcome becomes a checkable probe-set the fold ranks

Written before the test, per §9.4. bdo's steer (2026-06-16): the strategic-horizon
outcome *Ontum models its own nodes* existed only as prose
(`outcomes/ontum-models-its-own-nodes.md`) plus a sandbox (`holonsearch`), so the
`loop.pressure` fold could not rank it and the priority lived in a session's head.
Turn the prose into a checkable probe-set so the horizon applies measurable pressure.

> **Done when:** `outcomes/ontum-models-its-own-nodes.probes.json` exists, loads with zero refusals, and the fold reads it as a real build-phase gap (a real unmet capability as top leverage, the realization probe P5 dormant until its preconditions, every probe carried as continuing pressure) — pinned by a structural §10 test that asserts the shape the fold guarantees, never a snapshot id a later session's progress would trip (the brittle-pin lesson of report 0065).

## Not in scope (the named next node)

- **compose() / multi-outcome ranking.** `loop.pressure` is still single-outcome
  (`DEFAULT_PROBES` = causality), so the holon set ranks only under `--probes`.
  Making the fold discover and compose every `outcomes/*.probes.json` so the
  highest-pressure outcome auto-surfaces in the wake hook (summon / temporal) is
  the immediate follow-on node — one real node at a time (§9).
- Building the harvester itself (that is P1, the work this outcome now ranks).
