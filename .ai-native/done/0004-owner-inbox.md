# Done-line 0004 — the owner's inbox

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `python -m loop.node inbox` shows, read-only, every atom
> awaiting bdo's real stamp — the story, its confidence tag, every
> receipt's reasoning so far (what the second set of eyes actually said),
> the legal verdicts for the seam, and the exact one-line command that
> clears it — plus, separately, atoms awaiting other summons (the control
> session's to route, not bdo's) and atoms parked by a reject (with the
> rejecting reason); **and** clearing stays the existing pen — the inbox
> adds no second write path. Live: bdo's real queue renders with the
> currently-parked atom in it.

Source: §3 (the pulley — bdo's stamp is leverage, not a chore), D-4 (the
human is the last stop, so the human stage must be fast and unmissable),
D-6 (a clear result), I-3 (the view writes nothing). Stamp: bdo, via chat
2026-06-10 ("do you have a way of showing me my open items and a way for
me to clear them?").

Next, not now: amending a rejected atom to a new version (the
`needs-amend` path), notification beyond the CLI — each its own version.
