# Done-line 0154 — Landed is not closed: a real review closes an atom, not a merge

Written before code, per §9.4. When this line is met, stop.

The conflict (bdo, 2026-06-20): the clog cure that shipped (done-0133, `atom.work-closes-in-the-wild.v0`) settles an atom the moment its PR is landed by the independent merge-node — "the landing IS the deciding acceptance" (the two-acceptances reading of D-2). But landing proves the WORK merged, not that it delivered the CLAIM. bdo this thread chose the stronger rule: the REAL review must happen — a merge is not a delivery review, and "merged = done, no review" is the rubber-stamp value-confirm exists to forbid. So settle-on-landing and the review queue (done-0150) contradict: every newly-landed atom auto-settles via the short-circuit and never enters the review queue, half-defeating it. This corrects the cause forward: a real review — not a merge — closes an atom. (done-0133 stands as history; this is the next bar, not an edit of it.)

> **Done when:** the settle-on-landing short-circuit in `loop/orchestrate.py` `next_action` (the `if atom["id"] in on_main … return None` clause, done-0133) is REMOVED, so an atom on main no longer settles for that reason alone; landed work instead flows to `value-confirm` and is closed ONLY by a real review verdict through the one pen (the review queue, done-0150). `digest.atoms_on_main` is RETAINED — it is the delivery EVIDENCE the review reads, never a settle. `tests/test_orchestrate.py`’s two settle-on-landing tests are reversed to pin the new rule as a §10 refuse-to-fit: a landed atom with its seed on the record is NOT settled (it returns a real next action toward value-confirm), and it settles ONLY once a `value-confirm` `confirmed` receipt is on the log (landed ≠ closed). The ~11 atoms currently settled only by the short-circuit re-open and drain through real reviews (paced via `gate.py drain --limit`). This lands together with done-0150 so the shortcut never undercuts the review. The doctrine implication — that the "second acceptance" (two-acceptances/D-2) is a real review of delivery, not a mechanical merge — is surfaced for bdo at arc confirmation, not silently flipped. When a merge no longer closes an atom and only a real review does, with the tests pinning it, stop.
