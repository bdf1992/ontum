# Done-line 0052 — The admitted seats are prompt-pinned

﻿Written before code, per §9.4. When this line is met, stop.

> **Done when:** the two seats bdo's open realness gestures admit (#82:
> merge-node.claude.v1; #83: story-author.session.v1) are prompt-pinned
> versioned source in `.ai-native/nodes/` (§7) — each prompt stating today's
> contract (role, what it reads, what it returns through which pen, the bar,
> what it will not do), derived from the standing records (the merge-node
> SKILL and `pr.py land`'s refusal set; issue #83's honest naming of the
> session as today's author), never invented; the spawn rail can pin both
> (its refusal for these ids is for want of a rung, never "no versioned
> prompt"), pinned by a test against the repo's own records; and the stale
> `--by merge-node.claude.v0` examples in the merge-node and arc-intake
> SKILLs say the v1 seat (versions bumped, the sharpening changelogged).

## Out of scope, named

- **Admitting either seat.** That is bdo's #82/#83 gesture; this only makes
  the seats he would admit prompt-pinned when the admission lands.
- **The rung** that lets a branded merge-node spawn pass the rail (#89).
- **Semantic evals for the new prompts** — owed with the next change to each
  file, named the way value-gate.claude.v1 names its own eval debt.
