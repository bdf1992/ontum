# Done-line 0137 — epic.environments wave 1 — the production gate

Written before code, per §9.4. When this line is met, stop. The first buildable slice of `epic.environments` (bdo's confirmed arc, PR #277): the production gate — the live URL stops changing without bdo's stamp. Today `.github/workflows/pages.yml` auto-deploys `main`'s HEAD to the live surface bdo judges on (`bdf1992.github.io/ontum`) on every push, with no owner stamp — the one promotion that escapes him (epic glue, `atom.production-gate.v0`). This line closes that gap with the smallest real teeth and in the repo's established grain: a pure deterministic checker (the `atom-invariant`/`loop.phrasing` shape) whose authority is the owner stamp (the same authority as `confirm-arc` — the gate *executes* bdo's gesture, it never invents a verdict, per the `.github/` law and the arc's second guardrail).

The spine is non-circular by deploying the **stamped snapshot**, never HEAD: production = the latest non-superseded snapshot bdo authorized, so the act of recording a stamp (a commit) cannot itself become unstamped live content. A `production_promotion` admission (`by: bdo`, a `snapshot` commit sha, supersedable) is bdo's stamp; before the first stamp the gate is in a loud **unstamped passthrough** transition (deploys HEAD, prints that the first owner promotion engages the gate) so landing this never dark-takes the live site — the teeth engage on bdo's first gesture, the way `atom-invariant` engages on his branch-protection gesture. The GitHub-gesture intake rail (a session running the pen `--by bdo` on bdo's closed-issue comment, the `realness-intake`/`policy-intake` shape) is named as the activation path, NOT built here. Waves 2–3 (preview env, staging rung, runtime parity, served control plane) are not this line.

> **Done when:** a new read-only fold `loop/deploy.py` exists (stdlib-only, no network, no git — loop/'s law; in the `loop/gaps.py`/`loop/heal.py`/`loop/phrasing.py` grain: a pure fold over the committed log) that exposes (1) `production_snapshot()` → the current production snapshot sha (the latest non-superseded bdo-authored `production_promotion`, or `None`), and (2) a gate verdict for a candidate commit sha — `promote` iff it equals the stamped snapshot, else `hold` with a cited reason — and a CLI: `python -m loop.deploy` (read-only status) and `python -m loop.deploy gate --candidate <sha>` (exit 0 on promote / non-zero on hold, the CI seam); AND the stamp is written **only** through a pen verb that refuses every non-bdo signer (`loop.deploy promote --snapshot <sha> --by bdo`, the `confirm-arc`/`supersede-done` authority shape) appending one `production_promotion` admission; AND `.github/workflows/pages.yml` deploys the snapshot `loop.deploy` names (the stamped one when a stamp exists; a loud unstamped-passthrough of HEAD until then) instead of unconditionally deploying HEAD; AND the §10 test passes in `tests/test_deploy.py`: a stamped snapshot returns `promote` and is the one deployed, an un-stamped candidate is **refused** (`hold` — the gate notices the two locally-fine records refusing to fit), a superseding promotion moves the live snapshot, a non-bdo `promote` is refused at the pen, and a fabricated/constant gate fails the test. Read-only and propose-only in the fold; the only writer is bdo's stamp through the one pen (D-4).
