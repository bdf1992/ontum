# Done-line 0033 — frozen done-lines, the painful supersede

Written before code, per section 9.4. When this line is met, stop.

> **Done when:** A written done-line can no longer be edited or overwritten in place by any session: a freeze guard denies Edit and Write on existing files in a records directory whose `.pen.json` declares it frozen, refusing loudly and naming the only path. The done directory is configured frozen. The single way to change what done meant is an additive, owner-gated, reflection-bearing supersede ritual that records a loud `done_superseded` admission, embeds the abandonment reflection in the new line as permanent history, and never authorizes a session's own change (no one signs their own line). Tests pin the edit refusal, the still-passing creation, the non-frozen directory's freedom, and the supersede ritual's painfulness.
