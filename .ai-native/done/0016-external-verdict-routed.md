# Done-line 0016 — the external verdict comes home

Written before code, per §9.4. When this line is met, stop.

Context: bdo's amend on issue #7 (the `atom.github-verdict-mirror.v0`
stamp request) has been sitting unrouted on GitHub since 06:07Z — the
exact failure mode the mirror story names. The atom's records live
only on `claude/great-faraday-djmq2x`, a sibling session's branch on a
stale base. Event and receipt ids are content-derived and the fold
dedups by id, so a byte-exact import is replay-safe under the union
merge (done-line 0007's `.gitattributes`).

> **Done when:** bdo's amend is a receipt on the local log, verbatim
> with provenance, through the one pen — the mirror atom's file and
> its three log lines imported byte-exactly from the stranded branch
> so the verdict has a place to land; the amended story exists as
> `atom.surface-registry.v0` — external surfaces registered as
> admitted records, their information reflected onto the local log,
> work running at the four named speeds (ambient automation,
> self-paced loops, chat, external surfaces) — value-gated and
> awaiting bdo's stamp, with nothing external built before that
> stamp; the GitHub surface reflects the new state by hand (issue #7
> answered and closed as parked, the successor stamp issue open); and
> the world-split — a sibling lineage stranded on a stale base with
> colliding record numbers — is surfaced `needs-you` in the report,
> not silently merged.
