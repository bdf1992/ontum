# Done-line 0049 — Anything effectively mock is mocked: the shame reaches actors without the suffix

@
Written before code, per §9.4. When this line is met, stop.

> **Done when:** anything effectively mock is *mocked* — named and shamed by
> the same surfaces that scream the pipeline mocks, with no hiding behind a
> missing `.mock` suffix (bdo's directive, 2026-06-11). `loop/gaps.py` grows
> `effective_mocks(root)`, a pure read-only fold over the record with three
> bases: **pipeline-stage** (the existing still-mock PIPELINE set),
> **mock-actor** (a `.mock`-marked node standing as an actor on the record
> outside the PIPELINE lifecycle — today the story-author seed,
> `value-loop.story-author.mock.v0`, which announces every atom's birth and
> which no surface could see), and **unadmitted-actor** (a node writing
> receipts that no `node_real` admission has ever named on either side —
> today the merge-node: 22 landings, two self-asserted ids, zero admissions).
> The gap fold surfaces them as kinds `mock-stage`, `mock-actor`,
> `unadmitted-actor` at the top of the pressure order; the shame beat screams
> the whole set, not just PIPELINE. The land pen stops accepting free-text
> identity: `pr.py land` refuses a `--by` that is not an admitted node read
> from the trunk — the prose rule "the merge-node does not move until bdo
> admits it real" becomes executable, and a superseded id no longer lands.
> `reconcile` stamps the seed event's author through the admitted map
> (`real_map.get(SEED_NODE, SEED_NODE)`) so the author seat de-mocks by the
> same one admission as every stage. Tests pin: the story-author on a seeded
> root and an unadmitted receipt-writer are both flagged; one node_real
> admission clears both its sides; an unadmitted or superseded lander is
> refused while an admitted one lands; the shame beat names a non-PIPELINE
> effective mock. Full suite green.

## Out of scope, named (its own next lines)

- **The admissions themselves.** Realness is bdo's trust, exercised by his
  GitHub gesture: this session opens the realness-confirm issues for the
  merge seat and the author seat; admitting is his tap, never this code.
- **Author-independence at the land seat** (PR author ≠ lander, per PR) —
  unverifiable while every session shares one GitHub account; stays SKILL
  prose and spawned-agent discipline, named here as a known hole.
- **The system authoring its own stories** (corpus-to-system): the author
  seat's first admission names the session as today's true author; the
  machine-author is the arc's later rung, a v2 admission when it exists.
- **The trust ladder's unbranded side door** — already surfaced as an idle
  organ by the census and the gap fold; its own line.
@
