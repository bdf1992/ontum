# Done-line 0054 — Gate launches ask the trust ladder

﻿Written before code, per §9.4. When this line is met, stop.

> **Done when:** a gate launch asks the trust ladder before any mind is born:
> `gate.py launch` refuses when `summoned-session` (the class a mortal
> `claude -p` blinks in as, D-10) holds no `judge` rung — naming the
> rung-intake gesture that grants it — and proceeds when the ladder permits.
> The pen half of atom.trust-ladder.v0's glue ("pens and the spawn rail
> enforce the rung at act time") stops being prose: the spawn rail already
> gates session-level spawns, but the gate pen births its mind from inside
> Python where no hook sees it, so the pen itself must ask. The refusal is a
> pure function over a records root the suite hits directly — refused on an
> empty ladder, permitted once a fixture admission grants the rung — and
> fail-open when the ladder cannot be read (the rail's own documented
> choice: a broken sensor never blocks, never silently grants more than the
> degraded state it reports). The rung-confirm issue for
> summoned-session/judge is opened so the freeze this creates is one tap
> from bdo, exactly like the land pen's.

## Out of scope, named

- **Granting the rung** — bdo's gesture on the issue this opens.
- **Ladder enforcement at `loop.node judge` itself** (inline session
  verdicts) — mapping "who is judging" to an agent class at the one pen is
  identity-shaped design, its own line, not a 3am rider.
- **The other classes' rungs** — local-model and external-mind launches do
  not exist yet; their checks land with their launchers.
