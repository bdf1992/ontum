# Done-line 0047 — All five gates real, every one has refused: the mock temperature reaches 0/5

@
Written before the landing, per §9.4. When this line is met, stop.

> **Done when:** epic.experience-layer's gate bar is fully met on the trunk —
> the ambient mock temperature reads **0/5** and **every** one of the five
> pipeline gates carries at least one *landed* non-accept verdict in its
> history (a gate that has never refused is a mock with a bigger bill, report
> 0004). handoff-gate is admitted real (adm.b04f3b55114f, on bdo's "yes/make it
> real" stamp), the last mock removed. Each gate's refusal is on the live log,
> attributable by prompt_hash: value-gate reject_no_value (rcp.3b62807d5f6a),
> owner-stamp amend (rcp.efd40f7169c8), placement collision (rcp.1d7e08065c81),
> handoff send_back (rcp.2b1680fc9075), value-confirm missed (rcp.be98d54d0cf2).
> The two refusals driven this round are honest and named as purpose-built: a
> re-version of the gates-enumerated atom carrying its real empty hands_off_to
> (handoff send_back), and a transparently-labelled collision probe declaring a
> real shared-address conflict (placement collision) — the §10 proofs the epic
> explicitly requires, driven not faked.

## What this closes and what it does not

- **Closes:** the "five gates real" horizon of epic.experience-layer — the loop
  now judges every seam for real and every gate can and has said no. atom.gates-
  enumerated's story (0/5, every gate refuses) is delivered; a value-confirm
  judging it afresh would now read confirmed where v1 read missed.
- **Does not close:** the arc-completion surface (the "you at arc-done" half of
  bdo's "Both", done-line 0045) is still its own line; the refusing atoms park
  for bdo (D-4) and are his to retire or amend; semantic evals for the inference
  prompts remain owed at their next edit (§7).
@
