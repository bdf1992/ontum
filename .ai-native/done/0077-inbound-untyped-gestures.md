# Done-line 0077 — An untyped gesture on a reflected surface becomes a routed log event, surfaced as a config-compiled signal

**Goal:** `goal.async-signal-surfacing` — ambient signals (and bdo's async GitHub gestures) reach the live session each turn as config-compiled, action-bearing deltas. **Line 2 of 2.** Sibling: done-line 0076 `turn-delta-signal-surfacing` (the registry + per-turn delta layer this line plugs into). Goal served: a **Story** under `epic.owner-harness`. Same shared `goal.` tie raises the goal to first-class across both lines (location is not identity).

Written before code, per §9.4. When this line is met, stop; with line 0076 also met, the goal is complete.

> **Done when:** A `surface.observed` event type (the inbound twin of `surface.reflected`) records a human gesture on a registered surface — `{surface, external_ref, author, text, ts}` — idempotent by `(surface, external_ref, gesture-hash)` so re-reading is a no-op. An inbound read fold in `loop/reflect.py` (the twin of `drift()`, pure stdlib, no network) returns a registered surface's *unconsumed* gestures (those with no `surface.observed` ack). A reader pen (the inbound twin of the reflector pen, gh-backed — network reach lives in `.claude/`, never in `loop/`) appends the `surface.observed` events. An **untyped** gesture — a comment on a digest/divergence issue, not an arc/realness/rung confirm — routes to a tracked **owner-ask** (the existing owner-ask backlog) so it cannot strand; a typed confirm gesture still routes to its intake skill unchanged. The untyped-gesture backlog is registered as one more **signal in done-line 0076's registry**, so it surfaces every turn as a config-compiled delta carrying its action (read the comment; route or run the named skill) — the same prompt-as-code discipline, not a new inline path. The mirror stays **one-way for verdicts** (those land only through `loop.node judge`, D-4); two-way only for **signals** — recorded and routed, never auto-authority, and no hook judges the comment's intent from keywords (the intake doctrine forbids it). Proven end to end: bdo's "address the faults" comment on digest #113 (or its equivalent) becomes a `surface.observed` event + a tracked owner-ask + a config-compiled per-turn signal.

> **Non-example:** a hook that guesses the comment's intent from keywords and acts on it; an observed gesture becoming an authorization without bdo's gesture through the one pen; surfacing the new gesture as a bare count with no configured next move; or putting the gh reach inside `loop/` (it belongs in the `.claude/` pen layer, stdlib stays clean).

> **Proven by a §10 test:** a new untyped gesture folds to exactly one `surface.observed` + one owner-ask; re-reading the same gesture is a no-op; a verdict is never written by this path; and the gesture surfaces through the registry carrying its configured action, never as a bare count.

> **Evidence expected:** the `surface.observed` event type and its inbound read fold in `loop/reflect.py`; the reader pen under `.claude/`; an untyped gesture on the live surface recorded as `surface.observed` and routed to an owner-ask; the new signal visible in the per-turn registry-compiled deltas; the test above; the full suite green; a merge receipt.
