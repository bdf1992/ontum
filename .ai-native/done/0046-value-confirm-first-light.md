# Done-line 0046 — Value-confirm fires: a real mind lands the first missed, refusing a false confirm

@
Written before the landing, per §9.4. When this line is met, stop.

> **Done when:** value-confirm is a *capable node* on the trunk, not a prompt —
> proven by a real `missed` verdict on the live log that could have been
> `confirmed`. The stage `value-confirm.mock.v0` is admitted real to
> `value-confirm.claude.v1` (adm.888b7582e6f9, on bdo's in-session stamp), and a
> live session acting as that summoned mind judged a real atom and landed
> **`missed`** (rcp.be98d54d0cf2), carrying the law spec's prompt_hash
> (sha256:951def…) so the refusal is attributable to the exact prompt that cast
> it. The atom judged is `atom.gates-enumerated.v1` — a re-version of the v0 the
> mocks falsely marked `value_confirmed` — driven through the now-real gates for
> the first time (value accept rcp.d7b19e1aea73, placement sound
> rcp.d4e2e3c157a5) and then refused at delivery, because its own claim (mock
> temperature 0/5, every gate carrying a non-accept verdict) is not yet true on
> the record. The refusal parks for bdo (D-4). mock temperature falls to 1
> (handoff-gate, which has its realness gesture open). This is value-confirm's
> first light, the sibling of the value gate's (done-line 0040), and the answer
> to "a node capable, or performative": a verdict that moved real state.

## Named, not done here

- **handoff realness** — still bdo's stamp (gesture issue #69 open); when he
  taps it, mock temperature reaches 0/5.
- **placement's live-log refusal** — placement is real and can refuse (shown on
  real atoms read-only), but its first *landed* non-accept receipt is still owed;
  this line lands placement only a real `sound`.
- **The arc-completion surface** — the "you at arc-done" half of bdo's "Both"
  (done-line 0045) remains its own line; a `missed` does not implicate it.
@
