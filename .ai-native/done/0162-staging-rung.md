# Done-line 0162 — epic.environments — the git-tier staging rung + promotion path

Written before code, per §9.4. When this line is met, stop. The GIT-tier DEPLOYMENT face of `epic.environments` (bdo's confirmed arc, adm.b30f27b9d5ad), across the local→git boundary bdo confirmed ("go", 2026-06-20). The LOCAL tier is complete on main (the snapshot spine rcp.merge.369 + local-preview rcp.merge.381). The git tier makes `main` the recorded "integrated, not yet accepted" STAGING rung a snapshot is promoted FROM into production, and unifies the per-environment promotion records the loop already writes into ONE readable promotion PATH per snapshot: local → staging → production.

This COMPOSES, never double-builds (§10): it reads `loop/preview.py`'s `local_deployment` records (the local rung), `loop/deploy.py`'s `production_promotion`/`production_snapshot` (the production rung — note deploy.py keys production on a commit sha, so the path resolves a snapshot NAME to its commit via `loop/snapshot.py` to join the two namespaces), and `loop/snapshot.py`'s `resolve` for acceptance. It ADDS only the middle rung (a `staging_promotion` record) and the unifying path fold. It does NOT modify the landed deploy.py/preview.py behaviour.

The §10 teeth — the promotion path refuses to fit out of order: (1) only an ACCEPTED snapshot may be promoted to staging (a stale/ghost/unaccepted snapshot is refused, nothing recorded); (2) the path is ORDERED — a snapshot may be promoted to staging only after it reached the local rung (a staging promotion with no prior local deployment is refused as out-of-order), and the path fold NAMES any out-of-order state it reads (a rung reached without its predecessor — e.g. production without staging). The github tier (preview-env, served control plane; production-gate already shipped #286) is NOT this line.

> **Done when:** a new module `loop/promote.py` exists (stdlib, local-first, no git — a pure fold over the committed log plus one pen verb) that (1) `promote_staging(name, by)` appends ONE `staging_promotion` admission (environment `staging`, the snapshot name + commit, by, ts) iff the snapshot resolves `accepted` (via `snapshot.resolve`) AND has already reached the local rung (a `local_deployment` exists for it) — else it refuses writing NOTHING, citing non-accepted or out-of-order; (2) `promotion_path(name)` folds the snapshot's path across environments — local (from `preview.local_deployments`), staging (from the new `staging_promotion` records), production (from `deploy.production_snapshot`, matched on the snapshot's commit) — returning per-rung reached/not and an `out_of_order` flag true when a rung is reached without its predecessor; (3) a read-only ledger/paths fold and a CLI — `python -m loop.promote` (every snapshot's promotion path), `python -m loop.promote gate --name <n>` (exit 0 promotable-to-staging / non-zero held), `python -m loop.promote stage --name <n> --by <who>` (the staging promote pen) — each ending in a clear `done | report | needs-you`; AND the §10 test passes in `tests/test_promote.py`: an accepted, locally-deployed snapshot promotes to staging and records exactly one `staging_promotion`; a non-accepted snapshot is refused with nothing recorded; an accepted-but-not-locally-deployed snapshot is refused as out-of-order with nothing recorded; `promotion_path` reports the ordered path and flags an out-of-order state (a production snapshot whose name never reached staging); the same snapshot is refused before its local deployment and promotes after (the gate is not vacuous); and a fabricated/constant gate fails. Read-only and propose-only but for the one `promote_staging` pen; acceptance and the production rung are read from the existing folds, never re-derived or a second authority (D-4, §10).
