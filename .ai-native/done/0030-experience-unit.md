# Done-line 0030 — the experience unit: an obligation, or it is weather

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `loop/experience.py` defines the typed experience — a source
> beat, a registered surface, a composed context, a granted expectation (a
> return shape with a non-empty terminal set), and a rung-checked backing — and
> its birth-gate `experience_refusal()` **refuses** an experience that
> obligates nothing (no terminal set), names an unregistered surface or an
> unknown beat, or carries a backing whose class holds no rung for the shape it
> must return. §10 proof: a well-formed experience over a registered surface
> with a granted rung is accepted; drop the terminal set, the surface, or the
> rung and it is refused at birth. It composes the surface registry, the trust
> ladder, and the shape→capability obligation. The term `experience` stays
> PROVISIONAL (owed to the glyph-knolling ritual); `loop/` stays pure.

## Out of scope, named (later increments)

- **Launching** experiences from live beats (the launcher, the next wave-3
  piece) and **authoring** the story one returns (wave 4) — this piece is the
  unit and its birth-gate, nothing more.
- **Composing the context deterministically** (D-9) is a field here; the
  launcher assembles the place. The backing names a class (rung-checked); the
  registered *mind* it resolves to is the launcher's to bind.
