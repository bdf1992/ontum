# Done-line 0144 — the gate panel: a share of runs judge with all models and compile

Written before code, per §9.4. When this line is met, stop. bdo's direction this session, the impact half of the gate economy (after done-line 0142 added random-model + cost): have a **percentage of gate runs fan out to ALL models at once, each in its own stream, and compile at the end — to generate per-snapshot model comparisons naturally.** Those comparisons ARE the impact data the cost-only fold deferred: when every model judges the same atom (snapshot), the room shows where the cheap model diverges from the expensive one. His chosen settings: **unanimous-or-escalate** (a unanimous panel auto-advances; ANY split escalates to bdo — the split is the signal, never papered into a verdict) at a **~25% panel rate**.

Builds on the random-model rail (stacked on PR #324). Composes, does not double-build (§10): it reuses `launch_claude` (per-model), the run ledger's `record`, and `model_economy`; the panel is a new launch MODE, not a second judge or a second truth. Scope guard: the compile never invents a verdict — a non-unanimous room escalates; a stream that fails leaves the room unconfirmable and also escalates.

> **Done when:** `.claude/skills/gate/gate.py` has a panel path selected with probability `ONTUM_GATE_PANEL_RATE` (default 0.25, env-configurable; 0 never panels, 1 always) per launch: when chosen, it runs **every model in the pool concurrently** (each its own `claude -p` stream, via a thread pool) judging the same atom, collecting each stream's (model, verdict, cost); AND it **compiles unanimous-or-escalate** — if every stream returned a verdict and all agree, that one verdict lands through `loop.node judge` (one receipt, D-4) and the trust-rail issue closes; if the verdicts disagree OR any stream failed (so unanimity cannot be confirmed), NO verdict is landed — it escalates to bdo (`needs-you`), leaving the issue OPEN with the per-model split; AND each stream is recorded on the run ledger with its model, cost, verdict and atom, so `loop.runs`' cost-by-model economy sees all of them; AND `loop/runs.py` grows a **snapshot-comparison** fold: per atom judged by ≥2 models, the per-model verdict+cost and whether the room agreed — the natural cost-vs-impact comparison — surfaced in render and `--json`; AND `tests/test_gate_panel.py` joins the suite with §10 teeth using an injected fake runner (no live spawn): a unanimous panel compiles to the shared verdict, a split panel escalates with NO verdict landed, a panel with a failed stream escalates (unconfirmable), the rate bound holds (0 never panels / 1 always), and the comparison fold groups by atom and flags agreement vs disagreement (a constant fold that cannot tell a split from a consensus fails). Read-only fold except the one `record` write and the one `judge` write; impact is read from real comparisons, never fabricated.
