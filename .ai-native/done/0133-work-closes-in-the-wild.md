# Done-line 0133 — Work closes in the wild — the merge-node's landing settles the atom

﻿Written before code, per ?9.4 ? recorded just after the build this once: the follow-through road was forged live in bdo's two-acceptances thread. The bar is honest and frozen here. When this line is met, stop.

**Goal:** bdo's direction (this thread): work that is built and merged must *close in the wild* before the session dies ? agents own their work through to landing, nothing hangs unfinished, and nothing hangs on bdo. The terminal-pull gateway (done-line 0123) found the clog: atoms reach `value_accepted`, their PR is landed by the independent merge-node, yet the pipeline atom is abandoned one step past the value-gate and counts as inflight forever ? so the inflight cap cools off new atom births. The cause is two terminals that never reconciled: the git-merge (work on main) and the pipeline terminal (`value_confirmed`). This is the road: the merge-node lands a PR it did NOT author, so its `landed` receipt IS the deciding acceptance for the piece (D-2's two acceptances, reframed this same thread) ? and with the per-atom join already on the receipt (`landed_atoms`, done-line 0124), an atom that reached main settles. **Arc:** `epic.landing-throughput-response`.

> **Done when:** (1) `orchestrate.next_action` settles an atom whose id is in the `atoms_on_main` join (`digest.atoms_on_main`) AND whose current bytes are the landed version (a SEED event on the record for THIS hash) ? it returns None, so `sense` counts it settled, not inflight, and the cap stops cooling new births on already-landed work. (2) The teeth (?10): an un-landed new version (same id in the join, but a fresh in-place edit with no seed for the new hash) is NOT settled ? it refuses to ride its sibling version's landing. (3) The join is reused, not re-derived (`digest.atoms_on_main`, the one definition ? no second truth); the settle is keyed to the independent lander, never the author (D-2 holds). (4) Full suite green; dogfooded as an atom on the log (atom + an independent value-gate receipt naming it, D-2); a PR.

> **Non-example:** auto-deriving the pipeline forward on a merge (the loop standing in for the value-confirm judge would self-sign ? the merge-node IS the independent acceptance, the loop only reads it); settling on the atom id alone without the seed guard (an un-landed edit would ride the landing ? mercury); back-filling the pre-D-13 merges that carry no `landed_atoms` (history is append-only; the road drains FORWARD, old lands stand as lossy history); masking a landed-but-refused divergence (the digest divergence fold reads receipts independently of next_action, so a refusal under a confirmed arc still surfaces).

> **Evidence expected:** the `orchestrate.next_action` settle-on-main diff + the `sense`/`schedule` threading; the ?10 test pair in `tests/test_orchestrate.py` (landed+seed settles; un-landed new version refuses); the full suite green; the atom + its independent value-gate receipt; a PR under `epic.landing-throughput-response`.
