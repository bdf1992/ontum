# Done-line 0151 — epic.diagram — the structural plane: regions as first-class declared structure

Written before code, per §9.4. When this line is met, stop. The second cell of epic.diagram, and the first built **structure-first** (bdo's redirect, 2026-06-20): the plane and its boundaries before the fixtures and features. Today a diagram's regions are decorative rectangles (`subgraphs`) and a node's belonging to one is *inferred from bounding-box geometry*, not declared. This cell makes the plane a **space of declared regions** — the thing snap and genres were trying to stand on. The plane's layer/depth enrichment, region *nesting*/composition, and the holonic attribute model are LATER cells, not this one (the bond: those are separable value; plane+boundaries are not).

> **Done when:** the renderer and schema treat REGIONS as first-class declared structure, not decorative rectangles — (1) a spec declares `regions` as objects each with an `id` and a `label`; (2) a node declares the region it belongs to (`region: <id>`), and belonging is **structural** (declared), never inferred from geometry; (3) `compose.py` renders regions as the structural containers deterministically (byte-identical SVG on every run, the `.ai-native` grain); (4) `qa.py` gains a structural tooth citing the canon (C4 containment / cognitive integration): a node declaring a region that does not exist → **deny**, and a node whose geometry falls outside its declared region → **deny**; and the §10 test in `tests/` passes — an honest regioned diagram (the pipeline's stages grouped into declared `ingress` / `body` regions) renders and PASSES the gate, while a locally-fine variant where one node claims a nonexistent region (or sits outside its declared one) is REFUSED with the cited principle. Backward-compatible: a spec with no `regions` renders and gates exactly as today.
