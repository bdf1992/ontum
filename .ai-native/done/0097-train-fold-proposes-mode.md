# Done-line 0097 — train fold: a session's would-deny readings become a proposed mode

> **Done when:** loop/train.py is a read-only fold that turns a training session's would-deny readings into a proposed stricter mode, and proposes ONLY what the evidence supports. Given a train_session id (an opening security_mode admission), it reads the sensor trace (tool-use.jsonl) for that session and emits the operation profile: counts by intent (the recorded tags.classify value) and tool surface, and the would-block set {fence-rule -> count} over status:would-deny records. From that it emits a proposal — a candidate stricter mode that would deny exactly the would-block set — framed as a proposal only bdo can admit (--by bdo, D-4); it writes nothing and ends report | needs-you. Section 10 proof with a negative control: a training session carrying would-deny {git-add xN} proposes guarding git-add; a training session with zero would-deny records proposes NOTHING (the fold refuses to invent a mode from no evidence) — two sessions, the fold distinguishes; if the empty session ever yields a proposal, the fold is fabricating and the bar is not met. Read-only: proposes, never installs. Builds on done-line 0096 (the sensor that produces the readings).
