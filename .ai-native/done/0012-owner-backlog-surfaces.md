# Done-line 0012 — the hook surfaces the owner's backlog

(Renumbered from 0011: two sessions allocated the same id within
eleven minutes — this file and `0011-pr-pen.md`. The commit messages
both say 0011; this rename resolves the file-level collision, and
done-line 0013 builds the pen that makes the next one impossible.)

Written before code, per §9.4. When this line is met, stop.

> **Done when:** hook mode (`loop.summon --hook`) adds one line when
> items await the admitted-real owner stamp — the count and the inbox
> command, never the briefings themselves (the inbox is the briefing
> surface, D-4; this is surfacing, §6) — stays silent when the stamp is
> mocked or the queue is empty, still writes nothing and still exits 0
> always; and tests pin the line's presence, its absence under a mocked
> stamp, and the no-writes property.

Scope note, honest: this is the dual-benefit zone report 0006 named —
an owner-surface change the owner didn't verbatim request tonight. It
ships as its own commit so the stamp can reject it alone; the flag is
in the PR description per ritual 0.2.0.
