# Done-line 0031 — overnight-loop skill

Written before code, per section 9.4. When this line is met, stop.

> **Done when:** A versioned `overnight-loop` skill exists with a deterministic `brief` command that refuses unsafe overnight starts (main branch, dirty tree unless explicitly allowed, and unknown arc) and emits a bounded run contract for a named arc; tests pin both refusal and valid brief behavior.
