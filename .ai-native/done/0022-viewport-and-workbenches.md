# Done-line 0022 — the viewport and the workbenches - the owner window stays on main

Written before code, per §9.4. When this line is met, stop.

> **Done when:** the viewport/workbench discipline is on the record —
> CONTRIBUTING names the rule, the branch-ritual carries it as a
> versioned step — so a future session knows the primary checkout is
> the owner's window on `main` and session work happens in a worktree.

Direction (bdo, 2026-06-10, in conversation: "I think that would be
ideal"): the primary checkout `c:\Users\bdf19\ontum` is bdo's reading
surface and stays parked on `main`; sessions take a git worktree per
branch. The incident behind it, same afternoon: the shared checkout
changed branches four times under the owner's explorer while the
language directory "disappeared," and local `main` proved 38 commits
behind — it had never taken the return leg of a merge.

Id note: minted as 0020 in parallel with three sibling sessions; renumbered 0022 at rebase. 0020 is now held twice on main (git-commit-pen, reflection-automates) and 0021 by the story gate (PR #18) — the collision class is the wave-1 placement-gate case in epic.experience-layer.