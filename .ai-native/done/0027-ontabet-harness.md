# Done-line 0027 — the ontabet harness — laws, schema, basin

Written before code, per §9.4. When this line is met, stop.

Context: the ontabet session came home (`docs/sources/files.zip` — the
foreign review of the creole-glyph-language envoy plus the recursive-
pilish basin, the S-frame placements, and the landing map). Its Wave 1
is the no-pin work: until the generator emits the incidence laws and
the registry has columns for placements and weights, there is nowhere
for measurement data to land — the harness gap blocks data collection.
Every law landed here is computationally checked, never quoted; every
placement stays PROPOSED/MODEL-GUESSED — no pin (PIN-1…7) is decided
by this work.

> **Done when:** the basin (669 surveyed words, 60 OPEN) lives at
> `language/basin.md` and `knoll.py` parses it live into the registry
> with a measured per-letter density column; the generator derives and
> emits each cell's closure (3^dim) and star (2^codim) fans with both
> laws verified over all 27 addresses (Σ = 125 both ways) and demotes
> falsifier-less MINTED occupants to OPEN; the S-frame placements load
> as PROPOSED records under the grown schema (placements, attestations,
> frame-local chips + escape, typed empties, frame `sc`) with a
> structural gate that refuses address collisions and census
> violations; and tests prove two locally-fine placements refuse to
> fit — viewer template port (BUILD-3) and all measurement runs
> (MEAS-1/2/3) explicitly out of scope, each its own done-line.
