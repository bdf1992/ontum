# Done-line 0090 — The merge digest stops crying wolf: a refusal a newer landed version cleared is settled history

Written before code, per §9.4. When this line is met, stop.

This serves [[bdo-surfaces-gesture-first-glance]] under confirmed `epic.owner-harness` — bdo's steering surfaces must never claim a gesture that isn't there. The merge digest's **divergence** banner ("⚠ these need you") fired on `atom.field-topology.v0`'s `missed` for days *after* its successor `atom.field-topology.v1` landed and `loop/field.py` shipped (done-line 0078) — a refusal the next version already cleared, surfaced every day as live owner-work. A read-only fold fix; it captures nothing new.

> **Done when:** `loop/digest.py`'s divergence fold (`_divergences`) treats a refusal as **settled history, not a live divergence, when a later version of the same atom has landed** — the doctrine's own rule that editing an atom file makes a new version which restarts the pipeline while old receipts stay valid history but no longer apply (identity is the content hash, `reconcile.py`). Version is parsed off the `.vN` suffix (no suffix = v0); an atom whose base has a *higher landed* version is dropped from the `refusal-under-confirmed-arc` divergence only. The teeth (§10) stay: the **current/highest** version's own refusal still fires (the fold must not go blind); the queue-over-cap divergence is untouched; and the per-arc parked/refused counts and the all-time refusal tally still carry the superseded version as honest history (this corrects the false *alarm*, never the record). Proven by a test where v0-refused + v1-landed are each locally fine yet refuse to fit one picture — v0's refusal is suppressed, while v0-refused + v1-also-refused (nothing landed) keeps the divergence live; the full suite green; a merge receipt.

> **Non-example:** suppressing a refusal on the highest/only version (that is the live contradiction the digest exists to surface); dropping the queue-over-cap divergence; erasing the superseded atom from the parked/refused counts or the refusal tally (history is never retro-invalidated — only the alarm is corrected); retiring or editing the `atom.field-topology.v0` record itself (that is bdo's, D-4 — "yours to amend or retire"); capturing anything new (this is a pure read-only fold); or a fold that hard-codes the field-topology ids instead of deriving supersession generically.

> **Arc:** `epic.owner-harness` (CONFIRMED, adm.728a87a9ca48) — "your stamp stays leverage ... the queue refuses to flood you." A false daily "these need you" is exactly the flood this arc refuses; the merge-node lands this piece under that confirmation.
