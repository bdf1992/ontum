# Done-line 0095 — The landing page — Causality's welcome mat folds the live felt-field

Written before code, per §9.4. When this line is met, stop.

This is the first step of bdo's sequence **welcome → canvas → tools → paint** (2026-06-16): the landing page. Its frame: agenda [[anima]]'s felt-field is *emergent* — the sauce is the process, named after first taste — so a session does not author it; it builds the surface the field emerges *through*. The welcome mat is the system's first impression, and to be *capable of carrying* the felt-field it must **fold** that field from the records the loop already keeps — never paint it. A hand-authored "feels alive" surface is mercury: it carries nothing. So the mat is a **projection** (Causality's one hard rule: never a second source of truth, no second write path), reading the felt-field sensors that already exist on main — it does not re-sense them.

> **Done when:** A pure read-only projection (`causality/welcome.py`, stdlib only, in the `loop/web.py` render grain — phone-readable, self-contained, **no second write path**) renders Causality's welcome mat by folding the **live felt-field** from records already on the log, never painted: **pulse** (the beat of recent acts — count and the most-recent acts, from `loop.energy`'s act fold), **rhythm/breath** (the hour's lean — heat/steady/cool — and register, from `loop.temporal`), and **energy/tempo** (total beat, tempo tok/s, and the **strain** — burned/fallback wasted latency — from `loop.energy`). Every felt figure on the page is the number its fold emits (the census/term-economy citation discipline). Headlessly provable: `python -m causality.welcome render` writes the page, and a §10 test asserts every felt-field figure on the rendered page equals its fold's value over a fixture log — a fabricated/constant value fails the equality — and that an empty log renders **"absence"** (no acts carry the field yet), not a fake zero. The teeth (§10): a fixture log carrying one clean act and one burned act — each locally valid — refuses to fit one "all is well" first impression; the page surfaces the strain rather than averaging it away, the same refusal `loop.energy` makes. The full suite green; a merge receipt.

> **Non-example:** hand-authored "feels alive" prose or a constant/decorative metric (mercury — a fold that does not read the records fails the equality test); any POST / write path / mutation of the log (Causality is a projection, never a second source of truth); re-deriving energy or heat/cool here instead of projecting `loop.energy` / `loop.temporal` (double-building the field's fold — Causality *reads* the sensors, it does not re-sense them); depending on `loop.impact` (not on main — it is open PR #149; this folds only committed bytes); treating an absent metric as zero (absence is information); or a crash / `needs-you` on an empty log (the projection reads what is there, honestly).

> **Arc:** serves agenda [[anima]] (priority one) by building the first surface the emergent felt-field is carried *through* — the welcome mat carries it, it does not author it. It is also the first frame of the confirmed `epic.causality-surface` (welcome → canvas → tools → paint). No arc is named under anima yet; bdo names the converging arc. Drawn to the agenda honestly rather than invented (absence is information).
