# Done-line 0119 — The ask surface is a projection of the session's gesture, made structural

**Goal:** bdo, 2026-06-18 — the AskUserQuestion surface is "poorly used when generic." A generic radio menu forced him to break out to *Other* to ask "what's recommended?" — the tell that the question withheld the one thing he wanted (my reasoned pick + the divergence seam to set beside his own). He chose: make "the ontum way to ask" **structural, not my willpower** — a skill, with a registered memory location, and a policy with references. The question becomes a faithful **projection of the session's gesture**: each axis a real degree of freedom I am holding open; recommendation-first; routes narrated (the `preview` field), not labelled; configuration and choice in one panel (the tool's 1–4 questions). **Arc:** serves `epic.owner-harness` — the same inference-construct shape as the [[bdo-brief node]], applied to the *synchronous* owner-decision surface. It does not double-build the brief fold; it shares the shape (§10).

Written before code, per §9.4. When this line is met, stop.

> **Done when:** Three coherent homes exist and cross-reference each other. (1) A **skill** `.claude/skills/ask/SKILL.md` (prompt-as-code, the ritual) names the ontum-ask shape: lead with a Recommended option carrying its reasoning; narrate genuine routes in `preview`; when there are dials to set, raise a multi-question **config panel**, not a single menu; ask only at a genuine fork only bdo can settle, never to offload a call a session could make itself. (2) A **registered memory location** — a memory topic file indexed in `MEMORY.md` — is the durable home where the discipline and its accumulated learnings persist across mortal sessions; the skill names it as its memory. (3) A **policy with references** `.claude/skills/ask/policy.md` states the governed rules as cited records — each rule referencing the doctrine clause or bdo-frame it enforces (the offloading hard rule; inference-construct-not-menu; D-4) — and naming the **refusal**: what a generic menu looks like and why it is refused (the §10 teeth — the policy can say *no* to an ask, not only describe good ones). The discipline is adopted **this session**, demonstrated by the asks I raise carrying recommendation + narrated routes. The honest limit is named: the modal *chrome* is harness-owned and not ours to reskin — the lever is content, affordances, memory, and audit.

> **Non-example:** trying to reskin the AskUserQuestion modal chrome (harness-owned); a policy that only describes good asks and can refuse none (no teeth); a memory location not indexed in `MEMORY.md` (unregistered); a skill that re-derives the bdo-brief inference-construct instead of citing it (a second source of truth).

> **Out of scope — named (next increment, its own done-line):** the **audit fold** — a `PreToolUse` hook logging every AskUserQuestion call and its answer to the watcher trace, plus a read-only `loop/`-grain fold that reads the pattern: when the surface is reached for well (a genuine fork) vs poorly (offloading / generic menu), with *Other*-breakouts as the highest-signal failures. That sensor makes "am I asking well" a reading, not a vibe, and carries the route's real §10 code-test (a generic-menu ask gets flagged).

> **Evidence expected:** the three files above + the `MEMORY.md` index line; the three cross-referencing; this session's asks demonstrably recommendation-first and narrated; a numbered session report; a merge receipt.
