# Done-line 0029 — the viewer descends — frames navigable, operations on every level

Written before code, per §9.4. When this line is met, stop.

Context: BUILD-3 of epic.ontabet-speaks (the zip's hand-edited
`viewer.html` is the spec, never the landing), extended by bdo's ask:
surface the wave-1 harness data *and mutate it through operations* —
Rubik's turns, and descent/ascent between the global frame and the
local ones. The architectural move: descent re-labels the one cube
instead of opening a side widget, so every existing operation (drag
turns, scramble, unwind, explode, x-ray, sound) works at every level
for free. Words ride pieces, addresses stay cells: turning a descended
frame visibly displaces words from their PROPOSED addresses —
the pairing-as-state reading as experience, clearly captioned as
illustration; PIN-1 stays undecided, and nothing the viewer does
writes a record.

> **Done when:** the main cube can descend into a frame and ascend
> back by breadcrumb — S renders its 27 placed words [PROPOSED] from
> `REGISTRY.placements` (never hardcoded), letters render their
> term-frame slots with the basin's surveyed lexicon, pivot candidate,
> and measured density in the panel, ⊘ descends to the interstitial
> frame (kind pools in the panel; cell assignment honestly OPEN) — with
> all existing operations live on the descended frame and a displaced-
> from-home count visible after turns; the spec's ontabet index lands
> as one panel (ontabet table with bit codes · chips · a donors column
> fed by the measured density · sc rendered OPEN · the 48-spelling
> syllabary · terms · findings tabs), the cell inspector gains the
> composition section reading closure/star from the registry, the
> conversation-invalidated trio-spotlight/cascade/trio-panel are
> removed, the mobile bar fix lands; the CUBE_MATH block and its node
> tests stay green and the new frame-vocabulary logic is itself a pure
> tested block — deeper descent (children of letter frames) and any
> MEAS run stay out of scope, each its own done-line.
