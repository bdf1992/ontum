# Done-line 0139 — epic.diagram wave 1 — the floor and the refusing gate

Written before code, per §9.4. When this line is met, stop. The first cake of epic.diagram — the smallest honest slice the repo's three reviewers agreed on: a deterministic floor and a refusing gate whose teeth cite a named canon. Auto-layout, generative authoring, projection-from-truth, and the rest of the genres are later lines, not this one (epic.diagram pieces wave 2+).

> **Done when:** a new `diagrams/` module exists in ontum (sibling to `causality/`, never an extension of `canvas.js`) carrying: (1) a ported, stdlib-only, explicit-position `compose.py` that renders a committed diagram spec to byte-identical SVG on every run; (2) one canon reference file naming the architecture-diagramming SME — Moody's *The Physics of Notations* + graph-drawing aesthetics (Purchase) + C4 — that each gate rule cites by principle; (3) `qa.py` promoted to a refusing gate (nonzero exit + reason on stderr, fail-open on its own error, in the off-log-gate/mock-shame grain); and the §10 test passing in `tests/`: the honest pipeline state-machine diagram (value-gate → owner-stamp → placement → handoff → confirm) renders and PASSES the gate, while a locally-fine but dishonest variant of it (an orphan node, an edge through an unrelated node, or an invented shape) is REFUSED with the cited canon principle. The data-visualizer advisor is NOT wired in as governor here (it is the charts canon, the wrong SME).
