# Done-line 0193 — bench rescue verb

Written before code, per §9.4. Iteration 1 of the forest-never-strands experiment. Variable changed: the bench-rescue verb.

> **Done when:** `git.py` carries a `rescue <worktree>` verb that, for ANY worktree (not only the viewport), commits its uncommitted pile onto a dated rescue branch and pushes it — proof-carrying, the whiteout shape — guarded by a §10 test that catches a no-op/constant rescuer; the full suite is green; and the 20 forest `preserve-first` benches move from rescuable-by-no-envelope to rescuable (M1: 0 to 20). When true, stop.
