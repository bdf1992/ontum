# Done-line 0069 — Outcome-pressure fold v0 — the probe-set + loop/pressure.py over it (one increment toward the maximal Causality outcome)

Written before code, per §9.4. **This is one increment toward the maximal
outcome** in `outcomes/causality-outcome-pressure.md` — *not a replacement for
it.* When this line is met, stop *this increment*; the outcome continues, and
its unresolved probes remain live pressure.

> **Done when:** the maximal Causality / outcome-pressure outcome is
> represented as an evidence-bearing probe set, and `loop/pressure.py` can
> compute current state, phase, top leverage, and next safe move over that
> probe set — with tests proving uncheckable probes are refused, discovery /
> build / realize phases are distinguished, dependencies control nagging
> (outcome probes stay dormant until their capability preconditions are met),
> and the unresolved probes remain visible as continuing outcome-pressure
> rather than out-of-scope leftovers.

> **Non-example:** a flat fold that ranks one probe list and picks the top
> unmet probe (no phase; discovery reads as stalling; can't tell *possible*
> from *real*); or a `loop/pressure.py` that, run after a partial session,
> reports "done" or drops the unresolved probes instead of carrying them; or a
> probe-set authored as aspirational prose whose probes carry no check.

> **Pressure reduced:** for the first time the outcome-pressure gap is
> *folded* rather than held in bdo's head — current reality versus the full
> desired probe-set becomes a computed, un-fakeable measure that a later
> session re-derives. (It does not yet ride summon — see *Does not complete*.)

> **Does not complete** (continuing outcome-pressure — **NOT** out of scope for
> the goal): OP2 (summon delivers the pressure at wake), OP3 (owner / work /
> outcome pressure compose into one ranked field), CZ1–CZ4 (Causality persists
> / configures / ingests real evidence / projects the typed graph), OUT1–OUT2
> (sessions inherit-and-reduce across wakes; the outcome survives session-end).
> These remain the outcome; this done-line simply does not build them — and
> after it, the fold itself keeps them visible at every wake.

> **Evidence expected:** `loop/pressure.py` + a committed probe-set it reads +
> `tests/test_pressure.py` green (uncheckable-probe refusal; discover / build /
> realize distinction; dependency-dormancy of outcome probes; unresolved probes
> stay visible), and a merge receipt. Run over the Causality probe-set it
> reports build-phase with persistence (CZ1) as top leverage and every
> unresolved probe still listed.
