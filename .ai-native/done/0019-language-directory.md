# Done-line 0019 — the language directory - the creole gets a home

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `language/` exists under its own governing `CLAUDE.md`,
> with a strata map and one file per stratum — syntax, semantics,
> semiotics, pragmatics — each pinning what the stratum is, what is real
> on disk today, and what is open; pragmatics holds bdo's question, not
> an invented answer.

Direction (bdo, 2026-06-10): set up the ontum language directory with
the element of a rosetta-creole-type glyph system organizing syntax,
semantics, semiotics, all leading to "what is our Pragmatics."
Provenance context: `docs/sources/rosetta-creole.md` (inspiration, not
claim — the strata derive from the phase-2 vault and the repo's real
holdings, and only *cite* the source).

Out of scope, named: minting into `glyphs/registry.json` (generated,
vault-derived — proposed stratum marks stay in `language/` until
knolled and admitted); any compiler/emitter build; touching
`docs/phase-2/` or `docs/sources/`.
