# Done-line 0032 — the merge digest — the owner's data-rich surface

Written before code, per §9.4. When this line is met, stop.

> **Done when:** `python -m loop.digest` renders a read-only, per-arc/day
> fold of the log into the surface bdo watches *instead of operating the
> merge* — what landed, what refused, what awaits, the dials in play, and
> the field's heat/cool behaviour — grouped arc-first (done-line 0006).
> Each verdict is read generically from the log (a receipt with no
> `next_suggested_event` is a refusal), so the digest already speaks the
> merge-node's `{land, refuse, send_back}` the day those receipts appear.
> It writes nothing — a read surface like summon/census; verdicts still
> land only through the one pen (D-4). §10 proof: a *confirmed* arc that
> harbours a *refused* piece is a contradiction the digest must NOT smooth
> over — two locally-fine records (bdo's standing stamp; a gate's honest
> no) that refuse to fit, surfaced as a named divergence, not averaged
> away; a clean span shows none. `--since/--until` bound the span and the
> fold is pure (no clock inside it). Recorded in loop/CLAUDE.md as the read
> surface it is.

## Why (bdo, 2026-06-10)

"I no longer wish to be in charge of merging ... instead it should be
getting surfaced much more data-rich information ... based on our
setpoints and skills." This is the move done-line 0028 named and
deferred — draining the *PR* queue, not just the atom queue. Eyes before
hands-off: the digest is the surface bdo watches from, built before the
merge-node takes his hand off the wheel.

## Out of scope, named (the arc's other pieces)

- **The merge-node itself.** The arc-scale gate that judges "land on
  main?" and, once bdo admits it real (`--by bdo`) and the `bdo merges`
  hard rule is amended, actually lands the finished epic. Built next; both
  triggers are his. Until then bdo still merges — the digest only watches.
- **A served/pushed digest.** This renders to stdout (read-only). Wiring
  it as a reflection surface kind the Stop-beat pushes (the horizon's
  "page you open on your phone") is a later piece, on the reflect rails
  (done-lines 0018/0020), never a second write path.
- **Merge-specific landing semantics.** "Landed" today means the mock
  pipeline's `value_confirmed`; it becomes "merged to main" for free when
  the merge-node's receipts land, because the digest reads verdicts
  generically.
