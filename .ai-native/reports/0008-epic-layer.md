# Session report 0008 — the epic layer, and two riders on the record

- **Date:** 2026-06-10
- **Session:** v-next build from bdo's two riders (chat): epic-scale
  stories; larger swaths of value when local pieces glue in.
- **Branch:** `claude/great-faraday-djmq2x`
- **End-state:** `done` on done-line 0006; one item at bdo's stamp.

## 1. The riders, captured

1. **"It's not quality work, but yes on both."** Both accepts routed with
   the reservation verbatim on the receipts. Named plainly: the epic
   layer does *not* answer this — quality pressure belongs to the gates
   that are still mocks (L1 placement, L2 hand-off, eval, the L0
   `confirmed | missed` second check). The fourth real L0 summons flagged
   the same gap unprompted in its accept. **Making a quality gate real is
   the highest-signal next stamp.**
2. **"Epic level ideas and context, not individual tasks or tickets…
   larger swaths of value at once as long as they have local pieces that
   glue into value."** Now done-line 0006's sizing rule: a version may
   carry an epic-sized swath when each local piece lands its own receipt.
   §9.3's *node* rule is untouched.

## 2. What landed

- **Epics as first-class records** (`.ai-native/epics/`): arc told fully,
  context, owner-terms value, horizon, pieces with their glue.
- **Filing without re-judging:** atoms file via `incidence.serves` or the
  epic's pieces list — `epic.substrate` and `epic.owner-harness`
  retro-cover everything built so far; no settled hash moved (D-5).
- **Epic-first briefing** on both surfaces: the arc above the items, the
  glue on each card; unfiled atoms still render.
- The directive shipped as its own exemplar and is at bdo's stamp under
  its epic. 24/24 tests green.

## 3. Phase-2 echo (noted, not coded)

Many atoms settling into one epic is the vault's scale-recursion
(onta → ontum, `ontum-evolution.md` §4) arriving at the work layer: the
epic is the frame its pieces settle into; the glue notes are the
neighbor-relations. When epics one day carry epic-level verdicts, that
will be the keystone question: where is this arc's center, and what
moves it.

## 4. Open for bdo

- `atom.epic-layer.v0` at the stamp (briefed under `epic.owner-harness`).
- Next stamps on offer, in recommended order: **one real quality gate**
  (answers the standing reservation — L1 placement is the doctrine's
  seam-tester, §10), the slow loop, auth + serving the inbox beyond
  localhost, corpus-to-system ingestion.
