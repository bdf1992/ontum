# Report 0021 — Codex admitted as a guest engineering family

## What landed

By done-line [0024](../done/0024-codex-guest-engineer.md) — Codex admitted
as a *guest engineering family*, the loop's first third party:

- **[AGENTS.md](../../AGENTS.md)** at the repo root: Codex's composition
  surface, parallel to `CLAUDE.md`. A thin pointer that *defers to*
  CONTRIBUTING, the doctrine, and the per-directory `CLAUDE.md`
  environments — it does not fork their rules. Its one load-bearing
  section, "The fence is yours to hold," re-states **as self-enforced
  discipline** the guardrails the Claude-Code hooks enforce for a Claude
  session but **cannot enforce for Codex** — no raw git mutation to
  `main`, the log stays append-only, records minted through `loop.pen`,
  generated outputs never hand-edited.
- The **`codex/*` branch namespace + worktree workspace** convention, so
  Codex never clobbers bdo's primary checkout and the branch name records
  which family did the work.
- The **"two-party loop" line amended** in [CLAUDE.md](../../CLAUDE.md)
  and [CONTRIBUTING.md](../../CONTRIBUTING.md): owner-and-engineering, the
  engineering party variable (Claude or Codex), bdo unchanged as owner and
  last stop (D-4).

Scope chosen with bdo: **guest engineer**, not the (richer, OPEN)
envoy-reception *reviewing-mind* role; recorded as **done-line + report**,
not silently amended. The work was first authored mid-session on a branch
that pre-dated 24 commits of `main`; this PR is the clean re-landing off
current `main`, renumbered (done-line 0021→0024, report 0019→0021) so it
no longer collides with what merged in between.

## needs-you

- **The stamp.** This PR is the Codex admission, branched off current
  `main`, for your stamp — no self-merge (D-4).
- **Codex's first lane is yours to point.** The guest-engineer rail is
  built; the higher-value **cross-family review** role (the
  envoy-reception arc) stays OPEN. Whether Codex's first task is a build
  or a review is your call.

## End-state

`report` — Codex admitted as a guest engineering family: `AGENTS.md` and
the `codex/*` worktree convention authored on a clean branch off current
`main`, the two-party line amended, recorded by done-line 0024 and this
report. At the stamp.
