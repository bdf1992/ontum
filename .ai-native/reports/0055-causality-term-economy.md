# Report 0055 — Causality's term-economy witness — done-line 0060 landed as PR #114

## What landed

bdo handed a fully-shaped `/goal`: make Causality a living, evidence-backed
witness over ontum's own terminology, on a dedicated branch until validated.
This session built it whole and opened it as **PR #114** (the unit the
merge-node lands), against **frozen done-line 0060**.

The increment, all five deliverables landing together as bdo asked:

- **`causality/term_economy.py`** — a pure read-only fold (loop/gaps.py /
  census.py grain: stdlib, no network, no writes but the explicit projection
  regeneration). It resolves a declared seed's citations against committed
  repo bytes by stable substring and classifies each term into the economy
  taxonomy (minted-runtime/doctrine/workflow, projected, proposed, poetic,
  overloaded, orphaned, ghost). The repo is truth; the seed is a claim; a term
  is never minted by appearing on the surface.
- **The committed example** — `causality/examples/ontum-terms.{seed,projection}.json`
  projects the five canonical terms. `atom`, `receipt`, `node` →
  `minted-runtime` (doctrine + code + log all resolve). `seam` and `arc` →
  `overloaded`, on REAL evidence already on the record: `seam` is both the
  loop event-surface and the phase-2 geometric Site-primitive; `arc` is both
  the enforced `arc_confirmed` admission and the prose `arc` field with no
  enforcement path. The audit's teeth bit real vocabulary, not a synthetic.
- **`tests/test_term_economy.py`** — 11 tests pinning the §10 refusals: no
  evidence ⇒ never minted; a citation to nothing ⇒ ghost; two incompatible
  senses ⇒ overloaded; the projection reproduces byte-for-byte; and a
  fabricated constant classifier is shown to fail all three. Full suite 612
  green.
- **The audits** — `causality/audit/reality-audit.md` (Causality-as-presentation
  vs. -as-living-surface, citing the sealed envoy seams) and
  `term-fold-audit.md` (the method: deterministic surfaces vs. claimed minted).
- **The contracts** — `causality/contracts/projection-api.md` (eight families:
  TermNode, EvidenceEdge, SiteNode, ArtifactNode, FoldObservation,
  ProjectionView, GapFinding, TermMintingState) with JSON Schemas for the four
  the tool emits.
- **The skill** — `.claude/skills/causality-data-integrator/SKILL.md`: proposes
  seed entries from repo evidence, refuses to mint without it, never promotes
  (D-4).

The module is governed by its own `causality/CLAUDE.md`, composed into the
root surface (`@causality/CLAUDE.md`), and its projection bytes pinned in
`.gitattributes`. `epic.causality-surface` is already arc-confirmed by bdo
(adm.a675cb9d36fb), so #114 is merge-node-eligible once green — landing is the
merge-node's, never this author's.

Built in a clean worktree off origin/main; bdo's viewport was never touched.

## needs-you

Two items, surfaced as a cold read — neither is a chore for bdo, both are
genuinely his to see:

1. **Validation call on PR #114.** The terminology-economy lens is a new
   *specialization* of the already-confirmed causality-surface arc (its
   witness register). Because the arc is confirmed, the merge-node will land
   #114 once green — no extra gesture needed. If you want to eyeball this
   specific reframing before it folds to main, say so and it holds; otherwise
   it rides the standing confirmation. (Not re-introducing per-piece stamping
   you retired — just naming that this is a notable angle on the arc.)
2. **A prior session's stranded work in your viewport (not this arc's, not
   mine to land).** The primary worktree holds uncommitted inference-plane
   files — report `0052-goal-local-inference-plane`, done-lines `0061`/`0062`,
   a modified `admissions.jsonl` — never committed to any branch. The
   owner-ask-shame hook has been screaming two parked owner-asks from that
   0052 report across this session. Because those reports are uncommitted, the
   branch-local mint fold cannot see them and even minted this report at a
   colliding id first (the git pen's fleet-wide guard caught it — the
   done-line 0023 mechanism working); the report is now `0055`, clear of the
   fleet. That session's work needs landing or clearing; I did not touch it —
   committing another session's report or guessing its intent would be worse
   than surfacing it.

## End-state

`report` — done-line 0060 met and landed as PR #114 (merge-node-eligible under
the confirmed causality-surface arc); the term-economy fold classifies five
real terms with byte-reproducible evidence, catching `seam` and `arc` as
genuine overloads; 612 tests green; one validation call and one stranded-work
flag wait above, both cold-read, neither a chore.
