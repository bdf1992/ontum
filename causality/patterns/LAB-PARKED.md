# PARKED — the theme/variation lab (Causality's interaction model)

*bdo, 2026-06-16: the feedback should live in the surface, not in chat —
a live interactable array of themes + variations you steer by USE, where
the system suggests alterations. Decided: **finish Theme A first, then
decide** on this; **prototype in foundry, home to causality/ after.**
Parked, fully defined, so it survives session-death.*

## Why it's not portfolio polish — it IS Causality
The lab and Causality's surface are the same machinery (display-system
C17, self-host: Causality rendering Causality, here varying its own skin):

| The lab needs | The Causality primitive it prototypes |
| --- | --- |
| themes + variation axes (type, space, depth, colour) | a **typed** surface, multiple navigable axes (**4D**) |
| a variation can contain variations | **recursive embedding space** |
| the system proposes the next alteration | **AI routing** (describe-to-canvas / request-node) |
| select → commit a chosen state | **provisioning** (pencil→ink through a pen) |
| your clicks / dwell / accept are the signal | the **use-trace** (outcome probes — feedback-with-use) |

## The bounded first cut (when un-parked)
- a live **theme switcher** — Theme A (light) + 1-2 alternates, instant.
- **3-4 variation dials** — type scale · spacing rhythm · depth · colour-temp — each re-composes the whole page live as you drag (the strata/anima/force facets made manipulable).
- one **AI "suggest an alteration"** — describe or click; it proposes a variation (Interface-as-AI on the surface's own style).
- **feedback-with-use** — selections / accept-reject captured and deposited; your use is the signal, not a chat sentence.

## Trigger to un-park
bdo reacts to Theme A → says go. Then build the bounded first cut in the
foundry; home the proven machinery into `causality/` as the real CZ
surface (the deliberate later port, not a re-build).
