# DONE-LINE — v1 Pattern Commons Portfolio

*bdo's scope, 2026-06-16: flagship + catalog (options 1+2); effort = one
real browser-verified surface this session. Frozen bar — when met, stop.*

## The bar — one portfolio-grade page, both halves

**1. CATALOG — each card is a PLAYABLE micro-portfolio (bdo, 2026-06-16).**
Not static description: every pattern card hosts a small LIVE interactive
demo *of that pattern* — its own tiny portfolio piece — with
name · intent · verdict · axes · kin as the frame around the demo.
*Honest session scope (53 playable ≠ one session):* build the
**playable-card framework** + the **hero cards fully playable** (the
three new ai-native families' spine patterns: provenance-trace,
pencil-vs-ink, ghost-vs-record, schema-gate-before-draw, parked-pulse).
Remaining cards render as **playable-ready slots marked "demo pending"** —
honest coverage, never faked completeness. Full 53-playable is the
multi-session fill that follows.

**2. FLAGSHIP (live, interactive, portfolio-grade).** ONE working
register-grammar surface on a real graph — the thesis made visible:
- **commitment on the line channel** — solid = ink (record), dashed =
  pencil (request/simulation), broken = ghost.
- **strata on colour, anima on size/motion** — the triad, no collision.
- **click any node/edge → trace its route home** (provenance).
- editorial craft applied — the arrival, type leads, glass HUD.

## The teeth (§10 — the real test)
A cold viewer can tell **fact from proposal from ghost at a glance, with
no legend.** If they can't, the register grammar isn't readable yet and
the surface failed — regardless of how it looks.

## Verified
Browser-verified via the experience skill's element-level accept/reject
loop (a real browser), never asserted.

## Bounded
One session. Built in the foundry (the experience skill's ground, no
ontum governance friction). **Homing into `causality/demos.html` as the
governed product surface is the explicit follow-on** — after bdo blesses
this, and that step is where it advances CZ formally. Not this session.
