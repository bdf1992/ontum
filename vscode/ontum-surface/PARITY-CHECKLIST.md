# Parity checklist — the gradeable bar (Phase 1)

**The marker.** Phase 1 is *parity-green* when every row below is `green`. This
file is the bar the overnight loop builds toward and the bar self-grade +
`/code-review ultra` grade against. It is filled by the bridge spike
(`atom.branded-surface-bridge-spike.v0`): each row gets a **source** —
`inherit` (the SDK/CLI gives it for free), `build` (we render it), or `have`
(already exists in the repo) — and a **status** — `todo` / `wip` / `green`,
green only with **evidence** (a file:line, a passing test, or an observed
behaviour). A constant "all green" with no evidence is a failed grade (§10).

> **GATE:** every row's source is unknown until the spike answers it. You cannot
> mark a row `green` over a bridge the spike has not proven. If the spike finds
> the bridge infeasible (SDK *and* CLI), the loop STOPS and lands this checklist
> with the finding — it does not fake green.

| # | Capability (what normal Claude Code does) | Source | Status | Evidence |
|---|-------------------------------------------|--------|--------|----------|
| 1 | Open a branded ontum window (webview), standalone of the official panel | build | green | `extension.js` (`openSurface`/`activate`) + `shell.js` (`renderShell`); `node vscode/ontum-surface/test/open-window.test.js` → 7 checks, exit 0 (registers `ontum.surface.open`, opens one webview, branded `data-surface="ontum"` + `data-standalone="true"`, no `claude-code`/anthropic dependency, nonce-gated CSP). Proves the open-path + markup; pixel render needs a host. |
| 2 | List local sessions from the transcript store; select one | build | green | `sessions.js` (`listSessions`/`summarize`/`parseTranscript`/`storeDirFor`) reads `~/.claude/projects/<encoded-cwd>/*.jsonl`, derives id/title/messageCount/branch newest-first, torn-tail tolerant; `shell.js` `renderSessionList` paints selectable `data-session-id` buttons; `extension.js` reads the store on open + records the `ontum:select-session` round-trip. Evidence: `node vscode/ontum-surface/test/sessions.test.js` → 8 checks, exit 0 (fake store incl. a torn line); read-only smoke on the live store returned 3 real sessions with titles ("Warmup"×2, "(no prompt yet)"). |
| 3 | Read + render a transcript (user / assistant / tool-use / tool-result) | build | green | `transcript.js` (`readTranscript`/`foldTranscript`/`entriesFromMessage`/`toolResultText`/`fileForSession`) folds a session's `.jsonl` into ordered entries of kind `user-text`/`assistant-text`/`assistant-thinking`/`tool-use`/`tool-result` (thinking via the `thinking` field, tool_result via a user turn), dropping non-turn/empty blocks, torn-tail tolerant; `shell.js` `renderTranscript` paints each kind as an escaped, labelled, `data-kind`-tagged block (error results flagged `data-error`); `extension.js` `renderPanel`/`readSelectedTranscript` re-render the panel with the selected session's transcript. Evidence: `node vscode/ontum-surface/test/transcript.test.js` → 9 checks, exit 0 (fake store with all four kinds + an empty block + a torn line; asserts fold order, payloads, escaping, the empty-state note, and the select→re-render round-trip); read-only smoke on the live store folded a real session to 3 entries (`user-text, assistant-thinking, assistant-text`, title "Warmup"). |
| 4 | Live-tail the active session as it appends | build | green | `livetail.js` (`tailTranscript`/`activeSession`) tails ONE session by **byte offset**, folding only lines completed since the held offset and advancing past complete lines only — so a torn/partial final line is held and re-read, never skipped; a truncated/rotated file (offset past EOF) flags `reset`. `shell.js` factors `renderTranscriptRow`/`renderTranscriptRows` (bare blocks, no list wrapper) and adds an `ontum:append-entries` webview handler that `insertAdjacentHTML`s the new blocks onto the live list (creating it from the empty-state, keeping `data-count`, marking `data-live`, autoscrolling). `extension.js` (`pumpTail`/`startTail`/`stopTail`) anchors the tail at end on select (no history replay), `fs.watchFile`s the file, and posts only-new entries. Evidence: `node vscode/ontum-surface/test/livetail.test.js` → 12 checks, exit 0 (seed fold + offset to EOF; idempotent re-read; append tails only the new entry; a torn partial line yields nothing + holds offset, then folds in once completed; offset-past-EOF resets; activeSession picks newest by mtime; bare-row render + escaping; append-handler present; and the open→select→append round-trip posts ONLY the new block, not earlier turns). Rows 1–3 tests still 7+8+9/exit 0 (no regression). Read-only smoke on the live store: tailed a real "Warmup" session — from 0 → 3 entries to offset 5765, from mid → only the tail half (1 entry), from EOF → 0 (idempotent). |
| 5 | Drive a new turn through the engine (send a prompt, get a reply) | spike→inherit | green | `engine.js` drives ONE turn over the inherited CLI stream-json channel proven in SPIKE-FINDINGS.md: `encodeUserMessage` emits the newline-terminated `{type:'user',message:{role,content:[{type:'text',text}]}}` stdin envelope; `engineArgs` opens `--print --input-format stream-json --output-format stream-json --include-partial-messages --verbose --permission-mode default` (+ `--resume/--fork-session/--model` pass-through) — flags **verified live** on this machine (`claude --help` advertises `--input-format`/`--output-format stream-json`, `--include-partial-messages`, `--permission-mode`, `-r/--resume`, `--fork-session`); `foldReply` folds the output events into one reply (session id + `tools` from `system/init`; assistant entries via the SAME `transcript.foldTranscript`; final text from the `result` event or an honest assistant-text fallback; cost/usage/duration/num_turns + error state from `result`; a missing result → `no-result`+error, never faked); `driveTurn(spawn injectable)` writes the prompt to stdin and resolves `foldReply(events)`, buffering NDJSON across torn stdout chunks. Wired to the surface: `shell.js` composer (textarea+Send, Enter-to-send) posts `ontum:send-prompt`; `extension.sendPrompt` drives the turn and posts `ontum:turn-reply` (rendered blocks + cost/subtype) which the composer splices + status-lines. Evidence: `node vscode/ontum-surface/test/engine.test.js` → 16 checks, exit 0 (encode shape; args incl. both stream-json + default mode + resume/fork/model; foldReply over captured real-shaped events incl. fallback/no-result/error; driveTurn round-trip via a **fake** engine — stdin got the prompt, reply folded, chunk-split reassembled, spawn-error rejects; extension `sendPrompt` + the `ontum:send-prompt` handler drive a turn with an injected fake engine and post the reply; composer markup). Rows 1–4 tests still 7+8+9+12/exit 0 (no regression). Honest scope: the round-trip is proven against a fake engine (no billed model call); a real billed turn is left to a human at the surface, and incremental partial streaming is row 6. |
| 6 | Stream the assistant turn (text + thinking) as it arrives | spike→inherit | green | `engine.js` interprets the `--include-partial-messages` `stream_event` partials proven in SPIKE-FINDINGS.md: `partialDelta(event)` folds ONE event into ONE tiny render instruction (`{phase:'start'\|'delta'\|'stop', index, kind, text}`), tolerant of both the wrapped (`{type:'stream_event', event:{…}}`) and bare (`{type:'content_block_*'}`) forms and deriving `kind` (`assistant-text`/`assistant-thinking` — the SAME kinds `transcript.foldTranscript` emits) from the delta itself (text_delta/thinking_delta) so a delta renders even if its block-start was torn (`tool_use` block-starts were row 7's territory, since landed — they now stream too). `assembleStream(events)` folds the partial stream into ordered live blocks, and the test proves that preview equals `foldReply`'s authoritative final entries (kinds + text) — the live preview and the fold never disagree (one source of truth). Wired to the surface: `extension.postTurnDelta` posts an `ontum:turn-delta` per partial via `driveTurn`'s `onEvent` hook; `shell.js` paints a per-index `data-streaming="true"` block (text appended via `textContent`, so the preview is no less escaped than the fold) with a live caret, and the terminal `ontum:turn-reply` drops the preview before splicing the folded reply. Evidence: `node vscode/ontum-surface/test/stream.test.js` → 15 checks, exit 0 (partialDelta over thinking/text start·delta·stop + bare form + a tool_use start now streams its name (row 7) + non-partials→null; assembleStream order + preview==fold; driveTurn fires onEvent for every event across torn stdout chunks; extension wiring posts one `ontum:turn-delta` per partial **in order, all before** the terminal `ontum:turn-reply`, deltas reconstruct the thinking + text, the reply still carries the folded reply, postTurnDelta posts nothing for a non-partial; shell markup carries the turn-delta handler + the preview-clear reconcile). Rows 1–5 tests still 7+8+9+12+16/exit 0 (no regression). Honest scope: the partial-stream plumbing + host message protocol are proven host-free against the captured partial shapes the spike's flags produce; live `stream_event` partials only exist in a real billed run (the on-disk store keeps final messages, not partials), so pixel-level incremental DOM mutation needs a webview host and a real streaming turn is left to a human at the surface. |
| 7 | Render tool calls and their results | build | green | A tool-calling turn renders the call, its input, and its result. `engine.foldReply` folds an `assistant` `tool_use` + the following `user` `tool_result` (+ the assistant's continuation) into ordered entries `[tool-use, tool-result, assistant-text]` via the SAME `transcript.foldTranscript` the on-disk reader uses (one source of truth: tool name + input on the call, `tool_use_id`/`is_error`/text on the result). `shell.renderTranscriptRow` paints a `data-kind="tool-use"` block (name + its JSON input in a `<pre>`) and a `data-kind="tool-result"` block (its text in a `<pre>`, error results flagged `data-error="true"`), all HTML-escaped. For the LIVE preview (row 7's "calling tool X…"): `engine.partialDelta` now streams a `tool_use` `content_block_start` → `{phase:'start',index,kind:'tool-use',name}` and its `input_json_delta` fragments → `{phase:'delta',index,kind:'tool-use',text:partial_json}`; `assembleStream` reassembles them into one named block whose accumulated JSON **parses to the same input** the final fold yields; `extension.postTurnDelta` forwards each as an `ontum:turn-delta`, and `shell.js`'s delta handler labels the per-index streaming block `tool ▸ <name>` (`data-tool-name`). Evidence: `node vscode/ontum-surface/test/tools.test.js` → 15 checks, exit 0 (foldReply order + name/input/result/is_error; renderTranscriptRow tool-use + tool-result + the error flag + input HTML-escaping; partialDelta start-with-name + input_json_delta fragments + stop + non-partials→null; assembleStream named-block + streamed-input parses to folded-input; driveTurn fires onEvent for the tool partials across torn chunks + folds the reply; the extension wiring posts 4 tool-use `ontum:turn-delta`s — name on the start — all before the terminal `ontum:turn-reply` whose html carries the folded tool-use + tool-result blocks; the shell labels the preview). Rows 1–6 tests still 7+8+9+12+16+15/exit 0 (no regression; row 6's `stream.test.js` updated its one `tool_use→null` stub to the row-7 streaming behaviour, count unchanged at 15). Honest scope: the fold + render + partial-stream plumbing + host message protocol are proven host-free against the captured tool-call shapes; pixel-level DOM mutation needs a webview host and a real billed tool-calling turn is left to a human at the surface. |
| 8 | Render diffs / edits with accept–reject | build | green | An edit tool-call renders as a diff with Accept/Reject. `diff.js` (`isEditTool`/`diffLines`/`diffFromToolUse`) folds an edit `tool_use` into ordered diff lines (`{tag:'context'\|'del'\|'add'\|'hunk',text}`) via a small, deterministic common-prefix/suffix diff — handling `Edit` (old→new), `Write` (all-add), `MultiEdit` (hunk-separated edits), and `NotebookEdit` (new_source add, or del on `edit_mode:'delete'`); a non-edit tool folds to null (it stays row 7's JSON dump). `shell.renderDiffBlock`/`renderDiffLines` paint an Edit `tool_use` as a `data-diff-tool="true"` block (the file path + `+adds −dels` stat, `data-diff`-tagged add/del/hunk lines with a +/−/space gutter, and Accept + Reject buttons carrying the `data-tool-id`), all HTML-escaped, and `renderTranscriptRow` delegates edit tools to it (one source of truth with rows 3/5/7's fold). `shell.js` wires a **delegated** `ontum:diff-decision` click handler (on document via `closest`, so diffs spliced in by the live-tail (row 4) and turn-reply (rows 5–7) paths are wired too) that posts `{decision,toolId}`, locks the controls, and records the choice on the block. `extension.recordDiffDecision`/`getLastDiffDecision` record the round-trip (latest wins; malformed ignored). Evidence: `node vscode/ontum-surface/test/diff.test.js` → 15 checks, exit 0 (isEditTool set; diffLines prefix/suffix + pure insert/delete; diffFromToolUse for Edit/Write/MultiEdit + null for a non-edit tool; renderTranscriptRow paints the diff + accept/reject with the tool id + NOT the row-7 JSON pre + HTML-escaping + a non-edit tool keeps the row-7 pre; foldReply folds an Edit turn into a tool-use entry whose rendered row IS the diff; the shell carries the delegated handler + message type; recordDiffDecision records accept/reject + ignores malformed; the webview message round-trip via the real onDidReceiveMessage; sendPrompt's turn-reply html carries the folded diff + accept/reject for an Edit turn — via a fake engine, no billed call). Rows 1–7 tests still 7+8+9+12+16+15+15/exit 0 (no regression). Read-only smoke: scanned the 3 live sessions for this cwd — none holds an edit tool-use (all "Warmup"), so the fold is proven host-free by the test, not faked against an absent edit. Honest scope: the diff fold + render + the accept/reject AFFORDANCE + its decision round-trip are proven host-free; actually applying or reverting an edit on disk is the host permission flow's job (row 9) / a human's — the surface renders the decision, it does not fake the effect, and pixel-level DOM mutation needs a webview host. |
| 9 | Permission prompts (canUseTool / permission-mode surface) | spike→inherit | green | The surface drives the inherited engine's permission levers. `engine.PERMISSION_MODES` is the exact four-mode list `claude --help` advertises (verified live this tick: `--permission-mode` choices `acceptEdits, bypassPermissions, default, plan`, plus `--allowedTools`/`--disallowedTools`/`--dangerously-skip-permissions`); `engine.normalizePermissionMode` validates a mode (unknown → `default`, so a typo/hostile value can never escalate to bypass); `engine.engineArgs` threads the chosen mode into `--permission-mode` and emits `--allowedTools`/`--disallowedTools` for a non-empty allow/deny policy (the canUseTool levers the CLI exposes). `shell.renderPermissionControl`/`renderShell` paint the composer's permission `<select>` (all four modes, the current one `selected`, an unknown value shown as `default`, a `data-mode` mirror, and a per-mode human label) and a `change` handler posting `ontum:set-permission-mode`. `extension.setPermissionMode` records + normalizes the chosen mode; `sendPrompt` threads it into the driven turn (was hardcoded `default`); `renderPanel` re-paints the surface in the in-force mode. Evidence: `node vscode/ontum-surface/test/permission.test.js` → 14 checks, exit 0 (PERMISSION_MODES matches the advertised list; normalize passes the four + defaults the rest incl. null/`BYPASS`/objects; engineArgs carries each mode + normalizes a bad one + defaults with no opts + emits/omits allow-deny lists; renderPermissionControl renders four options with the current selected + never auto-selects bypass for an unknown; renderShell carries the select + the host-passed selected mode + the `ontum:set-permission-mode` change handler; setPermissionMode records valid + normalizes bad; a webview `ontum:set-permission-mode` round-trip via the real onDidReceiveMessage sets `plan` then falls back to `default` on a bad value; sendPrompt drives a turn whose spawned argv carries `--permission-mode plan` — proven by a fake spawn that captures argv, no billed call, the plan turn folds a clean reply). Rows 1–8 tests still 7+8+9+12+16+15+15+15/exit 0 (no regression). HONEST SCOPE: the permission-MODE surface + allow/deny policy (the canUseTool levers the `--print` CLI exposes, verified live) flow into the real engine argv, host-free. An interactive mid-turn `canUseTool` control-request callback is an SDK/persistent-session feature whose live event shape this CLI version (2.0.19) does not advertise in `--help`; the surface governs permissions via the mode + allow/deny policy the CLI DOES expose, and a real billed turn exercising a live prompt is a human's to run. |
| 10 | Slash commands | spike→inherit | green | A slash command is PASS-THROUGH to the engine (the spike's `inherit`): sent as the turn's prompt text down the SAME stream-json channel row 5 drives — the engine interprets `/help`/`/clear`/`/compact`/a project's custom `/foo`. The surface owns only the slash SURFACE: `slash.js` (`isSlashCommand`/`parseSlashCommand`) recognises + splits a composed command (`/git:commit tidy` → `{name:'git:commit',args:'tidy',raw}`; conservative — a bare `/`, `//`, or `/ ` is not a command), and `slash.discoverCommands`/`listSlashCommands` fold the SAME on-disk command store the CLI reads (`<cwd>/.claude/commands/*.md` + `~/.claude/commands/*.md`, namespaced by subdir → `git:commit`, description from front-matter `description:` or the first body line) merged with the common built-ins, deduped + sorted, **project shadows user shadows built-in** (the CLI's own override order). `shell.renderSlashMenu`/`renderShell` paint the composer's slash palette (selectable `data-command` items, scope + description, all escaped) and the shell script filters it as the human types a `/` prefix + fills the composer with `/<name> ` on pick (never blocks an unlisted command). `extension.readSlashCommands` discovers the project+user store on each render and passes it to the shell; a slash command sent via the composer rides the existing `ontum:send-prompt` → `sendPrompt` → `driveTurn` path (pass-through, unaltered). Evidence: `node vscode/ontum-surface/test/slash.test.js` → 13 checks, exit 0 (isSlashCommand true/false table; parseSlashCommand name/args/raw + null; discoverCommands over a fake `.claude/commands` store — namespaced subdir + front-matter description + non-.md ignored + missing-dir tolerant + sorted; listSlashCommands merges built-ins+project with project shadowing `help` + dedup + sort; renderSlashMenu escaped selectable items + empty note; renderShell carries the palette + the filter/fill wiring; extension.readSlashCommands discovers the project commands for the cwd; encodeUserMessage sends the raw `/command` verbatim; a webview-sent `/compact` drives a turn whose **stdin got the exact `/compact` text** — fake spawn, no billed call). Rows 1–9 tests still 7+8+9+12+16+15+15+15+14/exit 0 (no regression). Read-only smoke: this cwd has no `.claude/commands` dir yet, so live discovery returns the built-ins only (honest — the custom fold is proven by the test against a fake store, not faked against an absent one). HONEST SCOPE: the engine remains authoritative on what each command DOES and on the full built-in set; the surface recognises + discovers + offers + routes, and an unlisted command still sends. A real billed turn that runs a live `/command` is a human's to run. |
| 11 | Plan mode | spike→inherit | green | The spike resolved this to `inherit`: `--permission-mode plan` is already threaded into the drive argv by `engine.engineArgs` (row 9). Plan mode runs the engine READ-ONLY and, when its plan is ready, the engine calls the `ExitPlanMode` tool (the name this CLI version's tool registry exposes) presenting the plan and asking the human to approve proceeding. The surface owns the plan SURFACE beyond the mode value: `plan.js` (`isExitPlanTool` recognises `ExitPlanMode`/legacy `exit_plan_mode`; `isPlanMode` normalized; `planFromToolUse` folds the ExitPlanMode `tool_use` → `{name,toolId,plan}`, a missing/non-string plan folding to empty, never faked; `nextModeOnPlanDecision` EXITS plan on approve — conservatively to `default`, never escalating to acceptEdits/bypass — and stays in `plan` on keep / an unknown decision). `shell.renderPlanBlock` paints an ExitPlanMode `tool_use` as a `data-plan-tool="true"` plan card (the proposed plan text + Approve & proceed / Keep planning buttons carrying `data-tool-id`), `renderTranscriptRow` delegates the ExitPlanMode tool to it before the row-8 diff / row-7 JSON paths (one source of truth with rows 3/5/7/8), `renderShell` shows a read-only plan-mode banner (`ontum-plan-badge`, `data-region="plan-mode"`) only when the in-force mode is `plan`, and a **delegated** `ontum:plan-decision` click handler (document-level via `closest`, so plan cards spliced in by the live-tail (row 4) / turn-reply (rows 5–7) paths are wired too) posts `{decision,toolId}` + locks the controls. `extension.recordPlanDecision`/`getLastPlanDecision` record the round-trip and — on approve — transition `permissionMode` out of `plan` (then re-render the panel in the now-in-force mode); wired into `onDidReceiveMessage`. Evidence: `node vscode/ontum-surface/test/plan.test.js` → 17 checks, exit 0 (isExitPlanTool set; isPlanMode normalized; planFromToolUse fold + null for a non-plan tool + empty-on-odd-input; nextModeOnPlanDecision exit-on-approve / stay-on-keep / keep-current-on-unknown + never bypass/acceptEdits; engineArgs carries `--permission-mode plan`; renderTranscriptRow paints the plan card + approve/keep with the tool id + NOT the row-7 pre + HTML-escaping + a non-plan tool keeps its row-8 diff / row-7 pre; renderShell carries the delegated handler + the plan banner only in plan mode; foldReply folds a plan-proposing turn into a tool-use entry rendered AS the plan card; recordPlanDecision records + exits plan on approve / stays on keep / ignores malformed without clobbering the last good one; the webview `ontum:plan-decision` round-trip via the real onDidReceiveMessage exits plan mode; sendPrompt drives a `--permission-mode plan` turn whose reply html carries the plan card — fake spawn, no billed call). Full suite re-run: all 12 test files PASS (no regression). HONEST SCOPE: the plan-mode surface — the read-only banner, the ExitPlanMode plan card, the approve/keep AFFORDANCE, its decision round-trip, and the exit-plan-mode permission transition — is proven host-free against the captured ExitPlanMode shape and the live-verified `--permission-mode plan` lever; actually running the approved work is the engine's job under the exited mode, and a real billed plan turn is a human's to run. |
| 12 | @-mentions / IDE selection context | build | green | An @-mention pulls a workspace file into context and the active editor selection rides the turn — both are CONTEXT the surface attaches to the prompt that rides the SAME stream-json channel row 5 drives (the engine reads `@file` + the selection note; the surface does not re-implement file reading). `mentions.js` owns the surface: `isMention`/`parseMentions` recognise + extract `@path` tokens (conservative — an email's `@` / a bare `@` / a `/path` are NOT mentions, a trailing sentence dot is trimmed off the path), `mentionQuery` returns the partial token being typed at the caret (so the palette filters as you type), `discoverFiles`/`listMentionTargets` fold the workspace tree into a BOUNDED, sorted file list (breadth-first, skipping the noise dirs `.git`/`node_modules`/`dist`/… + dotfiles, capped at `limit` default 500 so a huge repo never hangs the palette; a missing/unreadable dir -> []), and `selectionContext`/`withSelectionContext` fold an IDE selection record `{file,startLine,endLine,text}` into a marked, deterministic preamble (`[selection src/app.js:10-20]\n<text>`, 1-based lines; single-line -> `:N`; nothing to attach -> '') prepended to the prompt — and a NO-selection prompt is left UNCHANGED (the row-5/10 pass-through is preserved, an @-mention rides through verbatim). `shell.renderMentionMenu`/`renderShell` paint the composer's @-palette (selectable `data-mention="<path>"` items, escaped) + a filter-as-you-type/`@`-token script (`refreshMention` shows + filters by the partial via the `MENTION_TAIL` regex that mirrors `mentions.mentionQuery`; picking replaces the `@token` being typed with `@<path> `; Escape closes it). `extension.readMentionTargets` discovers the workspace fold on each render and passes it to the shell; `currentSelection` reads `vscode.window.activeTextEditor`'s selection (empty selection -> nothing attached; 1-based lines) behind a `__setSelectionForTest` seam, and `sendPrompt` threads `withSelectionContext(prompt, currentSelection())` into the driven turn. Evidence: `node vscode/ontum-surface/test/mentions.test.js` → 15 checks, exit 0 (isMention/parseMentions recognise + extract + offsets + leave an email's `@`/bare `@`/`/path` alone + trim a trailing dot; mentionQuery partial-at-caret + null outside a token; discoverFiles folds a fake workspace — skips `.git`/`node_modules`/dotfiles, posix separators, sorted, honours `limit`, missing-dir -> []; selectionContext marked preamble + single-line `:N` + empty -> ''; withSelectionContext prepends a selection + leaves a no-selection prompt (incl. an @-mention) verbatim; renderMentionMenu escaped selectable items + honest count + empty note; renderShell carries the palette + the filter/replace wiring + ships the container with no files; extension.readMentionTargets discovers the workspace files for the cwd; the round-trip — a webview send WITH an injected selection drives a turn whose **stdin carries the `[selection …]` preamble + the typed prompt (with its @-mention) verbatim**, and a send with NO selection sends the typed `@README.md` text exactly — via a fake spawn, no billed call). Rows 1–11 tests still pass (no regression — full suite 13/13). Read-only smoke: the live fold of `vscode/ontum-surface` returned its 8 real shallow files (sorted, bounded at the cap), and a selection-context sample folded `engine.js:94-124` + its text into the marked preamble. HONEST SCOPE: the engine remains authoritative on what an @-mention actually pulls in; the surface recognises + discovers + offers + attaches, and an unlisted path still sends. A LIVE editor selection comes from the VS Code host (`window.activeTextEditor`) — the fold + the attach are proven host-free against a selection record + the injected seam, not against a live host; a real billed turn that reads a mentioned file is a human's to run. |
| 13 | MCP tools available + invocable | inherit | green | An MCP server's tools are LOADED by the inherited engine (the same `claude` binary reading the same cwd config) and ride along in the init event's `tools` list named `mcp__<server>__<tool>` — "available" IS that live list; "invocable" is the engine calling the tool mid-turn (pass-through), pre-authorizable by the row-9 allow/deny policy by name. Live-verified this tick: `claude --help` advertises `--mcp-config <configs...>`, `--strict-mcp-config`, the `mcp` subcommand, and `--allowedTools`/`--disallowedTools` (which authorize `mcp__server__tool` names). The surface owns only the MCP SURFACE: `mcp.js` (pure, no `vscode`) — `parseMcpTool`/`isMcpTool`/`mcpToolName` recognise + split + join the `mcp__server__tool` identifier (split on the FIRST `__` so a tool name may itself contain `__`; a plain `Bash`/`Read` is not MCP); `mcpServersFromTools` groups the LIVE init tools list by server (sorted, deduped — the proof of what loaded); `transportOf`/`readMcpServerNames` fold a config's `mcpServers` object (explicit `type`, else `url`→http, else `command`→stdio, else unknown; missing/torn JSON tolerant → []); `discoverMcpServers` merges the project `<cwd>/.mcp.json` + the user `~/.claude.json` `mcpServers` with **project shadowing user** (the precedence the CLI honours), sorted + deduped; `listMcpServers` annotates each configured server with the live tools it exposes + an honest `available` flag (configured-but-not-loaded → `available:false`; an env-named server in no config file → surfaced with scope `live`). `engine.engineArgs` threads `--mcp-config`/`--strict-mcp-config` (additive — emitted ONLY when a config is supplied, so the default cwd-inheriting drive is unchanged). `shell.renderMcpPanel`/`renderShell` paint the Sessions-aside MCP region (a `data-mcp-server` block per server carrying its scope + transport, a `data-available` flag, and its tools as `data-mcp-tool="mcp__server__tool"` items — the exact id the engine names + the allow-list authorizes — all HTML-escaped, with an honest empty note); `extension.readMcpServers` discovers the configured servers each render and annotates them with `lastEngineTools` (the live tools list captured from each turn's init event in `sendPrompt`, which re-renders so the panel reflects the now-loaded servers). Evidence: `node vscode/ontum-surface/test/mcp.test.js` → 17 checks, exit 0 (parseMcpTool/isMcpTool/mcpToolName split + reject + round-trip incl. a `__`-containing tool name; mcpServersFromTools groups + sorts + dedups + ignores non-MCP; transportOf shapes; readMcpServerNames fold + missing/torn → []; discoverMcpServers merge + **project shadows user** + sorted/deduped; listMcpServers available-when-live + configured-not-loaded honest + a live-only server tagged scope `live`; renderMcpPanel escaped blocks + the exact `mcp__server__tool` id + a hostile name escaped + empty note; renderShell ships the MCP region with + without servers; engineArgs adds `--mcp-config`/`--strict-mcp-config` only when supplied + strict is opt-in; extension.readMcpServers discovers a fake project `.mcp.json` then, after a turn whose init event named `mcp__weather__forecast`, marks weather available + db honestly not-loaded — fake spawn, no billed call; a READ-ONLY live smoke folds the real `~/.claude.json` without throwing). Full suite re-run: all 14 test files PASS (no regression). Read-only smoke: this machine's `~/.claude.json` carries one user-scope server and the worktree has no project `.mcp.json` (honest — the live tools list is null until a real billed turn, so `available` is false until the engine loads it). HONEST SCOPE: the engine remains authoritative on what an MCP tool DOES and on whether a server actually connects; the surface recognises + discovers + offers + annotates (it does not re-implement an MCP client), and a real billed turn that invokes an MCP tool is a human's to run. |
| 14 | Hooks / skills / settings inherited (the environment) | inherit | green | The hooks, skills, and settings configured on disk are LOADED + RUN by the inherited engine (the same `claude` binary reading the same cwd config) for free — the spike resolved this to `inherit` (the init event proves the environment is loaded). The surface owns only the SURFACE of the environment (discover + display), folded host-free from the same on-disk config the CLI reads exactly as `mcp.js` folds the MCP config. `environment.js` (pure, no `vscode`): `settingsPathsFor`/`readSettingsSource`/`discoverSettings` fold the settings LAYER search path the CLI merges (user `~/.claude/settings.json` → project `<cwd>/.claude/settings.json` → local `settings.local.json`, increasing precedence), each layer `present`-flagged with its sorted top-level keys (a MISSING layer → `present:false`; a present-but-TORN file → `present:true` `keys:[]` — honest, never invented); `hooksFromSettings`/`discoverHooks` fold the CLI hooks shape (`{<Event>:[{matcher?,hooks:[{type,command}]}]}`) to a flat `[{event,matcher,type,command,scope}]` merged across all layers + sorted by (event,matcher,command), tolerant of odd shapes (an event-wide hook → empty matcher); `frontMatterValue` reads a SKILL.md front-matter key as an inline scalar AND a folded block scalar (`description: >` — gathers the more-indented continuation lines joined with spaces, the shape the real skills use); `readSkillMeta`/`discoverSkills`/`skillsDirFor`/`listSkills` fold `<base>/.claude/skills/*/SKILL.md` (a skill with no/torn SKILL.md is still surfaced under its dir name — the engine loads a skill by its dir) with **project shadowing user**; `listEnvironment` combines the three folds → `{settings,hooks,skills}`. `shell.renderEnvPanel`/`renderShell` paint the Sessions-aside Environment region (three labelled groups — settings layers as `data-env-settings="<scope>"`+`data-present`+keys, hooks as `data-env-hook="<event>"`+matcher+scope, skills as `data-env-skill="<name>"`+description+scope — all HTML-escaped, honest per-group empty notes, subtitle counts); `extension.readEnvironment` folds `listEnvironment({projectDir:cwd,userDir:homedir})` each render (degrades to empty groups, never throws) + passes it to the shell. Evidence: `node vscode/ontum-surface/test/environment.test.js` → 22 checks, exit 0 (settingsPathsFor user/project/local precedence; discoverSettings present-flags + sorted keys + missing→`present:false` + torn→`present:true keys:[]`; hooksFromSettings flat fold incl. event-wide empty matcher + non-object tolerance; discoverHooks merges 4 layers scoped + sorted + no-sources→[]; frontMatterValue inline + quoted + folded `>` block scalar + absent→''; readSkillMeta fold + no-SKILL.md-still-surfaced; discoverSkills sorted + missing→[]; listSkills **project shadows a shared skill** + deduped/sorted; listEnvironment combines all three; renderEnvPanel escaped groups + a hostile skill name escaped + per-group empty notes; renderShell ships the Environment region with + without an environment; extension.readEnvironment discovers a fake project+user config via a fake `vscode`/`os.homedir`; a READ-ONLY live smoke folds the real `~/.claude` + project `.claude` without throwing). Full suite re-run: all 15 test files PASS (no regression). Read-only smoke: this machine's real config folds clean — the project `.claude/settings.json` (hooks: PreToolUse/PostToolUse/SessionStart/Stop/UserPromptSubmit) + `.claude/skills/*` + the user `~/.claude` layers, all surfaced host-free. HONEST SCOPE: the engine remains authoritative on what a hook DOES and on whether a skill actually loads (it runs them mid-turn); the surface discovers + displays the inherited environment, and a real billed turn that fires a hook or invokes a skill is a human's to run. |
| 15 | Image / file attach | build | green | An attached image / file rides the turn as a content block in the SAME Messages-API user envelope row 5 drives — an image as a base64 `image` block, a PDF as a base64 `document` block, a text file inlined as a marked `text` block — the engine reads it (vision/PDF), the surface does not re-implement vision. The surface owns only the attachment SURFACE: `attach.js` (pure, no `vscode`) — `mediaTypeForImage`/`isImageFile`/`classifyAttachment` classify a file by name → image (png/jpg/jpeg/gif/webp) / document (pdf) / text (everything else; conservative — an unknown type is never guessed to be an image the engine would reject); `readAttachment` folds the on-disk bytes into a bounded record (base64 `data` for image/document, utf8 `text` for a text file), capped at 5 MB and refusing a missing/oversized/unreadable file with an honest error record (never throws, never silently truncates — the cap binds both the stat AND the actual buffer length), injectable `fs`; `attachmentBlock`/`attachmentBlocks` encode the record(s) into the Messages-API blocks (image→`{type:'image',source:{type:'base64',media_type,data}}`, document→`{type:'document',…}`, text→`{type:'text',text:'[file <name>]\n<content>'}`), dropping an errored attachment; `displayAttachment` is a data-free tray view (the base64 never reaches the DOM). `engine.encodeUserMessage(text, attachments)` folds the blocks AHEAD of the typed text (the convention the API expects — the image/file leads, the question follows), admitting only well-formed `{type:string}` blocks; with NO attachments the content is EXACTLY `[{type:'text',text}]`, so every earlier row's verbatim-stdin proof (the prompt is `content[0].text`) still holds — purely additive — and `driveTurn` threads `o.attachments`. `shell.renderAttachTray`/`formatBytes` paint the composer's attachment tray (a `data-attachment` chip per staged file with its kind+size or honest error + a `data-attach-name` Remove button) + a 📎 attach button posting `ontum:attach-file` + a delegated remove handler posting `ontum:remove-attachment`; `extension.addAttachment`/`removeAttachment`/`getAttachments`/`pickAttachment` stage (de-dup by name) + drop + view + pick the attachments behind a `__setAttachPickerForTest` seam (production opens `vscode.window.showOpenDialog`), `sendPrompt` encodes the staged set into blocks ahead of the prompt then CLEARS it after the turn (the attachment rides exactly one turn), `renderPanel` paints the tray. Evidence: `node vscode/ontum-surface/test/attach.test.js` → 26 checks, exit 0 (classify image/document/text + case-insensitive + unknown→text; isImageFile/mediaTypeForImage; readAttachment folds a REAL 1×1 PNG → base64 round-trips the bytes + a text file → inlined utf8 + missing→`not found` + oversized→`too large`, no throw; attachmentBlock image/text/null + attachmentBlocks drops the errored; displayAttachment data-free; encodeUserMessage no-attach is EXACTLY `[{type:text}]` + folds blocks AHEAD + admits only well-formed; renderAttachTray escaped chips + remove wiring + honest error + empty→'' + renderShell ships the tray+button+wiring with/without attachments; extension addAttachment/getAttachments data-free stage + de-dup + removeAttachment; the round-trip — a webview attach via the injected picker stages the PNG, the next driven turn's **stdin carries it as a base64 image block AHEAD of the typed prompt**, the staged set CLEARS after the send, and a plain turn still sends exactly the text block — fake spawn, no billed call). Full suite re-run: all 16 test files PASS (no regression). HONEST SCOPE: the engine remains authoritative on what an attachment MEANS (vision/PDF reading); the LIVE file picker is the VS Code host dialog (`window.showOpenDialog`) — proven host-free against a path via the seam, not a live host; a real billed turn carrying an image is a human's to run. |
| 16 | Resume / continue an existing session | spike→inherit | green | The spike resolved this to `inherit`: the SAME `claude` binary RESUMES/CONTINUES a past conversation — live-verified this tick `claude --help` advertises `-c, --continue` (the most recent conversation), `-r, --resume [sessionId]` (a specific one), and `--fork-session` (when resuming, create a new session id; valid with either). The surface drives those inherited levers and owns only the choice SURFACE. `engine.RESUME_MODES` is `['new','continue','resume']`; `engine.normalizeResumeTarget` validates a target conservatively (an unknown mode, or a `resume` with no sessionId, → `new` — a turn never silently resumes the wrong session); `engine.resumeArgsFromTarget` maps the target → the engineArgs fragment (`continue`→`{continueSession}`, `resume`→`{resume:id}`, `+fork`→`{forkSession}`, `new`/id-less/lone-fork → `{}`); `engine.engineArgs` threads `--continue` / `--resume <id>` (+ `--fork-session` ONLY alongside a resume/continue) into the drive channel's argv — additive, so the default fresh-session drive is unchanged. `shell.renderResumeControl`/`renderShell` paint the composer's session-continuity surface (New / Continue recent / Resume selected buttons, the in-force one `aria-pressed`, a `data-resume-mode` mirror, the Resume button carrying the row-2 selected session id + `disabled` when none is selected, all escaped) + a delegated `wireResume` handler posting `ontum:set-resume`. `extension.setResumeTarget`/`getResumeTarget` record + normalize the chosen target (a `resume` with no explicit id defaults to the selected session); `sendPrompt` threads `resumeArgsFromTarget(resumeTarget)` into the driven turn; `renderPanel` paints the in-force target. Evidence: `node vscode/ontum-surface/test/resume.test.js` → 25 checks, exit 0 (RESUME_MODES; normalizeResumeTarget valid + id-less-resume→new + unknown/hostile→new + bare-string + fork preserved; resumeArgsFromTarget new→{} / continue / resume(+fork) / id-less-resume→{} / lone-fork dropped; engineArgs carries `--continue` / `--resume <id>` / `--fork-session` only-alongside + no-opts carries none; renderResumeControl three buttons + in-force pressed + Resume carries selected id + disabled-without-one + unknown→new + hostile id escaped; renderShell ships the region + `ontum:set-resume` + `wireResume`; setResumeTarget records continue/resume + normalizes bad; the webview `ontum:set-resume` round-trip via the real onDidReceiveMessage; selecting a session then `resume` (no id) resumes the selected one; sendPrompt drives a turn whose spawned argv carries `--resume <selected id>`, then a continue turn carries `--continue` not `--resume`, then a new-session turn carries neither — fake spawn, no billed call). Full suite re-run: all 17 test files PASS (no regression). HONEST SCOPE: the resume/continue choice surface + its target→argv mapping (the levers the `--print` CLI exposes, verified live) flow into the real engine argv, host-free; a real billed turn that actually replays a past conversation is a human's to run at the surface. |
| 17 | Stop / interrupt a running turn | spike→inherit | green | Normal Claude Code lets the human STOP an in-flight turn (Esc / the Stop button). Grounded live this tick: `claude --help` advertises NO interrupt/abort/control flag for the `--print` headless channel (no `--interrupt`/`--abort`/control-request option exists in 2.0.19), so the only HONEST way to stop a headless turn is to TERMINATE the spawned engine process — the same `child.kill()` the watchdog uses, here user-initiated; the turn then settles via its `close` with whatever events arrived, folded by `foldReply` and LABELLED interrupted, never a fabricated completion. `engine.markInterrupted(reply)` is the pure labeller — a shallow merge that re-tags `subtype:'interrupted'` + `isError:true` while PRESERVING every folded field (sessionId + the entries/text that streamed before the stop + exitCode/stderr), so a stopped turn shows its honest partial result, not an empty one; `engine.INTERRUPT_SUBTYPE` is the `'interrupted'` tag (distinct from a `no-result` engine failure or a `timeout`). `engine.driveTurn` hands `opts.onStart(handle)` a control handle `{interrupt(), child}` SYNCHRONOUSLY once the engine spawns; `interrupt()` kills the process (idempotent — returns `false` once the turn has settled, so a late Stop is a harmless no-op) and the `close` handler resolves `markInterrupted(foldReply(events))`; `engine.engineArgs` carries NO interrupt/control flag (the honest mechanism is the signal, not an argv lever). `shell.renderShell` paints the composer's Stop button (`ontum-compose-stop`, `hidden` by default), a shared `setTurnRunning(running)` toggle shows it on submit + re-hides it on the terminal `ontum:turn-reply`, a Stop click posts `ontum:interrupt-turn`, and the turn-reply path reports an `interrupted` reply as a calm "Turn stopped." (not a red "Turn failed."). `extension.interruptTurn`/`isTurnRunning` track the in-flight turn: `sendPrompt`'s `onStart` captures the handle into `activeTurnControl`, a `finally` clears it when the turn settles (so a late Stop is a no-op), and the `ontum:interrupt-turn` message handler calls `interruptTurn()`. Evidence: `node vscode/ontum-surface/test/stop.test.js` → 19 checks, exit 0 (INTERRUPT_SUBTYPE; markInterrupted labels + preserves the partial fold + keeps a prior error + is pure + tolerates a non-object; engineArgs has no interrupt/abort/control flag; driveTurn hands onStart a handle synchronously, `interrupt()` stops a live turn (returns true) + kills the process, the stopped turn settles as an honest interrupted partial whose pre-stop prose survives the fold, a second interrupt after settle returns false; extension isTurnRunning false when idle + a no-turn interruptTurn is a no-op, a turn in flight holds the handle, interruptTurn stops it + the reply folds interrupted + the handle drops + a late Stop is a no-op; a real `ontum:interrupt-turn` webview message kills the engine + settles interrupted; renderShell paints the hidden Stop button + posts ontum:interrupt-turn + toggles it with setTurnRunning(true/false) + branches on the interrupted subtype → "Turn stopped." — fake stalling spawn, no billed call). Full suite re-run: all 18 test files PASS (no regression). HONEST SCOPE: the stop surface terminates a real driven turn and labels the partial result honestly, host-free against a fake stalling engine; there is no headless mid-turn control channel in this CLI version, so stopping a real billed turn is the same process signal a human triggers at the surface. |
| 18 | Cost / usage display | build | green | Normal Claude Code shows what a turn cost — dollars, tokens in/out (incl. prompt-cache reads/writes), duration, model turns — and the session's cumulative spend. The cost/usage DATA already rides the reply (`engine.foldReply` folds `total_cost_usd`→`cost`, the `usage` object, `duration_ms`→`durationMs`, `num_turns`→`numTurns` off the terminal `result` event), so row 18 owns only the DISPLAY. `usage.js` (pure, no `vscode`): `num` gates a value to a finite number else null (the single honesty gate — a missing/NaN/Infinity field becomes an em-dash, never a fabricated 0); `tokenTotals(usage)` folds the `usage` object → `{input,output,cacheRead,cacheCreation,total}` (`total` sums the four real billed token kinds, or null when NONE was reported); `foldUsage(reply)` → the one-turn display model `{cost,durationMs,numTurns,tokens,hasData}` (`hasData` true iff at least one usage signal was reported, so a turn that errored before its `result` shows an honest "no usage reported" state, not a fake $0 row); `accumulateUsage(prev,reply)` folds a running SESSION total (pure — `prev` never mutated; `turns` counts only turns that reported real usage, so a Stop/spawn-error that reported nothing never inflates the tab); `formatUsd` (4dp sub-$1 / 2dp ≥$1, em-dash for null), `formatTokens` (en-US thousands grouping), `formatDuration` (ms / s / m s — pure string math, deterministic), `usageSummary` (one honest line or "no usage reported"). `shell.renderUsageBar(reply,sessionTotal)`/`renderShell` paint the composer's usage meter (`data-region="usage"`, `data-has-usage`, per-stat `data-usage="cost|tokens|input|output|cache-read|cache-write|duration|turns"` spans + a `data-region="usage-total"` Session line, an honest "No usage reported yet." empty state, all HTML-escaped) + its CSS; the `ontum:turn-reply` handler swaps the `.ontum-usage` outerHTML with the host-rendered `m.usageHtml`. `extension.sendPrompt` sets `lastReply` + folds `sessionUsage = accumulateUsage(sessionUsage, reply)` after each turn and posts `usageHtml: renderUsageBar(reply, sessionUsage)`; `renderPanel` paints the in-force meter; `getSessionUsage` exposes the running total. Evidence: `node vscode/ontum-surface/test/usage.test.js` → 25 checks, exit 0 (tokenTotals sums the four + reports a missing field null + odd/absent usage → null no fake 0; foldUsage folds the model + marks a no-usage turn `hasData:false` + tolerates a non-object; accumulateUsage builds a running total + does NOT count a no-usage turn + is pure; formatUsd sub-cent + dash; formatTokens grouped + dash; formatDuration ms/s/m·s + dash; usageSummary one line + "no usage reported"; renderUsageBar paints the engine's own numbers + an honest empty state + no-reply empty + the running session total + escapes a hostile field; the extension round-trip — no session usage before any turn, a driven turn folds the reported cost/usage, sendPrompt posts the host-rendered meter on turn-reply, two turns accumulate onto the running total, and a no-usage errored turn does not inflate it — fake spawn, no billed call). Full suite re-run: all 19 test files PASS (no regression). HONEST SCOPE: every value is a FOLD of the engine's own reported usage — the surface never meters, estimates, or invents a number; an absent field shows an em-dash, never a fabricated 0; a real billed turn's live cost is whatever the engine's own `result` reports. |

**Self-grade rule.** A row is `green` only if its evidence column names something
a cold reader can check. The overall grade = (green rows / 18) with every
non-green row carrying its honest blocker. Parity-green = 18/18.
