# Overnight progress — the state each mortal tick re-derives from

Sessions are mortal; the files stay. Each overnight tick is a FRESH session that
reads this file (and the committed tree) to know where the build is, does one
bounded increment, updates this file, and commits. No tick relies on the memory
of the one before it — only on what is committed here.

## Marker status

- **MARKER:** parity checklist (`PARITY-CHECKLIST.md`) 18/18 `green`. **MET** — all 18 rows green, each with checkable evidence (a passing `node` test).
- **STATE:** `marker-met`  ← one of: not-started · spiking · building · marker-met · STOPPED-infeasible
- **Green rows:** 18 / 18
- **Honest-stop tripped?** no  ← set to `yes` + reason if the bridge proves infeasible (SDK and CLI both)
- **Bridge spike:** GO (see `SPIKE-FINDINGS.md`) — feasible via the CLI/SDK stream-json channel; all `spike` rows resolve to `inherit`.

## Current increment

1. ~~atom.branded-surface-bridge-spike.v0~~ — done (spike GO).
2. ~~atom.branded-surface-viewer.v0~~ — read + live-tail branded viewer **done** (rows 1–4 green: the branded standalone window + the session list/select + transcript read & render + live-tail the active session as it appends).
3. ~~atom.branded-surface-parity.v0~~ — drive a turn, build every row to green. **DONE — 18/18 green, marker MET.** (rows 5–13 **done**: drive a new turn + stream it incrementally + render tool calls and their results + render diffs / edits with accept–reject + the permission-mode surface (canUseTool levers the CLI exposes) + slash commands (recognise/parse/discover/route, pass-through to the engine) + plan mode (the ExitPlanMode plan card with approve/keep + the read-only banner + exit-plan-mode on approve) + @-mentions / IDE selection context (recognise/discover/offer @-file completions + attach the editor selection as a marked context preamble, pass-through to the engine) + MCP tools available + invocable (recognise/parse `mcp__server__tool`, group the live tools list by server, discover the configured servers from `.mcp.json` + `~/.claude.json`, annotate each with the live tools it exposes) + the inherited environment honoured (discover the settings LAYERS the CLI reads, the configured HOOKS folded across them, and the available SKILLS — project shadowing user) + image/file attach (classify/read/encode an image or file into the Messages-API content block(s) the turn carries ahead of the typed text) + resume/continue a session (the New / Continue recent / Resume selected surface threading `--continue` / `--resume <id>` (+ `--fork-session`) into the drive argv, conservative — an unknown/id-less target falls back to a fresh session) + stop/interrupt a running turn (the Stop button terminates the in-flight engine process — there is no headless control flag — and the turn settles as an honest interrupted partial, never a faked completion); next is row 18: cost / usage display).

## Tick log (append one line per tick, newest last)

- 2026-06-24 setup (supervised): scaffold authored; **bridge spike = GO** (CLI/SDK stream-json proven, env inherited); STATE→building. Next: increment 1 = the read+live-tail branded viewer (checklist rows 1–4).
- 2026-06-24 ~03:20 tick: **row 1 → green** — built the branded standalone webview (`vscode/ontum-surface/{package.json,extension.js,shell.js,media/ontum.svg}`); verified `node …/test/open-window.test.js` (7 checks, exit 0). Green 0→1/18. Next: row 2 (session list from the transcript store).
- 2026-06-24 ~03:50 tick: **row 2 → green** — added `sessions.js` (reads `~/.claude/projects/<encoded-cwd>/*.jsonl`, summarizes id/title/messageCount/branch, newest-first, torn-tail tolerant), `shell.renderSessionList` (selectable `data-session-id` buttons), and wired `extension.js` to read the store on open + record the `ontum:select-session` round-trip. Verified `node …/test/sessions.test.js` (8 checks, exit 0, fake store incl. torn line) + read-only smoke on the live store (3 real sessions). Row 1 test still 7/exit 0 (no regression). Green 1→2/18. Next: row 3 (read + render a transcript).
- 2026-06-24 ~04:20 tick: **row 3 → green** — added `transcript.js` (`readTranscript`/`foldTranscript` fold a session's `.jsonl` into ordered entries: user/assistant text, assistant thinking via the `thinking` field, `tool_use`, and `tool_result` riding a user turn; non-turn/empty blocks dropped; torn-tail tolerant), `shell.renderTranscript` (escaped, `data-kind`-tagged, error-flagged blocks + an honest empty-state note), and wired `extension.js` (`renderPanel`/`readSelectedTranscript`) to re-render the panel with the selected session's transcript. Verified `node …/test/transcript.test.js` (9 checks, exit 0; fake store with all four kinds + empty block + torn line; fold order, payloads, escaping, empty note, select→re-render round-trip) + read-only smoke on the live store (folded a real session to 3 entries: user-text, assistant-thinking, assistant-text). Rows 1–2 tests still 7+8/exit 0 (no regression). Green 2→3/18. Next: row 4 (live-tail the active session as it appends).
- 2026-06-24 ~04:48 tick: **row 4 → green** — added `livetail.js` (`tailTranscript` tails ONE session by byte offset, folding only lines completed since the held offset and advancing past complete lines only — a torn/partial final line is held + re-read, never skipped; offset-past-EOF flags `reset`; `activeSession` picks the newest file by mtime), factored `shell.renderTranscriptRow`/`renderTranscriptRows` (bare blocks) + an `ontum:append-entries` webview handler (`insertAdjacentHTML` onto the live list, `data-count`/`data-live`, autoscroll), and wired `extension.js` (`pumpTail`/`startTail`/`stopTail`: anchor tail at end on select → no history replay, `fs.watchFile`, post only-new entries). Verified `node …/test/livetail.test.js` (12 checks, exit 0: seed fold→EOF; idempotent re-read; append tails only the new entry; torn partial yields nothing + holds offset, then folds in once completed; offset-past-EOF resets; activeSession newest; bare-row render + escaping; append-handler present; open→select→append posts ONLY the new block) + read-only smoke on the live store (tailed a real "Warmup" session: from 0→3 entries to offset 5765, from mid→tail half only, from EOF→0). Rows 1–3 tests still 7+8+9/exit 0 (no regression). Green 3→4/18. Increment 2 (viewer rows 1–4) complete. Next: row 5 (drive a new turn — the first `spike`-source row, via the inherited CLI/SDK bridge).
- 2026-06-24 ~05:20 tick: **row 5 → green** — added `engine.js` (the turn driver over the inherited CLI stream-json channel): `encodeUserMessage` (newline-terminated user envelope for stdin), `engineArgs` (`--print --input-format stream-json --output-format stream-json --include-partial-messages --verbose --permission-mode default` + `--resume/--fork-session/--model` pass-through), `foldReply` (folds the output events → one reply: session id + `tools` from `system/init`, assistant entries via the SAME `transcript.foldTranscript`, final text from `result` or honest assistant-text fallback, cost/usage/duration/num_turns + error state from `result`, missing result → `no-result`+error), and `driveTurn(spawn injectable)` (writes the prompt to stdin, buffers NDJSON across torn stdout chunks, resolves `foldReply`). Grounded the inherit claim with a **live** `claude --help` (the stream-json/permission-mode/resume/fork flags are advertised on this machine). Wired to the surface: `shell.js` composer (textarea+Send, Enter-to-send) posts `ontum:send-prompt`; `extension.sendPrompt` (+ `__setSpawnForTest` seam) drives the turn and posts `ontum:turn-reply` (rendered blocks + cost/subtype) the composer splices + status-lines. Verified `node …/test/engine.test.js` (16 checks, exit 0: encode shape; args incl. both stream-json + default mode + resume/fork/model; foldReply over captured real-shaped events incl. fallback/no-result/error; driveTurn round-trip via a **fake** engine — stdin got the prompt, reply folded, chunk-split reassembled, spawn-error rejects; extension `sendPrompt` + `ontum:send-prompt` handler drive a turn with an injected fake engine + post the reply; composer markup). Rows 1–4 tests still 7+8+9+12/exit 0 (no regression). Green 4→5/18. Honest scope: round-trip proven vs a fake engine (no billed model call); a real billed turn is a human's to run; incremental partial streaming is row 6. Next: row 6 (stream the turn via the `onEvent`/`stream_event` partials).
- 2026-06-24 ~05:49 tick: **row 6 → green** — taught `engine.js` to interpret the `--include-partial-messages` `stream_event` partials: `partialDelta(event)` folds ONE event → ONE tiny render instruction (`{phase:start|delta|stop, index, kind, text}`), tolerant of the wrapped + bare content_block forms, deriving `kind` (`assistant-text`/`assistant-thinking`, the same kinds `foldTranscript` emits) from the delta itself so a delta renders even with a torn block-start; `tool_use` starts fold to null (row 7). `assembleStream(events)` folds the partials into ordered live blocks, and the test proves that preview **equals** `foldReply`'s final entries (one source of truth). Wired: `extension.postTurnDelta` posts `ontum:turn-delta` per partial via `driveTurn`'s `onEvent` hook; `shell.js` paints per-index `data-streaming` blocks (text via `textContent`, live caret) and the terminal `ontum:turn-reply` drops the preview before splicing the folded reply. Verified `node …/test/stream.test.js` (15 checks, exit 0: partialDelta start/delta/stop for thinking+text, bare form, tool_use→null, non-partials→null; assembleStream order + preview==fold; driveTurn fires onEvent for every event across torn chunks; extension posts one delta per partial **in order, all before** the reply, deltas reconstruct thinking+text, reply intact, postTurnDelta→null for a non-partial; shell markup). Rows 1–5 tests still 7+8+9+12+16/exit 0 (no regression). Green 5→6/18. Honest scope: plumbing + host message protocol proven host-free against the spike's captured partial shapes; live partials exist only in a real billed run (the on-disk store keeps final messages, not partials), so pixel-level DOM mutation needs a host and a real streaming turn is a human's to run. Next: row 7 (render tool calls + results).
- 2026-06-24 ~06:20 tick: **row 7 → green** — render tool calls and their results. `engine.foldReply` already folds a tool-calling turn (an `assistant` `tool_use` + the following `user` `tool_result` + the assistant's continuation) into ordered `[tool-use, tool-result, assistant-text]` entries via the SAME `transcript.foldTranscript`, and `shell.renderTranscriptRow` already paints `data-kind="tool-use"` (name + JSON input `<pre>`) and `data-kind="tool-result"` (text `<pre>`, `data-error` on failures, all escaped) — this tick proved that end-to-end AND added the **live tool-call preview**: taught `engine.partialDelta` to stream a `tool_use` `content_block_start` → `{phase:start,index,kind:'tool-use',name}` and its `input_json_delta` fragments → `{phase:delta,…,text:partial_json}` (was row-7-stub null before), `assembleStream` to carry the tool `name` (text/thinking blocks keep the bare `{index,kind,text}` shape so row 6's preview==fold equality is untouched), and `shell.js`'s delta handler to label the streaming block `tool ▸ <name>` (`data-tool-name`). Verified `node …/test/tools.test.js` (15 checks, exit 0: foldReply order + name/input/result/is_error; renderTranscriptRow tool-use + tool-result + error flag + input HTML-escaping; partialDelta start-with-name + input_json_delta + stop + non-partials→null; assembleStream named block + **streamed input JSON parses to the folded input**; driveTurn fires onEvent for the tool partials across torn chunks + folds the reply; extension wiring posts 4 tool-use `ontum:turn-delta`s — name on the start — all before the terminal `ontum:turn-reply` whose html carries the folded tool-use + tool-result blocks; shell label). Updated row 6's `stream.test.js`: its one `tool_use→null` stub now asserts the row-7 streaming behaviour (count unchanged at 15). Rows 1–6 tests still 7+8+9+12+16+15/exit 0 (no regression). Green 6→7/18. Honest scope: fold + render + partial-stream plumbing + host message protocol proven host-free against the captured tool-call shapes (no billed model call); pixel-level DOM mutation needs a webview host and a real tool-calling turn is a human's to run. Next: row 8 (render diffs / edits with accept–reject).
- 2026-06-24 ~06:50 tick: **row 8 → green** — render diffs / edits with accept–reject. Added `diff.js` (`isEditTool`/`splitLines`/`diffLines`/`diffFromToolUse`): a small, deterministic common-prefix/suffix diff folds an edit `tool_use` into ordered `{tag:'context'|'del'|'add'|'hunk',text}` lines — `Edit` (old→new), `Write` (all-add), `MultiEdit` (hunk-separated edits), `NotebookEdit` (new_source add, or del on `edit_mode:'delete'`); a non-edit tool folds to null (stays row 7's JSON dump). `shell.renderDiffBlock`/`renderDiffLines` paint a `data-diff-tool="true"` block (file path + `+adds −dels` stat, `data-diff`-tagged add/del/hunk lines with a +/−/space gutter, Accept + Reject buttons carrying `data-tool-id`), all escaped, and `renderTranscriptRow` delegates edit tools to it (one source of truth with rows 3/5/7's fold); a **delegated** `ontum:diff-decision` click handler (document-level via `closest`) posts `{decision,toolId}`, locks the controls, records the choice — so diffs spliced in by the live-tail (row 4) and turn-reply (rows 5–7) paths are wired too. `extension.recordDiffDecision`/`getLastDiffDecision` record the round-trip (latest wins; malformed ignored), wired into `onDidReceiveMessage`. Verified `node …/test/diff.test.js` (15 checks, exit 0: isEditTool set; diffLines prefix/suffix + pure insert/delete; diffFromToolUse Edit/Write/MultiEdit + null for non-edit; renderTranscriptRow diff + accept/reject w/ tool id + NOT the row-7 pre + escaping + non-edit keeps the pre; foldReply Edit turn → tool-use entry rendered AS the diff; shell carries the delegated handler + message type; recordDiffDecision records + ignores malformed; the webview message round-trip via the real onDidReceiveMessage; sendPrompt's turn-reply html carries the folded diff — fake engine, no billed call). Rows 1–7 tests still 7+8+9+12+16+15+15/exit 0 (no regression). Read-only smoke: scanned the 3 live sessions for this cwd — none holds an edit tool-use (all "Warmup"), so the fold is proven host-free by the test, not faked against an absent edit. Green 7→8/18. Honest scope: the diff fold + render + the accept/reject AFFORDANCE + its decision round-trip are proven host-free; applying/reverting an edit on disk is the host permission flow's job (row 9) / a human's, and pixel-level DOM mutation needs a webview host. Next: row 9 (permission prompts — canUseTool / permission-mode surface).
- 2026-06-24 ~07:20 tick: **row 9 → green** — permission prompts (the permission-mode surface). Grounded `inherit` live this tick: `claude --help` advertises `--permission-mode` (choices `acceptEdits, bypassPermissions, default, plan`) + `--allowedTools`/`--disallowedTools` + `--dangerously-skip-permissions` (no `--permission-prompt-tool`/control-request flag in 2.0.19). Taught `engine.js` the surface: `PERMISSION_MODES` (the exact advertised four), `normalizePermissionMode` (unknown → `default`, so a typo/hostile value never escalates to bypass), and `engineArgs` now threads the normalized mode into `--permission-mode` + emits `--allowedTools`/`--disallowedTools` for a non-empty allow/deny policy (the canUseTool levers the CLI exposes). Wired the surface: `shell.renderPermissionControl` paints the composer's permission `<select>` (four modes, current `selected`, `data-mode` mirror, per-mode human label, unknown shown as `default`) + a `change` handler posting `ontum:set-permission-mode`; `extension.setPermissionMode`/`getPermissionMode` record + normalize the chosen mode, `sendPrompt` threads it into the driven turn (was hardcoded `default`), `renderPanel` re-paints in the in-force mode. Verified `node …/test/permission.test.js` (14 checks, exit 0: PERMISSION_MODES matches advertised; normalize passes four + defaults the rest incl. null/`BYPASS`/objects; engineArgs each mode + normalizes bad + defaults no-opts + emits/omits allow-deny; renderPermissionControl four options + current selected + never auto-selects bypass; renderShell select + host-passed selected + the change handler; setPermissionMode records valid + normalizes bad; webview `ontum:set-permission-mode` round-trip via the real onDidReceiveMessage sets `plan` then falls back to `default` on a bad value; sendPrompt drives a turn whose spawned argv carries `--permission-mode plan` via a fake spawn that captures argv — no billed call, plan turn folds clean). Rows 1–8 tests still 7+8+9+12+16+15+15+15/exit 0 (no regression). Green 8→9/18. Honest scope: the permission-MODE surface + allow/deny policy (the levers the `--print` CLI exposes, verified live) flow into the real engine argv, host-free; an interactive mid-turn `canUseTool` control-request callback is an SDK/persistent-session feature whose live event shape this CLI version does not advertise, so it is NOT claimed — a real billed turn exercising a live prompt is a human's to run. Next: row 10 (slash commands).
- 2026-06-24 ~10:05 tick: **row 11 → green** — plan mode. The bridge spike resolved this row to `inherit`: `--permission-mode plan` is already threaded into the drive argv by `engine.engineArgs` (row 9). Plan mode runs the engine READ-ONLY and, when the plan is ready, the engine calls the `ExitPlanMode` tool (the name this CLI version's tool registry exposes) presenting the plan and asking the human to approve proceeding. Built the genuine plan-mode SURFACE beyond the mode value: added `plan.js` (`isExitPlanTool` recognises `ExitPlanMode`/legacy `exit_plan_mode`; `isPlanMode` normalized; `planFromToolUse` folds the ExitPlanMode `tool_use` → `{name,toolId,plan}`, a non-string/missing plan folds to empty never faked; `nextModeOnPlanDecision` EXITS plan on approve — conservatively to `default`, never escalating to acceptEdits/bypass — and stays in `plan` on keep / unknown). Wired the surface: `shell.renderPlanBlock` paints an ExitPlanMode `tool_use` as a `data-plan-tool="true"` plan card (the proposed plan text + Approve & proceed / Keep planning buttons carrying `data-tool-id`), `renderTranscriptRow` delegates the ExitPlanMode tool to it BEFORE the row-8 diff / row-7 JSON paths (one source of truth), `renderShell` shows a read-only plan-mode banner (`ontum-plan-badge`, `data-region="plan-mode"`) only when the in-force mode is `plan`, and a **delegated** `ontum:plan-decision` click handler (document-level via `closest`, so plan cards spliced in by the live-tail/turn-reply paths are wired too) posts `{decision,toolId}` + locks the controls. `extension.recordPlanDecision`/`getLastPlanDecision` record the round-trip and — on approve — transition `permissionMode` out of `plan` then re-render the panel in the now-in-force mode; wired into `onDidReceiveMessage`. Verified `node vscode/ontum-surface/test/plan.test.js` → 17 checks, exit 0 (isExitPlanTool set; isPlanMode normalized; planFromToolUse fold + null for non-plan + empty-on-odd-input; nextModeOnPlanDecision exit-on-approve/stay-on-keep/keep-current-on-unknown, never bypass; engineArgs carries `--permission-mode plan`; renderTranscriptRow paints the plan card + approve/keep w/ tool id + NOT the row-7 pre + HTML-escaping + a non-plan tool keeps its row-8 diff/row-7 pre; renderShell carries the delegated handler + the plan banner only in plan mode; foldReply folds a plan-proposing turn into a tool-use entry rendered AS the plan card; recordPlanDecision records + exits plan on approve / stays on keep / ignores malformed without clobbering; the webview `ontum:plan-decision` round-trip via the real onDidReceiveMessage exits plan mode; sendPrompt drives a `--permission-mode plan` turn whose reply html carries the plan card — fake spawn, no billed call). Full suite re-run: all 12 test files PASS (no regression). Green 10→11/18. Honest scope: the plan-mode surface — the read-only banner, the ExitPlanMode plan card, the approve/keep AFFORDANCE, its decision round-trip, and the exit-plan-mode permission transition — is proven host-free against the captured ExitPlanMode shape and the live-verified `--permission-mode plan` lever; running the approved work is the engine's job under the exited mode, and a real billed plan turn is a human's to run. Next: row 12 (@-mentions / IDE selection context).
- 2026-06-24 ~07:50 tick: **row 10 → green** — slash commands. A slash command is PASS-THROUGH to the engine (the spike's `inherit`): it rides the SAME stream-json prompt channel row 5 drives, the engine interprets it. Added `slash.js`: `isSlashCommand`/`parseSlashCommand` recognise + split a composed command (`/git:commit tidy` → `{name,args,raw}`; conservative — bare `/`, `//`, `/ ` are not commands), and `discoverCommands`/`listSlashCommands` fold the SAME on-disk store the CLI reads (`<cwd>/.claude/commands/*.md` + `~/.claude/commands/*.md`, namespaced by subdir → `git:commit`, description from front-matter or first body line) merged with common built-ins, deduped + sorted, **project shadows user shadows built-in**. Wired the surface: `shell.renderSlashMenu`/`renderShell` paint the composer's slash palette (selectable `data-command` items, escaped) + a filter-as-you-type/`/`-prefix script that fills the composer with `/<name> ` on pick (never blocks an unlisted command); `extension.readSlashCommands` discovers the project+user store on each render and passes it to the shell; a composed slash command rides the existing `ontum:send-prompt`→`sendPrompt`→`driveTurn` path unaltered. Verified `node …/test/slash.test.js` (13 checks, exit 0: recognise/parse tables; discoverCommands over a fake store — namespaced + frontmatter + non-md ignored + missing-dir tolerant + sorted; listSlashCommands merge/dedup/**project shadows built-in `help`**; renderSlashMenu escaped items + empty note; renderShell palette + filter/fill wiring; extension.readSlashCommands finds the project commands; encodeUserMessage sends `/command` verbatim; a webview-sent `/compact` drives a turn whose **stdin got the exact `/compact`** — fake spawn, no billed call). Rows 1–9 tests still 7+8+9+12+16+15+15+15+14/exit 0 (no regression). Read-only smoke: this cwd has no `.claude/commands` dir yet, so live discovery returns built-ins only (honest — the custom fold is proven host-free by the test, not faked against an absent store). Green 9→10/18. Honest scope: the engine stays authoritative on what a command DOES + the full built-in set; the surface recognises + discovers + offers + routes, and a real billed `/command` turn is a human's to run. Next: row 11 (plan mode — `--permission-mode plan`).
- 2026-06-24 ~11:13 tick: **row 13 → green** — MCP tools available + invocable. The bridge spike resolved this row to `inherit`: MCP is provided by the SAME `claude` binary reading the SAME cwd config, so a configured server is LOADED by the engine and its tools ride along in the init event's `tools` list named `mcp__<server>__<tool>` — "available" is that live list, "invocable" is the engine calling the tool mid-turn (pass-through), pre-authorizable by the row-9 allow-list. Grounded `inherit` live this tick: `claude --help` advertises `--mcp-config <configs...>` + `--strict-mcp-config` + the `mcp` subcommand + `--allowedTools`/`--disallowedTools` (which authorize `mcp__server__tool` names). So the surface owns only the SURFACE of MCP — added `mcp.js` (pure, no `vscode`): `parseMcpTool`/`isMcpTool`/`mcpToolName` recognise + split + join the `mcp__server__tool` identifier (split on the FIRST `__` so a tool name may itself contain `__`); `mcpServersFromTools` groups the LIVE init tools list by server (sorted, deduped — the proof of what loaded); `transportOf`/`readMcpServerNames` fold a config's `mcpServers` (type|url→http|command→stdio|unknown, missing/torn tolerant → []); `discoverMcpServers` merges the project `<cwd>/.mcp.json` + the user `~/.claude.json` mcpServers with **project shadowing user** (the precedence the CLI honours); `listMcpServers` annotates each configured server with the live tools it exposes + an honest `available` flag (configured-but-not-loaded → available:false; an env-named server in no config → scope `live`). Threaded `engine.engineArgs` with `--mcp-config`/`--strict-mcp-config` (additive — emitted only when a config is supplied, so the default cwd-inheriting drive is unchanged). Wired the surface: `shell.renderMcpPanel`/`renderShell` paint the Sessions-aside MCP region (a `data-mcp-server` block per server with its scope/transport, a `data-available` flag, and its tools as `data-mcp-tool="mcp__server__tool"` items — the exact id the engine names + the allow-list authorizes — all escaped, honest empty note); `extension.readMcpServers` discovers the configured servers each render and annotates them with `lastEngineTools` (the live tools list captured from each turn's init event in `sendPrompt`), re-rendering after a turn so the panel reflects the now-loaded servers. Verified `node vscode/ontum-surface/test/mcp.test.js` → 17 checks, exit 0 (parseMcpTool/isMcpTool/mcpToolName split+reject+round-trip incl. a `__`-containing tool name; mcpServersFromTools groups+sorts+dedups + ignores non-MCP; transportOf shapes; readMcpServerNames fold + missing/torn → []; discoverMcpServers merge + **project shadows user** + sorted/deduped; listMcpServers available-when-live + configured-not-loaded honest + a live-only server tagged scope `live`; renderMcpPanel escaped blocks + the exact `mcp__server__tool` id + hostile-name escaped + empty note; renderShell ships the MCP region with/without servers; engineArgs adds `--mcp-config`/`--strict-mcp-config` only when supplied + strict is opt-in; extension.readMcpServers discovers a fake project `.mcp.json` then, after a turn whose init event named `mcp__weather__forecast`, marks weather available + db honestly not-loaded — fake spawn, no billed call; a READ-ONLY live smoke folds the real `~/.claude.json` without throwing). Full suite re-run: all 14 test files PASS (no regression). Read-only smoke: this machine's `~/.claude.json` carries one user-scope server, no project `.mcp.json` in the worktree (honest — the live tools list is null until a real billed turn, so `available` is false until the engine loads it). Green 12→13/18. Honest scope: the engine stays authoritative on what an MCP tool DOES and on whether a server actually connects; the surface recognises + discovers + offers + annotates, and a real billed turn that invokes an MCP tool is a human's to run. Next: row 14 (environment honoured).
- 2026-06-24 ~10:30 tick: **row 12 → green** — @-mentions / IDE selection context. An @-mention is PASS-THROUGH CONTEXT to the engine (the spike's `inherit`, like slash commands), and the live editor selection is attached as a marked, deterministic preamble. Added `mentions.js` (pure, no `vscode`): `isMention`/`parseMentions` recognise + extract every `@path` in order (trailing punctuation trimmed; email `@`, bare `@`, `/path` left alone), `mentionQuery` returns the partial token at the caret (or `''` for a bare trailing `@`, null outside a token), `discoverFiles`/`listMentionTargets` breadth-first fold the workspace tree (skips `.git`/`node_modules`/`dist`/… + dotfiles, capped at a 500-file limit, missing dir → `[]`, sorted), `fileToMention` normalises to posix separators, and `selectionContext`/`withSelectionContext` fold an editor selection record (1-based lines, single-line → `:N`) into a `[selection <file>:<a>-<b>]` preamble prepended to the prompt — a no-selection prompt is returned **unchanged** (so rows 5–11's verbatim-stdin tests still hold). Wired the surface: `shell.renderMentionMenu`/`renderShell` paint the composer's @-palette (selectable `data-mention` items, escaped, honest empty note) + an input-driven filter (`MENTION_TAIL` partial-token match via `indexOf`) that on pick replaces the `@token` with `@<path> `, Escape closes it alongside the slash palette; `extension.readMentionTargets` discovers the cwd's files each render + passes them to the shell, `currentSelection` reads `window.activeTextEditor`'s selection (empty → null, never throws) behind a `__setSelectionForTest` seam, and `sendPrompt` threads `withSelectionContext(base, currentSelection())` into the existing `driveTurn` path. Verified `node vscode/ontum-surface/test/mentions.test.js` → 15 checks, exit 0 (isMention/parseMentions incl. email/bare/slash; mentionQuery at/outside a token; discoverFiles skips noise + dotfiles + honours the limit + tolerates a missing/no dir; selectionContext/withSelectionContext fold + unchanged-without-selection; renderMentionMenu escaped items + empty note; renderShell carries the palette + filter/replace wiring + a no-targets container; extension.readMentionTargets discovers the cwd files; a turn driven WITH an injected selection carries `[selection src/app.js:3-5]` on stdin; a turn with NO selection sends the typed `@README.md` verbatim — fake spawn, no billed call). Full suite re-run: all 13 test files PASS (no regression). Read-only smoke: folded the live `vscode/ontum-surface` tree host-free → bounded file list, noise dirs skipped. Green 11→12/18. Honest scope: the engine stays authoritative on what an @-mention actually pulls in; a live editor selection comes from the host (`window.activeTextEditor`) — proven host-free against a record + the injected seam, not a live host; a real billed turn carrying a selection is a human's to run. Next: row 13 (MCP tools available + invocable).
- 2026-06-24 ~11:35 tick: **row 14 → green** — hooks / skills / settings inherited (the environment). The bridge spike resolved this row to `inherit`: the engine is the SAME `claude` binary reading the SAME cwd config, so the hooks, skills, and settings configured on disk are LOADED + RUN by the engine for free — the surface owns only the SURFACE of the environment (discover + display), proven host-free against a fake on-disk config exactly as `mcp.js` folds the MCP config. Added `environment.js` (pure, no `vscode`): `settingsPathsFor`/`readSettingsSource`/`discoverSettings` fold the settings LAYER search path the CLI reads (user `~/.claude/settings.json` → project `<cwd>/.claude/settings.json` → local `settings.local.json`, increasing precedence), each layer `present`-flagged with its sorted top-level keys (a missing layer → present:false, a torn-but-existing file → present:true keys:[] — honest, never invented); `hooksFromSettings`/`discoverHooks` fold the CLI hooks shape (`{<Event>:[{matcher?,hooks:[{type,command}]}]}`) to a flat `[{event,matcher,type,command,scope}]` merged across all layers, sorted by (event,matcher,command), tolerant of odd shapes; `frontMatterValue` reads a SKILL.md front-matter key as an inline scalar AND a folded block scalar (`description: >` — gathers more-indented continuation lines joined with spaces, the shape the real skills use); `readSkillMeta`/`discoverSkills`/`skillsDirFor`/`listSkills` fold `<base>/.claude/skills/*/SKILL.md` (a skill with no/torn SKILL.md is still surfaced under its dir name) with **project shadowing user**; `listEnvironment` combines the three folds → `{settings,hooks,skills}`. Wired the surface: `shell.renderEnvPanel`/`renderShell` paint the Sessions-aside Environment region (three labelled groups — settings layers as `data-env-settings="<scope>"`+`data-present`+keys, hooks as `data-env-hook="<event>"`+matcher+scope, skills as `data-env-skill="<name>"`+description+scope — all escaped, honest per-group empty notes, subtitle counts) + its CSS; `extension.readEnvironment` folds `listEnvironment({projectDir:cwd,userDir:homedir})` each render (degrades to empty groups, never throws) and passes it to the shell. Verified `node vscode/ontum-surface/test/environment.test.js` → 22 checks, exit 0 (settingsPathsFor precedence; discoverSettings present-flags + sorted keys + missing→present:false + torn→present:true keys:[]; hooksFromSettings flat fold incl. event-wide empty matcher + odd-shape tolerance; discoverHooks merge across 4 layers scoped+sorted; frontMatterValue inline + quoted + folded `>` block scalar + absent→''; readSkillMeta fold + no-SKILL.md-still-surfaced; discoverSkills sorted + missing→[]; listSkills **project shadows user** deduped/sorted; listEnvironment combines; renderEnvPanel escaped groups + hostile-name escaped + per-group empty notes; renderShell ships the Environment region with/without an environment; extension.readEnvironment discovers a fake project+user config — fake `vscode`/`os.homedir`; a READ-ONLY live smoke folds the real `~/.claude` + project `.claude` without throwing). Full suite re-run: all 15 test files PASS (no regression). Read-only smoke: this machine's real config folds clean — the project `.claude/settings.json` (hooks: PreToolUse/PostToolUse/SessionStart/Stop/UserPromptSubmit) + `.claude/skills/*` + the user `~/.claude` layers, all surfaced host-free. Green 13→14/18. Honest scope: the engine stays authoritative on what a hook DOES and on whether a skill actually loads (it runs them mid-turn — the init event proves the environment is loaded); the surface discovers + displays the inherited environment, and a real billed turn that fires a hook or invokes a skill is a human's to run. Next: row 15 (image/file attach).
- 2026-06-24 ~12:11 tick: **row 15 → green** — image / file attach. Normal Claude Code attaches an image (drag/paste/pick) or a file to a message; the attachment rides the turn as a content block in the SAME Messages-API user envelope row 5 drives (an image as a base64 `image` block, a PDF as a base64 `document` block, a text file inlined as a marked `text` block) — the engine reads it, the surface does not re-implement vision. So the surface owns only the SURFACE of attachments. Added `attach.js` (pure, no `vscode`): `mediaTypeForImage`/`isImageFile`/`classifyAttachment` classify a file by name → image (png/jpg/jpeg/gif/webp) / document (pdf) / text (everything else — conservative, an unknown type is never guessed to be an image the engine would reject); `readAttachment` folds the on-disk bytes into a bounded record (base64 `data` for image/document, utf8 `text` for a text file), capped at 5 MB and refusing a missing/oversized/unreadable file with an honest error record (never throws, never silently truncates — the cap binds both the stat AND the actual buffer length), injectable `fs` so a test folds a real temp file; `attachmentBlock`/`attachmentBlocks` encode the record(s) into the Messages-API content blocks (image→`{type:'image',source:{type:'base64',media_type,data}}`, document→`{type:'document',…}`, text→`{type:'text',text:'[file <name>]\n<content>'}`), dropping an errored attachment; `displayAttachment` is a data-free tray view (name/kind/bytes/error — the base64 never reaches the DOM). Extended `engine.encodeUserMessage(text, attachments)` to fold the blocks AHEAD of the typed text (the convention the API expects — the image/file leads, the question follows), admitting only well-formed `{type:string}` blocks; with NO attachments the content is EXACTLY `[{type:'text',text}]`, so every earlier row's verbatim-stdin proof (the prompt is `content[0].text`) still holds — row 15 is purely additive — and threaded `o.attachments` through `driveTurn`. Wired the surface: `shell.renderAttachTray`/`formatBytes` paint the composer's attachment tray (a `data-attachment` chip per staged file with its kind+size or honest error, a `data-attach-name` Remove button) + a 📎 attach button posting `ontum:attach-file` + a delegated remove handler posting `ontum:remove-attachment`; `extension.addAttachment`/`removeAttachment`/`getAttachments`/`pickAttachment` stage (de-dup by name) + drop + view + pick the attachments behind a `__setAttachPickerForTest` seam (production opens `vscode.window.showOpenDialog`), `sendPrompt` encodes the staged set into blocks ahead of the prompt then CLEARS it after the turn (the attachment rides exactly one turn), `renderPanel` paints the tray. Verified `node vscode/ontum-surface/test/attach.test.js` → 26 checks, exit 0 (classify image/document/text + case-insensitive + unknown→text; isImageFile/mediaTypeForImage; readAttachment folds a REAL 1×1 PNG → base64 round-trips the bytes + a text file → inlined utf8 + missing→`not found` + oversized→`too large` no throw; attachmentBlock image/text/null + attachmentBlocks drops the errored; displayAttachment data-free; encodeUserMessage no-attach is EXACTLY `[{type:text}]` + folds blocks AHEAD + admits only well-formed; renderAttachTray escaped chips + remove wiring + honest error + empty→'' + renderShell ships the tray+button+wiring with/without attachments; extension addAttachment/getAttachments data-free stage + de-dup + removeAttachment; the round-trip — a webview attach via the injected picker stages the PNG, the next driven turn's stdin carries it as a base64 image block AHEAD of the typed prompt, the staged set CLEARS after the send, and a plain turn still sends exactly the text block — fake spawn, no billed call). Full suite re-run: all 16 test files PASS (no regression). Read-only honest scope: the engine stays authoritative on what an attachment MEANS (vision/PDF reading); the LIVE file picker is the VS Code host dialog (`window.showOpenDialog`) — proven host-free against a path via the seam, not a live host; a real billed turn carrying an image is a human's to run. Green 14→15/18. Next: row 16 (resume/continue a session — the first of the two remaining `spike`-source rows).
- 2026-06-24 ~12:40 tick: **row 16 → green** — resume / continue an existing session. The spike resolved this to `inherit`; grounded live this tick: `claude --help` advertises `-c, --continue` (the most recent conversation), `-r, --resume [sessionId]` (a specific one), and `--fork-session` (when resuming, create a NEW session id; valid with either). Taught `engine.js` the surface beyond the existing `--resume` pass-through: `engineArgs` now also emits `--continue` (`o.continueSession`) and emits `--fork-session` ONLY alongside a resume OR a continue (a lone fork has nothing to fork from — dropped; the default fresh-session drive is unchanged); added `RESUME_MODES` (`new`/`continue`/`resume`), `normalizeResumeTarget` (conservative — an unknown mode / a `resume` with no sessionId → `new`, so a turn never silently resumes the wrong session; preserves `fork`), and `resumeArgsFromTarget` (one source of truth mapping a target → the engineArgs fragment: `continue`→`{continueSession}`, `resume`→`{resume:id}`, `+fork`→`{forkSession}`, `new`/id-less/lone-fork → `{}`). Wired the surface: `shell.renderResumeControl`/`renderShell` paint the composer's session-continuity control (New / Continue recent / Resume selected buttons, the in-force one `aria-pressed`, a `data-resume-mode` mirror, the Resume button carrying the row-2 selected session id + `disabled` when none is selected, all escaped) + a delegated `wireResume` handler posting `ontum:set-resume`; `extension.setResumeTarget`/`getResumeTarget` record + normalize the chosen target (a `resume` with no explicit id defaults to the selected session), `sendPrompt` threads `resumeArgsFromTarget(resumeTarget)` into the driven turn (via `Object.assign` — purely additive over rows 9/15), `renderPanel` paints the in-force target, and `ontum:set-resume` is wired into `onDidReceiveMessage`. Verified `node vscode/ontum-surface/test/resume.test.js` → 25 checks, exit 0 (RESUME_MODES; normalizeResumeTarget valid + id-less-resume→new + unknown/hostile→new + bare-string + fork-preserved; resumeArgsFromTarget new→{}/continue/resume(+fork)/id-less-resume→{}/lone-fork-dropped; engineArgs carries `--continue` / `--resume <id>` / `--fork-session` only-alongside + no-opts carries none; renderResumeControl three buttons + in-force pressed + Resume carries selected id + disabled-without-one + unknown→new + hostile id escaped; renderShell ships the region + `ontum:set-resume` + `wireResume`; setResumeTarget records continue/resume + normalizes bad; the webview `ontum:set-resume` round-trip via the real onDidReceiveMessage; selecting a session then `resume` (no id) resumes the selected one; sendPrompt drives a turn whose spawned argv carries `--resume <selected id>`, then a continue turn carries `--continue` not `--resume`, then a new-session turn carries neither — fake spawn, no billed call). Full suite re-run: all 17 test files PASS (no regression). Green 15→16/18. Honest scope: the resume/continue choice surface + its target→argv mapping (the levers the `--print` CLI exposes, verified live) flow into the real engine argv, host-free; a real billed turn that actually replays a past conversation is a human's to run at the surface. Next: row 17 (stop/interrupt a running turn — the last `spike`-source row).
- 2026-06-24 ~13:15 tick: **row 17 → green** — stop / interrupt a running turn. Grounded the spike's `inherit` live this tick: `claude --help` advertises NO interrupt/abort/control flag for the `--print` headless channel (no `--interrupt`/`--abort`/control-request option in 2.0.19), so the ONLY honest way to stop a headless turn is to TERMINATE the spawned engine process — the same `child.kill()` the watchdog uses, here user-initiated; the turn then settles via its `close` with whatever events arrived, folded by `foldReply` and LABELLED interrupted, never a faked completion. Added to `engine.js`: `INTERRUPT_SUBTYPE` (`'interrupted'`, distinct from `no-result`/`timeout`) and the pure `markInterrupted(reply)` (a shallow merge re-tagging `subtype:'interrupted'`+`isError:true` while PRESERVING the partial fold — sessionId + the entries/text that streamed before the stop + exitCode/stderr — so a stopped turn shows its honest partial result, not an empty one); `driveTurn` now hands `opts.onStart(handle)` a control handle `{interrupt(), child}` SYNCHRONOUSLY once the engine spawns, `interrupt()` kills the process (idempotent — returns false once settled, so a late Stop is a no-op), and the `close` handler resolves `markInterrupted(foldReply(events))` when interrupted; `engineArgs` carries NO interrupt flag (the honest mechanism is the signal, not an argv lever). Wired the surface: `extension.interruptTurn`/`isTurnRunning` track the in-flight turn — `sendPrompt`'s `onStart` captures the handle into `activeTurnControl`, a `finally` clears it when the turn settles, and the `ontum:interrupt-turn` message handler calls `interruptTurn()`; `shell.renderShell` paints the composer's Stop button (`ontum-compose-stop`, hidden by default), a shared `setTurnRunning(running)` shows it on submit + re-hides it on the terminal `ontum:turn-reply`, a Stop click posts `ontum:interrupt-turn`, and an `interrupted` reply renders a calm "Turn stopped." (not a red "Turn failed."). Verified `node vscode/ontum-surface/test/stop.test.js` → 19 checks, exit 0 (INTERRUPT_SUBTYPE; markInterrupted labels + preserves the partial fold + keeps a prior error + is pure + tolerates a non-object; engineArgs has no interrupt/abort/control flag; driveTurn hands onStart a handle synchronously, `interrupt()` stops a live turn (true) + kills the process, the stopped turn settles as an honest interrupted partial whose pre-stop prose survives the fold, a second interrupt after settle returns false; extension isTurnRunning false when idle + no-turn interruptTurn is a no-op, a turn in flight holds the handle, interruptTurn stops it + the reply folds interrupted + the handle drops + a late Stop is a no-op; a real `ontum:interrupt-turn` webview message kills the engine + settles interrupted; renderShell paints the hidden Stop button + posts ontum:interrupt-turn + toggles it with setTurnRunning(true/false) + branches on the interrupted subtype → "Turn stopped." — fake STALLING spawn, no billed call). Full suite re-run: all 18 test files PASS (no regression). Green 16→17/18. Honest scope: the stop surface terminates a real driven turn and labels the partial result honestly, host-free against a fake stalling engine; there is no headless mid-turn control channel in this CLI version, so stopping a real billed turn is the same process signal a human triggers at the surface. Next: row 18 (cost / usage display — the last row).
- 2026-06-24 ~13:50 tick: **row 18 → green — MARKER MET (18/18)**. Cost / usage display, the last row. The cost/usage DATA already rides the reply (`engine.foldReply` folds `total_cost_usd`→`cost`, the `usage` object, `duration_ms`→`durationMs`, `num_turns`→`numTurns` off the terminal `result` event), so this is a pure DISPLAY increment — no metering, no estimation. Added `usage.js` (pure, no `vscode`): `num` gates a value to a finite number else null (the single honesty gate — a missing/NaN/Infinity field becomes an em-dash, never a fabricated 0); `tokenTotals(usage)` folds the usage object → `{input,output,cacheRead,cacheCreation,total}` (`total` sums the four real billed token kinds, null when none reported); `foldUsage(reply)` → the one-turn model `{cost,durationMs,numTurns,tokens,hasData}` (`hasData` true iff a usage signal was reported, so a turn that errored before its result shows an honest "no usage reported", not a fake $0 row); `accumulateUsage(prev,reply)` folds a running SESSION total (pure — prev never mutated; `turns` counts only turns that reported real usage, so a Stop/spawn-error never inflates the tab); `formatUsd`/`formatTokens`/`formatDuration` (em-dash for null, deterministic) + `usageSummary`. Wired the surface: `shell.renderUsageBar(reply,sessionTotal)`/`renderShell` paint the composer's usage meter (`data-region="usage"`, per-stat `data-usage` spans + a Session-total line, honest "No usage reported yet." empty state, all escaped) + CSS, and the `ontum:turn-reply` handler swaps the `.ontum-usage` outerHTML with the host-rendered `m.usageHtml`; `extension.sendPrompt` sets `lastReply` + folds `sessionUsage = accumulateUsage(...)` after each turn + posts `usageHtml`, `renderPanel` paints the in-force meter, `getSessionUsage` exposes the total. Verified `node vscode/ontum-surface/test/usage.test.js` → 25 checks, exit 0 (tokenTotals sum/missing→null/odd→null-no-fake-0; foldUsage model + no-usage hasData:false + non-object; accumulateUsage running total + skips a no-usage turn + is pure; formatUsd sub-cent + dash; formatTokens grouped + dash; formatDuration ms/s/m·s + dash; usageSummary line + "no usage reported"; renderUsageBar engine's-own-numbers + empty state + no-reply empty + session total + escapes a hostile field; the extension round-trip — no session usage before any turn, a driven turn folds the reported cost/usage, sendPrompt posts the meter on turn-reply, two turns accumulate, a no-usage errored turn does not inflate it — fake spawn, no billed call). Full suite re-run: all 19 test files PASS (no regression). Green 17→18/18 — **parity-green, marker MET**. Honest scope: every value is a FOLD of the engine's own reported usage — the surface never meters, estimates, or invents a number; an absent field shows an em-dash, never a fabricated 0; a real billed turn's live cost is whatever the engine's own `result` reports. Next: §Marker reached closeout — self-grade GRADES.md + `/code-review ultra` peer grade + open the PR (never merge); STATE→marker-met so later ticks exit.
