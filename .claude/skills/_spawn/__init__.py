"""The shared spawn home for the skill pens (issue #411).

A package so `from _spawn.headless import headless_spawn` resolves once
`.claude/skills` is on the path. Empty by intent — the contract lives in
`headless.py`.
"""
